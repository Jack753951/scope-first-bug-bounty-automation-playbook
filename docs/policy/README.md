> Public sanitized edition: sensitive personal, target, path, IP, alias, advisory, and run-specific evidence fields are redacted. This repository does not authorize live testing.

# Policy

Active binding policy: see [`/SAFETY.md`](../../SAFETY.md) at the repo root.

This directory holds historical/archived policy material under `archive/`. Nothing in `archive/` is active authority. If `SAFETY.md` doesn't cover a case, raise it with the operator — don't restore an archived file.
