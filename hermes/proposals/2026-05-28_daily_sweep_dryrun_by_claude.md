> Public sanitized edition: sensitive personal, target, path, IP, alias, advisory, and run-specific evidence fields are redacted. This repository does not authorize live testing.

# Dry-run: daily-sweep simulated by Claude

Date: 2026-05-28 (Thursday).
Simulator: Claude Code (this session), acting as Hermes.
Purpose: validate the trimmed hermes-as-primary structure end-to-end before Hermes goes live.
Boundary: nothing in this file is real Hermes output. State files were NOT touched. Inbox was NOT regenerated.

## Setup

```
bin/hermes prompt daily-sweep | wc -l   # 911 lines
```

Prompt loads: SAFETY.md, INDEX.md, .hermes.md, autonomous_actions.md, stop_conditions.md, current_navigation.md, live_bounty_lane_queue.json, daily_sweep.md, state snapshot. Total = 911 lines. Coherent.

## Step-by-step walk-through (per `hermes/loops/daily_sweep.md`)

### Step 1 — Bootstrap check
- All required-read files present ✓
- `hermes/state/hermes_state.json` exists ✓
- Stop-condition scan: nothing tripped ✓

Verdict: pass.

### Step 2 — Active lane status refresh
- `handoff/live_bounty_lane_queue.json.lanes = []`
- Nothing to refresh.

Verdict: no-op, expected after parked-lane archive.

### Step 3 — Deadline check
- No lanes with `park_expires_at`.
- Nothing to do.

Verdict: no-op.

### Step 4 — Intake scoring
- `handoff/pending_intake.json.candidates = []`.
- `handoff/current_navigation.md` § Highest-priority operator action lists 5 recommended starting programs: Aikido Security, KU Leuven RDP, Allegro, AS Watson, Anaconda VDP.

**Structural ambiguity hit here:** policy doesn't say whether Hermes should auto-populate `pending_intake.json` from the operator's recommended list, or wait for the operator to commit.

Decision (as Hermes): do NOT auto-populate. Reasoning:
- `autonomous_actions.md` allows Hermes to add "scored candidates from Hermes intake loop". But the recommendation list in `current_navigation.md` was operator-authored, not intake-generated. Auto-importing it implies promotion without operator input.
- Operator selecting one of the 5 (or specifying another) is the gating decision. Hermes should surface the decision in inbox, then enrich the chosen one.

Action: leave pending_intake empty; surface decision in inbox (step 8).

**Finding 1: `daily_sweep.md` step 4 needs a sub-rule for "what if queue and intake are both empty AND current_navigation lists candidates?" Either auto-promote, or surface as inbox decision. Current text leaves it implicit.**

### Step 5 — CVE / KEV diff
- No active lanes → no tech stacks to match CVEs against.
- No network access in this session (dry-run constraint).

Verdict: would be no-op today. Hermes with network would pull NVD/KEV and write `intelligence/cve_briefs/cve_brief_20260528.md` regardless (already exists from earlier today).

### Step 6 — CT log diff
- No in-scope wildcards (no `programs/<slug>/scope.json` files exist after parked-lane archive).
- Skip.

Verdict: no-op.

### Step 7 — Drift detection (weekly, Monday only)
- Today is Thursday → skip per spec.

**Finding 2: spec says "skip on non-Monday" but doesn't say which Monday is "weekly". First Monday of new architecture would be 2026-06-01. Worth noting that on that date Hermes WILL run drift checks and should not be surprised by it.**

### Step 8 — Regenerate operator inbox

Current `handoff/operator_inbox_20260528.md` has 0 decisions because queue + intake both empty. Hermes' regeneration would produce:

```markdown
# Operator Inbox — 2026-05-28

Generated by Hermes daily-sweep at <ts>.

Boundary: this inbox lists decisions only. Hard stops in /SAFETY.md.

## Today's decisions (1)

### 1. Pick first-bounty target

- State: no active lanes; clean slate after parked-lane archive.
- Hermes priority: highest.

Operator needs to choose a target before any further Hermes work has meaning.
Recommended starting set (from current_navigation.md):
- Aikido Security (Intigriti, public, $50-$2,500)
- KU Leuven Responsible Disclosure (Intigriti, public)
- Allegro (Intigriti, public, €100-€4,000)
- AS Watson (Intigriti, public, $10-$8,500)
- Anaconda VDP (Intigriti, public, responsible disclosure)

Options:
- `PROMOTE <slug>` — operator names choice; Hermes enriches via Playwright
  policy fetch, drafts programs/<slug>/scope.json, creates lane_state.json,
  registers in live_bounty_lane_queue.json.
- `OTHER <slug>` — operator names a different program; same enrichment flow.
- `WAIT` — defer to tomorrow; Hermes daily-sweep re-surfaces this decision.

## Active lane snapshot

| Lane | State | Status | Deadline |
|---|---|---|---|
| (none) | | | |

## Latest CVE brief

See `intelligence/cve_briefs/cve_brief_20260528.md`.

Boundary: see /SAFETY.md. No live action authorized.
```

**Finding 3: `scripts/build_operator_inbox.py` currently emits "_No active decisions._" when queue+intake are empty. That's wrong now — the "no active decisions" case actually IS a decision (pick a target). build_operator_inbox.py should be updated to detect this state and emit a `PROMOTE/OTHER/WAIT` decision row. Or Hermes does this in daily-sweep itself.**

### Step 9 — Daily digest

Would write `hermes/digests/2026-05-28.md`:

```markdown
# Hermes Daily Digest — 2026-05-28

Generated 2026-05-28T<ts>Z (dry-run by Claude).

## What Hermes did today

- Daily sweep ran. No active lanes; intake empty.
- No CVE/KEV diff possible without active-lane tech stacks.
- No CT log diff possible without in-scope wildcards.
- Surfaced one inbox decision: pick first-bounty target.

## What changed

- Lanes: none (queue empty).
- Scope: no changes (no scope.json in active programs).
- Pending intake: no changes (empty).
- CVE matches: 0 (no lanes to match against).

## What needs operator attention

`handoff/operator_inbox_20260528.md` — pick first-bounty target (recommended set in current_navigation.md). No other decisions today.

## What Hermes is parking until tomorrow

- Nothing actionable until a lane exists.

## Drift detection

Skipped — Thursday, not Monday. Next check 2026-06-01.

## Budget

- Tokens used: ~0 (dry-run; would be modest for daily sweep when queue empty).
- Claude calls: 0
- Codex calls: 0

## Stop conditions / failures

- None.

## Findings (dry-run only)

1. Step 4 ambiguity on auto-promotion from current_navigation recommendations.
2. Step 7 has no anchor date for "weekly".
3. scripts/build_operator_inbox.py needs empty-queue handling (or move to Hermes).
4. daily_sweep.md mentions intelligence/program_briefs/ but no program_briefs are written in the daily-sweep steps — possibly out-of-step.
```

### Step 10 — Update state

Would update `hermes/state/hermes_state.json`:

```diff
   "loops": {
     "minute_alerts": {"last_run_at": null, "last_status": null},
     "hourly_diff":   {"last_run_at": null, "last_status": null},
-    "daily_sweep":   {"last_run_at": null, "last_status": null}
+    "daily_sweep":   {"last_run_at": "2026-05-28T<ts>Z", "last_status": "ok_empty_queue"}
   }
```

Would append to `hermes/state/hermes_log.jsonl`:

```json
{"ts":"2026-05-28T<ts>Z","event":"daily_sweep_complete","loop":"daily_sweep","summary":"empty queue + intake; surfaced pick-target decision in inbox","data":{"lanes":0,"candidates":0,"cve_matches":0,"stop_conditions":0,"dry_run":true,"simulator":"claude-code-session"}}
```

## Structural findings summary

| # | Finding | Severity | Suggested fix |
|---|---|---|---|
| 1 | `daily_sweep.md` step 4 doesn't say what to do when intake is empty but `current_navigation.md` lists recommended programs. Implicit decision: surface as inbox or auto-promote? | medium | Add sub-rule: "If both queue and pending_intake are empty AND current_navigation lists recommended programs, surface a `pick_target` decision in inbox. Do not auto-promote." |
| 2 | "Weekly drift detection" has no anchor; first Monday of new architecture would be 2026-06-01. | low | Note the first scheduled drift-check date in `daily_sweep.md` step 7 or `INDEX.md` § Drift-detection. |
| 3 | `scripts/build_operator_inbox.py` emits "_No active decisions._" when queue+intake empty, but the empty state IS itself a decision (pick a target). | medium | Either update the script to detect and emit `pick_target` decision, or move inbox generation entirely into Hermes daily-sweep (Hermes would write directly, deprecating the script). |
| 4 | `daily_sweep.md` mentions `intelligence/program_briefs/` but the 10 steps never write to it. Step 5 writes CVE briefs only. | low | Either add a "fetch fresh program brief for active lanes" step, or remove the program_briefs reference from daily_sweep.md. |
| 5 | The "what Hermes did today" portion of the digest (per `digests/README.md`) reads like a status report. When the answer is "almost nothing because queue empty", the digest still has shape, which is good. Validates the structure under the limit case. | (positive) | — |
| 6 | `bin/hermes prompt daily-sweep` output structure is coherent and parseable. Code-fence wrapping per file is unambiguous. | (positive) | — |
| 7 | `bin/hermes status` correctly reflects empty queue. No misleading information. | (positive) | — |

## Overall verdict (dry-run)

**Structure is workable.** Hermes can run daily-sweep from this scaffold without confusion. The four findings are minor adjustments, not blockers.

**Highest-value fix before first real Hermes run: Finding 1.** Without that sub-rule, Hermes might either (a) skip surfacing the pick-target decision and produce an empty inbox (bad — operator wouldn't know to act), or (b) auto-promote one of the 5 recommendations without operator input (worse — bypasses operator choice).

**Recommended action before real run:**
1. Edit `hermes/loops/daily_sweep.md` step 4 with the sub-rule from Finding 1.
2. Optionally fix Finding 3 (build_operator_inbox.py empty-case).
3. Optionally fix Finding 4 (program_briefs reference).
4. Real run can wait for these or proceed; findings are not blockers.

## Boundary

This dry-run did NOT modify Hermes state. Real Hermes can run the same prompt and produce real artifacts; this file documents what would happen.
