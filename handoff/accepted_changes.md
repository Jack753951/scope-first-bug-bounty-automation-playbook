> Public sanitized edition: sensitive personal, target, path, IP, alias, advisory, and run-specific evidence fields are redacted. This repository does not authorize live testing.

# Accepted Changes

## 2026-05-28 — Operator promoted hacking2 to primary workspace

- Operator clarified that old main repo `<private-workspace>` should be treated as nonexistent/inactive for current work because cleanup cost is too high.
- `<private-workspace>` is now the main/primary Cybersec Lab workspace for future work in this thread/profile context.
- Old repo lanes are not to be reconciled, imported, or used as active truth unless the operator explicitly asks.
- Updated `handoff/current_navigation.md` to record the authority boundary.
- Boundary: workspace-routing/governance update only; no target contact, scan/fuzz/exploit, credential handling, scope edit, lane import, report promotion, or submission.


## 2026-05-28 — Imported Hermes-era skills + recorded live Obsidian vault path

- Copied 5 skill packages from `<user-home>\AppData\Local\hermes\skills` into `skills/`:
  - `security/authorized-attacker-flow/` — attacker path → bounded proof workflow.
  - `security/owasp-single-vuln-lab-wave/` — single-vuln-class lab proof methodology (the workflow behind `modules/bundles/`).
  - `security/fla<api-key-redacted>/` — Flask MFA bypass code-review pattern.
  - `note-taking/multi-project-memory-routing/` — process for routing knowledge between memory / handoff / Obsidian / skills.
  - `note-taking/obsidian/` — Obsidian vault read/write skill.
- Copied 4 standalone references from retired `cybersecurity-workspace-orchestration/references/` into `skills/security/references/`:
  - `kali-vm-novnc-gui-control.md`, `<bug-bounty-platform>-alias-signup-gates.md`, `<bug-bounty-platform>-logged-in-scope-intake-via-novnc.md`, `bundle-freshness-automation.md`.
- Added a 2026-05-28 banner to each imported SKILL.md noting Hermes orchestration retired; methodology still applies; safety gate is `/SAFETY.md`.
- Recorded live Obsidian vault location: `<user-home>\Documents\ObsidianProjects\Projects\Cybersec Lab - 2` (52 files, namespaces 00_Index → 07_Daily). Added to `handoff/current_artifact_index.md` and saved as Claude Code reference memory at `~/.claude/projects/<private-workspace-slug>/memory/obsidian_cybersec_lab_vault.md`.
- Repo snapshot of old Obsidian state remains at `notes/archive_20260528/obsidian_projects/Cybersec Lab.md` for history.
- NOT imported (per single-agent restructure boundary): `red-teaming/godmode` (LLM jailbreak, off-mission), `dogfood` (Hermes self-config), `mcp/*`, `security/cybersecurity-workspace-orchestration` (Hermes-coupled; references harvested separately), and 22 unrelated category subtrees.
- Boundary: skill/reference import + index update only; no target-touching action, no scan/fuzz/exploit, no credential handling, no scope edits, no live activation.

## 2026-05-28 — Single-agent restructure Batches 6 & 7 of 8

- **Batch 6** archived `docs/strategy/{cleanup,live_bounty,platform,README}` to `docs/strategy/archive/*_20260528/`; promoted `engineering_direction_20260527.md` to `docs/strategy/CURRENT.md` and rewrote (~120 lines). Archived `notes/*` (12 items) to `notes/archive_20260528/`; future notes go to Obsidian outside repo. Archived `docs/runbook/HERMES_WORKFLOW.md`, `docs/ENGINEERING_INDEX.md`, `AGENTS.md`, `.hermes.md`. Archived single-target `nginx_*` tooling. Moved root `cve_brief_20260528.*` into `intelligence/cve_briefs/`. Rewrote `README.md` (~75 lines) and `PROJECT_CHARTER.md` (~45 lines). Deleted accidental empty files (`({i`, `x.id`) and `RESTRUCTURE_PLAN.md` (temp). Validation: lane contracts, recurring substrate, inbox summary tests all PASS.
- **Batch 7** shipped `scripts/build_operator_inbox.py` (~165 lines, pure stdlib): reads `handoff/live_bounty_lane_queue.json` + lane states + optional `handoff/pending_intake.json`, emits `handoff/operator_inbox_<YYYYMMDD>.md` with ≤5 decisions (situation + 3 options + default), an active-lane snapshot table, and latest CVE brief pointer. Created `handoff/pending_intake.json` with <program-redacted> Public Bug Bounty as the first pre-lane candidate. Ran the script — generated `handoff/operator_inbox_20260528.md` containing 3 decisions (<program-redacted> human-check, <program-redacted> TW second-account, <program-redacted> policy review).
- Boundary: structural/policy/lane-decision/tooling changes only; no target-touching action, no scan/fuzz/exploit/callback/OAST, no credential handling, no account mutation, no report promotion or submission.

## 2026-05-28 — Single-agent restructure (Batches 1–5 of 8)

Operator authorized handing project to Claude Code as single-agent driver with three binding behavioral rules: no new policy/strategy .md without driver advisory, weekly lane state change or KILL required, driver may veto new files. Restructure performed in fresh-copy repo `<private-workspace>` on branch `restructure/lean-2026-05-28`; original `<private-workspace>` preserved untouched.

- **Batch 1**: pre-restructure snapshot commit + `RESTRUCTURE_PLAN.md` classifying every top-level path.
- **Batch 2**: archived Hermes orchestration runtime to `archive/hermes_orchestration_20260528/` — `bin/hermes`, `run_hermes_worker.ps1`, `run_codex_review.ps1`, `agent_orchestration_template/`, `.hermes/`, `config/worker_roles.txt`, worker-IPC handoff files, `scripts/check-worker-attestation.py`, worker-attestation tests.
- **Batch 3**: consolidated 6 binding policies into single `/SAFETY.md` (148 lines); archived 12 superseded policies to `docs/policy/archive/superseded_20260528/`; `docs/policy/README.md` shrunk to 5-line pointer.
- **Batch 4**: collapsed `handoff/` root from ~25 items to 11 (5 .md + 4 state JSONs + 2 dirs); moved phase4/periodic-review/dated history to `handoff/archive/cleanup_20260528/`.
- **Batch 5**: lane decisions — KILL `<program-redacted>`, `<program-slug>`, `<program-redacted>` (no negative control / no signup path / high cost); KEEP <program-redacted> + <program-redacted> TW with `park_expires_at: 2026-06-04` and KILL fallback; <program-redacted> remains closed (`no_finding`); queue reduced to 2 active lanes (<program-redacted> priority 1, <program-redacted> TW priority 2). <program-redacted> Public Bug Bounty marked as highest-priority operator action; lane will be created from logged-in policy facts (operator pastes content to driver).
- Active-truth handoff files rewritten heavily: `active_strategy_queue.md`, `current_navigation.md`, `current_artifact_index.md` all collapsed to lean shape pointing to `/SAFETY.md`.
- Validation: `bash tests/test_current_live_bounty_contracts.sh` PASS after lane state changes.
- Remaining batches: 6 (archive strategy/notes variants, delete RESTRUCTURE_PLAN.md), 7 (`scripts/build_operator_inbox.py` v0), 8 (move one lane forward — operator-driven).
- Boundary: structural/policy/lane-decision changes only; no target-touching action, no scan/fuzz/exploit/callback/OAST, no credential handling, no account mutation, no report promotion or submission.

## 2026-05-28 — H1/noVNC autonomous triage promoted <program-redacted> Taiwan to prepared run card

- Confirmed Hermes can operate the existing Kali/noVNC browser via SSH + `xdotool`; no operator action was needed for browser navigation.
- Checked Intigriti candidates: <program-redacted> preview is marked `Suspended` and was killed for first-bounty execution; <program-redacted> is open but requires Intigriti login/2FA/full policy gate and was parked.
- Checked H1 Opportunity Discovery: <program-slug> private opportunity is visible but requires `Claim your spot` and may consume the `up to 1 per 30 days` private slot, so it was parked behind operator approval.
- Opened H1 <program-redacted> Taiwan policy/scope, downloaded and parsed the scope CSV locally, and created `handoff/current/h1_fresh_target_triage_20260528.md` plus `handoff/current/coupang_tw_first_bounty_run_card_20260528.md`.
- Compared the latest H1 <program-redacted> CSV against `config/scope.txt`: H1 lists 45 URL assets, while repo whitelist currently contains 10 <program-redacted> hosts; live execution remains limited to that 10-host subset unless the operator updates `config/scope.txt`.
- <program-redacted> Taiwan is now the strongest prepared first-bounty candidate from this pass; decision is `PREPARED_NOT_EXECUTED` pending live-target safe gate/operator approval.
- Boundary: policy/scope metadata only; no target testing, scanning, fuzzing, exploitation, OAST/callback, account mutation, non-owned data access, or report submission.

## 2026-05-28 — Passive enrichment for <program-redacted> and <program-redacted> candidates

- Operator instructed Hermes to keep collecting information autonomously until operator action is genuinely required.
- Created `handoff/current/passive_candidate_enrichment_20260528.md` from public metadata/docs only.
- Enriched <program-redacted> Public Bug Bounty with public preview reward signal (`$100-$5,500` visible in preview), 2FA requirement, and public <program-redacted> docs/API surface including organization members, groups, API keys, sources, permissions, search, analytics, and platform APIs.
- Enriched <program-redacted>.be Dedicated BBP with public <program-redacted> disclosure guidance and sensitive HR-data boundary notes.
- Current ranking: <program-redacted> policy review first, <program-redacted> second, latest-vuln AI/devtool lane as source/local/passive carry-forward.
- Boundary: public/passive reading only; no target testing, scanning, fuzzing, exploitation, OAST/callback, login/session scraping, account mutation, credential handling, or report submission.

## 2026-05-28 — Freshness-first bounty candidate intake

- Created `handoff/current/freshness_first_bounty_candidates_20260528.md` from public metadata sources only: CISA KEV, NVD recent critical CVEs, GitHub Security Advisories, Intigriti public program listings, and <bug-bounty-platform> public directory landing metadata.
- Shortlisted fresh-vulnerability themes including Dify, Langflow, LiteLLM, Traefik Gateway API, Open WebUI, and LiquidJS as local/passive/source-review candidates only.
- Shortlisted fresh-target policy-review candidates including Intigriti <program-redacted> Public Bug Bounty and <program-redacted>.be Dedicated Bug Bounty Program.
- Updated `handoff/current_artifact_index.md` to point to the new freshness-first candidate file.
- Boundary: public metadata and policy-review planning only; no target contact, scanning, fuzzing, exploitation, OAST/callback, account mutation, credential handling, or report submission.

## 2026-05-28 — Operator confirmed latest-vuln and latest-target as highest-value zones

- Operator clarified that the two highest-value areas are latest vulnerabilities and latest targets.
- Updated `docs/strategy/platform/engineering_direction_20260527.md` and `handoff/current_artifact_index.md` to prioritize the intersection of fresh vulnerability × fresh target.
- Added lane framing: `fresh_vuln_lane` for new advisories/PoCs/patch diffs converted into local proof/passive detector/scoped candidate, and `fresh_target_lane` for newly launched/updated/invited/scope-expanded targets mapped to access-control/business-logic bundles.
- Boundary: strategy/index update only; no target contact, scanning, exploitation, or account action.

## 2026-05-28 — Operator requested stronger attacker mindset

- Operator clarified that the project should use stronger attacker thinking.
- Updated `docs/strategy/platform/engineering_direction_20260527.md` and `handoff/current_artifact_index.md` with the stance: aggressive hypotheses, bounded execution.
- Interpretation: Hermes should think like a high-output bounty hunter, move faster through target hypotheses, and avoid treating generic caution as a blocker; live execution remains constrained by scope, program policy, owned controls, stop-before rules, and explicit operator gates.
- Boundary: planning/index updates only; no live target testing or account actions.

## 2026-05-28 — Bugcrowd login blocked; H1 and Intigriti available

- Operator reported Bugcrowd cannot be logged into from Kali/noVNC at this time.
- Operator reported the other opened platforms are logged in.
- Updated `handoff/current/target_source_intake_20260528.md` to mark Bugcrowd as registered but login-blocked, so first-bounty work does not depend on Bugcrowd.
- Boundary: platform login/source tracking only; no live target testing or credential handling.

## 2026-05-28 — Bugcrowd and Intigriti registered as target sources

- Operator confirmed registration on Bugcrowd and Intigriti.
- Added target-source intake tracker at `handoff/current/target_source_intake_20260528.md` with registered platform status and first-bounty scoring template.
- Boundary: source/intake tracking only; no live target testing, scanning, exploitation, account mutation, credential handling, or report submission.

## 2026-05-28 — Operator confirmed first-bounty capability vision

- Operator selected 30-day primary success metric `A`: first report-ready draft / submission candidate.
- Operator selected next-week posture `D`: prove the dry-run loop before forcing a live lane.
- Operator selected live automation boundary `D`: decide per lane, with dry-run/local and scoped passive defaults unless explicit operator gate/safe phrase is given.
- Operator selected report threshold `D`: depends on program bounty policy.
- Operator clarified that early phase can spend several hours of human-gated effort if it materially improves the chance of the first bounty.
- Updated `handoff/current/direction_review_synthesis_20260528.md`, `docs/strategy/platform/engineering_direction_20260527.md`, and `handoff/current_artifact_index.md` to make first-bounty outcome the active north star and demote broad detector expansion until the inbox/evidence consumer path is proven.
- Boundary: planning/index updates only; no target contact, no scan/fuzz/exploit/callback/OAST, no credential handling, no account mutation, no report submission.

## 2026-05-28 — Claude Code + Codex direction review receipts

- Created direction review packet `handoff/current/direction_review_packet_20260528.md` and called both Claude Code and Codex as read-only direction reviewers.
- Claude Code receipt: `handoff/current/claude_code_direction_review_20260528.md` with raw JSON `handoff/current/claude_code_direction_review_20260528.json`; verdict `APPROVE_WITH_CHANGES`; required context reads completed.
- Codex receipt: `handoff/current/codex_direction_review_20260528.md`; direct local read attempt hit Windows sandbox `spawn setup refresh`, then Hermes retried with embedded synchronized context packet `handoff/current/direction_review_codex_embedded_packet_20260528.md`; verdict `APPROVE_WITH_CHANGES`.
- Synthesis written to `handoff/current/direction_review_synthesis_20260528.md`.
- Boundary: read-only direction review and local handoff receipt writing only; no target contact, no scan/fuzz/exploit/callback/OAST, no credential handling, no account mutation, no report submission.

## 2026-05-28 — Repository hygiene policy and engineering index added

- Added `docs/policy/repo_hygiene_policy.md` as the active cleanup/governance policy for the messy worktree.
- Added `docs/ENGINEERING_INDEX.md` as the minimal future read-order and engineering index.
- Updated `docs/policy/README.md`, `handoff/INDEX.md`, `handoff/current_artifact_index.md`, and `README.md` to point future agents to the active index set instead of dated sprawl.
- Archived superseded or historical policy/memory files under `docs/policy/archive/2026-05-cleanup/`; archived dated root handoff files under `handoff/archive/phase_history/2026-05-27-root-handoff/`.
- Recorded cleanup checkpoint at `handoff/current/worktree_cleanup_index_20260528.md`.
- Decision recorded: do not default to `git reset --hard`, `git clean -fdx`, or a blank new repo; clean in place with freeze → classify → split commits → quarantine/archive → verify.
- Boundaries recorded: no broad deletion of scope, program lane state, evidence, logs, scans, governance, handoff, or sensitive runtime material during cleanup.
- Validation: `git diff --check` PASS; `bash tests/test_current_live_bounty_contracts.sh` PASS; `bash tests/test_recurring_substrate_dry_run.sh` PASS; `bash tests/test_operator_inbox_summary.sh` PASS; `bash ./bin/hermes review` PASS. Boundary: policy/index/archive-only repository organization; no target-touching automation executed.

- 2026-05-28: Added the first safe continuous-engineering substrate slice after the platform-pivot direction review. Normalized active live-bounty lane contracts for <program-redacted>, <program-slug>, <program-redacted>, <program-name>, <program-redacted> Taiwan, and <program-redacted> with `operator_decision` / `machine_state` mapping, made current lane states schema-valid, repaired `handoff/live_bounty_lane_queue.json` coherence, added regression `tests/test_current_live_bounty_contracts.sh`, added `schemas/operator_inbox_candidate.schema.json`, added dry-run/no-target pipeline entrypoint `platform/pipeline/recurring_substrate_dry_run.py` with regression `tests/test_recurring_substrate_dry_run.sh`, and added `platform/inbox/operator_inbox_summary.py` with regression `tests/test_operator_inbox_summary.sh` to render compact Markdown operator decisions from validated candidate batches. Follow-up strong-agent review found and Hermes fixed three safety blockers: dry-run candidate input can no longer emit `EXECUTE`, active navigation now separates env-backed signup fields from offline substrate/secret/human gates, and <program-redacted> post-human-check work now stops for result-state recording unless separate operator approval is given for mutation/control setup. Updated current navigation, active strategy queue, artifact index, and platform README to record Tier A single-live-lane discipline and Tier B recurring-substrate fallback. Validation passed: current lane contract regression, recurring substrate dry-run regression, operator inbox summary regression, live-bounty state/redaction regression, live-bounty lane runner regression, and queue runner returns expected <program-redacted> operator gate exit 10. Boundary: offline/local contract and dry-run candidate generation/rendering only; no live recurring job activation, no target contact, no scan/fuzz/DAST, no exploit/callback/OAST/tunnel, no credential/token/OTP handling, no account mutation, no report promotion, and no submission.

- 2026-05-28: Synchronized Cybersec Lab active direction to the operator’s automated multi-agent bug bounty platform north star. Updated `.hermes.md`, `handoff/current_navigation.md`, `handoff/active_strategy_queue.md`, and `notes/obsidian_projects/Cybersec Lab.md` so future workers treat the project as a recurring freshness + capability-library platform: Hermes as project owner/task router/verifier, frequent strong-agent consultation with context sync, dual-track latest-vuln lab-proof→bundle and pattern-to-in-scope-target hunting, bounded proof-only execution, env-backed signup autofill with human verification/final-submit gates, recurring minute/hour/day/week/month cadence targets, and structural priorities around contract alignment, platform migration, evidence-to-report, detector harness, artifact integrity, repo hygiene, and ROI lanes. Boundary: policy/memory/navigation synchronization only; no target-touching action, no scheduler activation, no scan/fuzz/exploit/callback/OAST/tunnel, no account action, no credential handling, no evidence deletion, and no report submission.

- 2026-05-27: Simplified growth-blocking Cybersec Lab process gates per operator direction. Rewrote active policy files so the old review-tier and multi-party approval machinery is no longer an active blocker: `docs/policy/review_tiering_policy.md` is now a growth-first review policy, `docs/policy/multi_party_review_decision_policy.md` is now advisory unless concrete blockers are named, `docs/policy/oss_recon_gate.md` is lightweight source guidance, and `docs/policy/active_testing_policy.md` keeps hard stops while allowing local/lab capability growth by default. Updated `.hermes.md`, `docs/policy/README.md`, `docs/policy/WORKER_PROMPT_TEMPLATES.md`, `docs/policy/multi_agent_tactical_review_memory_sync_rule_20260526.md`, `docs/policy/model_usage_routing_policy.md`, `docs/policy/memory_and_strategy_routing.md`, `handoff/current_navigation.md`, `handoff/active_strategy_queue.md`, `handoff/periodic_reviews/README.md`, `handoff/periodic_reviews/review_template_v0.md`, and `notes/obsidian_projects/Cybersec Lab.md` to favor focused validation, compact handoff, reusable scripts/bundles, and checkpoint-driven progress. Marked older tier/gate vocabulary in historical policy artifacts as superseded where it could be mistaken for active authority. Boundary: process/governance simplification only; no target-touching action, no scope expansion, no scanner/fuzzer/DAST/exploit/callback/tunnel, no OAuth/channel/API-token/scheduler/deployment/publishing activation, no credential/secret/PII handling, no evidence deletion, and no report-ready promotion/submission.

- 2026-05-27: Completed first-pass Hermes profile memory hygiene after creating `cybersec` and `youtube`. Wrote profile-local built-in memory files under `<user-home>\AppData\Local\hermes\profiles\cybersec\memories\` and `<user-home>\AppData\Local\hermes\profiles\youtube\memories\`, then compacted default/global memory and user profile entries into cross-project routing pointers. Updated YouTubeAgent handoff with the new `youtube` profile boundary. Boundary: memory/session/profile hygiene only; no cron/Gateway migration, no live target work, no scope/config change, no scanner/fuzzer/DAST/exploit/callback, no credential handling, and no report promotion/submission.

- 2026-05-27: Completed profile isolation smoke checks. Verified `cybersec` and `youtube` have isolated config paths, `.env` paths, profile-local memory sentinels, aliases under `<user-home>\.local\bin\`, stopped Gateways, and empty cron lists, while `default` remains the only running Gateway and retains the existing five cron jobs. Operational note: new profile memory/context requires a fresh Hermes process (`hermes --profile cybersec` / `cybersec` or `hermes --profile youtube` / `youtube`); the current default-profile session does not switch in place. Boundary: read-only profile verification and handoff update only; no cron/Gateway migration, no target-touching work, no scheduler/upload changes, and no report promotion/submission.

- 2026-05-27: Created dedicated Hermes profiles for large-project isolation: `cybersec` for `<private-workspace>` and `youtube` for `<user-home>\Desktop\youtubestrict\youtube_agent`, using `hermes profile create <name> --clone` so config, `.env`, `SOUL.md`, and skills were cloned while cron/Gateway ownership was not copied. Verified `default` remains the only running Gateway and current cron owner; `cybersec` and `youtube` Gateways are stopped and their cron lists are empty. Updated current navigation and active strategy queue so future Cybersec Lab sessions should use `hermes --profile cybersec` / alias `cybersec`. Boundary: profile/session/memory hygiene only; no cron/Gateway migration, no target-touching work, no scope/config change, no scanner/fuzzer/DAST/exploit/callback, no credential handling, and no report promotion/submission.

- 2026-05-27: Advanced <program-name> (`<program-redacted>`) from passive-only into the operator-authorized in-scope A/B owned-control attempt. Selected `<program-name>-teammate-invite-role-boundary` over broad scanning because it has higher reportability for <program-name>'s workspace/role/API shape. Attempted to invite Account B from Account A with company-admin toggle off; <program-name> returned `Teammates invitation failed — Email addresses already used by other teammates`, while the teammate table/search did not show Account B and the second browser profile displayed Account A identity rather than `+<program-redacted>-b`. Added `programs/<program-redacted>/notes/program-redacted_a3_attempt_invite_boundary_checkpoint_20260527.md`, updated `programs/<program-redacted>/lane_state.json` to `CANDIDATE_REVIEW` / `blocked_operator_action`, and ran JSON/diff/dry-run validation. Boundary: in-scope owned-account low-speed testing only; no customer/non-owned data, no DoS/rate-limit stress, no credential/cookie/token/API-key/OTP/password/phone storage, no destructive action, and no report submission.

- 2026-05-27: Checkpointed the broad Cybersec Lab cleanup migration in git commit `0412cb7` after `bash ./bin/hermes review` passed, removed accidental `kali/Logs/` staging by adding it to `.gitignore`, fixed trailing whitespace in <program-name> notes, and then resumed the <program-name> (`<program-redacted>`) practical lane only as passive noVNC observation. Started the local noVNC SSH tunnel, confirmed an authenticated <program-name> workspace was visible without CAPTCHA/login/account warning, saved a local-only screenshot pointer at `setting/local/screenshots/program-redacted_live_20260527/passive_resume_visible_front_20260527.png`, added `programs/<program-redacted>/notes/program-redacted_live_practical_resume_20260527.md`, and updated `programs/<program-redacted>/lane_state.json` plus `handoff/kali_vm_operations_state.json`. Boundary: passive owned-workspace UI mapping only; no scanner/fuzzer/DAST, target-touching script, API token/API call, channel/OAuth/mailbox/webhook/integration connection, invite/role/team mutation, workflow/rule/Topics activation, outbound message/comment/discussion, customer/non-owned data, credential/cookie/token/OTP storage, report promotion, or report submission.

- 2026-05-27: Completed cleanup closeout plus periodic third-party agent review after the tmp/GHSA verified proof.

- 2026-05-27: Continued the vuln-intel → proof-pattern loop into an explicitly requested Kali victim-lab proof and verified `<specific-ghsa-id>` / npm `tmp@0.2.5` path traversal arbitrary file creation with a marker-only local proof. Added tested runner `scripts/labs/tmp_path_traversal_safe_marker_wave1.sh`, regression `tests/test_tmp_path_traversal_safe_marker.sh`, proof packet `labs/proofs/tmp_path_traversal_ghsa_ph9p_20260527.md`, handoff `handoff/tmp_path_traversal_ghsa_ph9p_verified_local_lab_20260527.md`, and bundle `modules/bundles/verified_lab_flow_tmp_path_traversal_arbitrary_file_creation.md`; updated script inventory, current navigation, active queue, artifact index, and Obsidian. Evidence pulled from `<artifact-output-dir>/tmp_path_traversal_ghsa_ph9p_20260527T1238Z/` shows `control_inside_safe_base=true`, `escaped_inside_safe_base=false`, `escaped_marker_found=true`, and verdict `verified_tmp_path_traversal_arbitrary_file_creation_lab_only`. Victim NAT was enabled only for npm package acquisition, then disabled and verified as `INTERNET_CLOSED_EXPECTED`. Boundary: local disposable/recoverable lab only; no live target, public IP/domain, scanner/fuzzer/DAST, callback/OAST, secret/system-file access, overwrite, persistence, credential handling, report-ready promotion, or report submission.

- 2026-05-27: Added the metadata-only vuln-intel → proof-pattern automation rung. New tools: `tools/bundle_index.py`, `tools/vuln_intel_to_bundle_index.py`, and `tools/vuln_to_proof_loop.py`; updated `tools/README.md`; added regression `tests/test_bundle_freshness_delta.sh`; added scheduler prompt draft `handoff/cron_prompts/bundle_freshness_delta_prompt_20260527.md`. The flow now indexes existing proof bundles, diffs latest vulnerability-intel candidates against proof-library coverage, and emits a non-executing local-lab run-card, draft proof-pattern artifact, and live-target prerequisite map. A live run generated `handoff/vuln_intel/vuln_intel_candidates_20260527T042644Z.*`, `handoff/vuln_intel/bundle_freshness_delta_20260527T042644Z.*`, and `handoff/vuln_intel/proof_loop_20260527T042644Z/`. Boundary: metadata/planning/local file artifacts only; no local lab execution, scanner/fuzzer/DAST, PoC/exploit, browser/noVNC, account action, callback/OAST, live target request, scope edit, report submission, or persistent cron activation.

- 2026-05-27: Completed first-pass directory/policy cleanup for Cybersec Lab. Reduced `handoff/` root from 365 files to a compact current collaboration surface, archived full navigation snapshots under `handoff/archive/nav_snapshots/`, moved policy/contract files to `docs/policy/`, strategy references to `docs/strategy/`, target notes to `programs/<slug>/notes/`, local lab proof packets to `labs/proofs/`, worker run receipts to `logs/runs/`, and old phase/review history to `handoff/archive/phase_history/`. Added `handoff/INDEX.md`, `docs/policy/README.md`, `docs/strategy/README.md`, `handoff/project_cleanup_migration_plan_20260527.md`, and `handoff/project_cleanup_migration_manifest_20260527.json`. Cleanup is documentation/filesystem only; it does not authorize target-touching work.

- 2026-05-26: Completed the <program-name> (`<program-redacted>`) Account B owned/passive UI surface map after the operator completed signup/auth locally. Hermes used only low-speed Kali/noVNC observation and saved a redacted structured summary at `handoff/live_bounty_evidence/<program-redacted>/owned_account_signup_profile_workspace_surface_map/evidence_surface_map_20260526.json` plus `handoff/program-redacted_account_b_passive_surface_map_20260526.md`; local screenshots remain under ignored `setting/local/screenshots/program-redacted_live_20260526/`. Updated `programs/<program-redacted>/lane_state.json` to schema-valid `A2_SURFACE_MAP_COMPLETE` / `surface_only`, updated `handoff/live_bounty_lane_queue.json` status, and refreshed `handoff/current_navigation.md`, `handoff/active_strategy_queue.md`, and `handoff/current_artifact_index.md`. Observed only owned/passive surfaces: Customer Support shared section, `Support` and `Support - Priority` owned inbox labels, setup guide, and the hard gate to connect a first shared channel. Boundary: no Gmail/Office 365/OAuth/mailbox/channel connection, no invite/role/team mutation, no workflow/rule/Topics activation, no API token/API call, no messages/comments/discussions, no customer/non-owned data, no scanner/fuzzer/DAST, no report-ready promotion, and no report submission.

- 2026-05-26: Filled the <program-name> (`<program-redacted>`) signup form in the shared Kali/noVNC VM browser up to the operator phone gate. Used the dedicated Chromium profile `/home/kali/browser-profiles/<program-redacted>-hermes` and only entered non-secret operator-approved signup metadata: <bug-bounty-platform> alias email, safe name labels, bug-bounty company label, job title, industry `Technology`, and company size `1 - 10 employees`; left the phone field empty/focused for the operator. Updated `programs/<program-redacted>/lane_state.json` and `handoff/third_target_contact_checkpoint_20260526.json` with latest local screenshot pointer `setting/local/screenshots/program-redacted_live_20260526/form_ready_for_phone.png`. Boundary: no phone/password/OTP/CAPTCHA/email verification, no signup submission, no chat/support interaction, no scanner/fuzzer/DAST, no exploit/callback/OAST/tunnel, no customer/non-owned data, no API token/integration/billing/payment/KYC path, and no report generation/submission.

- 2026-05-26: Resumed the <program-name> (`<program-redacted>`) third-target lane only to verify the current noVNC/browser gate. Confirmed noVNC HTTP was reachable, restarted/verified the Kali VNC tunnel state, opened the dedicated Kali Chromium profile `/home/kali/browser-profiles/<program-redacted>-hermes`, navigated `https://<in-scope-host>/` -> `https://<in-scope-host>/signin` -> `Try for Free` -> official `https://<program-domain>/signup`, and captured the local screenshot pointer `setting/local/screenshots/program-redacted_live_20260526/signup_gate.png`. Updated `programs/<program-redacted>/lane_state.json`, `handoff/live_bounty_lane_queue.json`, `handoff/live_bounty_lane_runner_status.json`, `handoff/third_target_contact_checkpoint_20260526.json`, `handoff/program-redacted_pre_contact_ready_checkpoint_20260526.md`, `handoff/current_navigation.md`, `handoff/active_strategy_queue.md`, and `handoff/current_artifact_index.md` to keep the lane machine-readable as `A2_PENDING_OPERATOR_AUTH` / `blocked_operator_action`. Validation: JSON parse OK, lane queue validation OK, local-only lane runner selected `<program-redacted>` and returned expected `blocked_operator_action` / exit `10`, `git diff --check` OK for changed <program-name>/navigation artifacts, and `bash ./bin/hermes review` OK. Boundary: no signup submission, no phone/password/OTP/CAPTCHA/email verification handling, no chat/support interaction, no scanner/fuzzer/DAST, no exploit/callback/OAST/tunnel, no customer/non-owned data, no API token/integration/billing/payment/KYC path, and no report generation/submission.

- 2026-05-26: Hardened the local Hermes engineering review gate from advisory output into fail-closed validation for core offline checks. `bin/hermes review` now uses Python JSON validation when `jq` is unavailable, fails non-zero on invalid JSON, Python compile failures, shell syntax failures, active `.agent.lock`, missing worker attestation checker, or unavailable Python for attestation, while preserving route-not-run SKIP semantics for absent rolling worker results. Added `tests/test_hermes_review_fail_closed.sh` to prove a clean minimal review root passes and invalid JSON, broken Python, broken shell, active lock, and missing attestation checker fail with concrete `handoff/latest_check.md` findings. Focused validation passed: `bash tests/test_hermes_review_fail_closed.sh`, `bash tests/test_worker_context_attestation.sh`, `bash tests/test_worker_roles_vocabulary.sh`, `bash tests/test_agent_capability_substrate.sh`, `bash tests/test_live_bounty_state_and_redaction.sh`, `bash tests/test_live_bounty_lane_runner.sh`, `bash tests/test_live_bounty_preview_grounding.sh`, `bash tests/test_live_bounty_preview_synthesize.sh`, `bash tests/test_recon_gate.sh`, `./bin/hermes review`, and `git diff --check`. Boundary: local/offline review-gate hardening only; no VM start, no target request, no browser automation, no scanner/fuzzer/DAST, no exploit/callback/OAST/tunnel, no credential/PII handling, no scope expansion, no report generation, and no report submission.

- 2026-05-26: Opened the third live target, <program-name> (`<program-redacted>`), only after logged-in <bug-bounty-platform> scope confirmation from Kali/noVNC. Used the operator's already logged-in <bug-bounty-platform> session to open the <program-name> Scope page, downloaded the official scope CSV to ignored local storage at `setting/local/hackerone_scope/<program-redacted>/scopes_for_program-redacted_at_2026-05-26_08_10_14_UTC.csv`, and confirmed six in-scope assets: `<in-scope-host>`, `<in-scope-host>`, two `com.frontapp.mobile` app IDs, and <program-name> Mac/Windows executables. Created `programs/<program-redacted>/scope.json` and `programs/<program-redacted>/lane_state.json`, added only `<in-scope-host>` and `<in-scope-host>` to `config/scope.txt`, and wrote `handoff/program-redacted_first_contact_scope_and_signup_gate_20260526.md`. Dry-run scope gates passed for `https://<in-scope-host>/` and `https://<in-scope-host>/`; out-of-scope control `https://<program-domain>/` failed closed. First manual/noVNC target contact opened a separate Kali Chromium profile at `/home/kali/browser-profiles/<program-redacted>-hermes`, reached `https://<in-scope-host>/signin`, clicked `Try for Free`, and observed the official signup form at `https://<program-domain>/signup` with work email/name/company/job/industry/phone/company-size fields. Hermes stopped at `A2_PENDING_OPERATOR_AUTH` because phone/identity/signup details are operator-handled. Operator noted two phone numbers are available if needed; no phone number was recorded. Boundary: no scanner/fuzzer/DAST, exploit, callback/OAST/tunnel, customer/non-owned data, customer messages, support/chat widget interaction, API token creation/storage, credentials/cookies/tokens/OTP/password handling, billing/payment/KYC, report generation, or report submission.

- 2026-05-26: Implemented Agent capability substrate v1 as the checkpoint layer before formal third-target contact. Added `schemas/attack_path_role_synthesis.schema.json`, `schemas/kali_readiness_state.schema.json`, `schemas/no_finding_learning_seed.schema.json`, `scripts/attack-path-role-synthesize.py`, `scripts/kali-readiness-state.py`, `scripts/no-finding-learning-seed.py`, and `tests/test_agent_capability_substrate.sh`. Created current machine-state artifacts `handoff/kali_vm_operations_state.json`, `handoff/tines_surface_learning_seed_20260526.json`, compact valid JSONL `handoff/live_bounty_learning_seeds.jsonl`, and `handoff/third_target_contact_checkpoint_20260526.json`. The helpers are local/file-only: role synthesis downgrades selected lanes on REQUEST_CHANGES/BLOCKED/FAIL, Kali readiness keeps `target_touching_allowed=false`, and no-finding learning seeds reject candidate/report_ready evidence so they cannot become evidence promotion. Forbidden target-like args `--target`, `--url`, `--host`, `--scope`, and `--live` now fail closed as structured exit `30` in exact and `--flag=value` forms. Ran OSS/design recon and an architecture reviewer before implementation; first Codex deterministic review returned `REQUEST_CHANGES` for invalid JSONL and `--flag=value` exit-code drift, both fixed; second Codex review returned `PASS`. Validation: `python -m py_compile scripts/attack-path-role-synthesize.py scripts/kali-readiness-state.py scripts/no-finding-learning-seed.py`, `bash tests/test_agent_capability_substrate.sh`, JSONL per-line parse check, `python scripts/kali-readiness-state.py validate --input handoff/kali_vm_operations_state.json`, `python scripts/no-finding-learning-seed.py validate --input handoff/tines_surface_learning_seed_20260526.json`, `python scripts/check-worker-attestation.py --root .`, `bash ./bin/hermes review`, and `git diff --check`. Boundary: offline/local agent-operable substrate only; no live target request, signup/login, browser automation, scan/fuzz/DAST, exploit/callback/OAST/tunnel, VM/network action, credential handling, scope edit, evidence promotion, report generation, or report submission. Formal third-target contact remains blocked until the operator confirms <program-name>'s exact logged-in <bug-bounty-platform> asset table/signup coverage/alias readiness and explicitly approves any scope/config change.

- 2026-05-26: Tested the new worker memory-sync flow with real wrapper routes and added the narrow role-vocabulary hardening slice. A Cowork/Claude adversarial-planner smoke test ran through `bash ./bin/hermes cowork`; it produced a contract-compliant `handoff/cowork_result.md` with `REQUEST_CHANGES`, which was archived as `handoff/archive/rolling/cowork_result_role_vocab_smoke_20260526_144100.md` after the smoke test. Based on that review, implemented the smaller role-scaffolding option: added `config/worker_roles.txt` canonical hyphenated roles, `templates/role_packet_base.md`, `tests/test_worker_roles_vocabulary.sh`, and extended `scripts/check-worker-attestation.py` with role vocabulary enforcement, `other:<reason>` fallback, and `--roles-required` coverage checks. Updated `bin/hermes` so Cowork/Claude Impl/Codex validate their own output artifact immediately with `--require-present`, and updated `.hermes.md`, prompt tests, and `handoff/current_artifact_index.md` to document the enforced contract. Ran Codex as `deterministic-reviewer`; `handoff/codex_review.md` returned `PASS`. Validation: `python -m py_compile scripts/check-worker-attestation.py scripts/test_hermes_worker_context_prompt.py`, `python scripts/test_hermes_worker_context_prompt.py`, `bash tests/test_worker_context_attestation.sh`, `bash tests/test_worker_roles_vocabulary.sh`, `python scripts/check-worker-attestation.py --root .`, `python scripts/check-worker-attestation.py --root . --file handoff/codex_review.md --roles-required deterministic-reviewer`, `bash ./bin/hermes review`, and `git diff --check`. Boundary: local/static workflow hardening only; no new target-touching authority, no approval-heavy process, no scan/fuzz/browser automation/exploit/callback, no credential handling, no scope edit, no evidence deletion, and no report submission.

- 2026-05-26: Hardened worker memory-sync and multi-agent collaboration contracts. Updated `bin/hermes` worker prompts so Cowork/Claude Impl/Codex must read `handoff/current_navigation.md`, `handoff/active_strategy_queue.md`, `handoff/current_artifact_index.md`, `handoff/accepted_changes.md`, and `notes/obsidian_projects/Cybersec Lab.md`, then include `Worker identity`, `Context read attestation`, `Validation`, and `Verdict` sections in non-template results. Added `scripts/check-worker-attestation.py` and `tests/test_worker_context_attestation.sh`; expanded `scripts/test_hermes_worker_context_prompt.py`; wired the checker into `hermes review` as the `Multi-agent Artifact Gate` and made review return non-zero when present non-template worker artifacts fail the contract. The checker now requires checked context boxes, `Missing / not read: none`, non-placeholder route/tool/role/task/output fields, validation fields, and explicit verdicts. Legacy rolling worker artifacts that predated the contract were archived under `handoff/archive/rolling/*_pre_attestation_*` so future rolling results must be contract-compliant rather than silently inheriting stale non-attested outputs. Boundary: local/static orchestration hardening only; no target-touching action, worker execution, scanner/fuzzer/browser automation, credential handling, scope change, or evidence deletion.

- 2026-05-26: Performed the first repo-noise cleanup pass after writing `handoff/redundant_file_inventory_20260526.md`. Sent clear transient/debug files to the Windows Recycle Bin rather than hard-deleting them: `UsersOwnerAppDataLocalTemptmpjikmnusiout.json`, `handoff/live_bounty_lane_runner_stdout_check.json`, `handoff/live_bounty_preview_grounding_stdout_check.json`, and `handoff/live_bounty_preview_grounding_status.json`. Moved unverified root CVE draft outputs into ignored local quarantine as `setting/local/quarantine/cve_brief_20260526_unverified_do_not_use.{json,md}` and moved older tracked root CVE briefs into `cves/unverified/2026-05-22_websearch_fallback_unverified.md` and `cves/unverified/2026-05-23_websearch_fallback_unverified.md`. Added `handoff/current_artifact_index.md` as the handoff cleanup/navigation index and linked it from current navigation/active queue. Updated `.gitignore` to prevent recurrence for root temp/CVE draft outputs, `handoff/tmp/`, and `handoff/*_stdout_check.json`. Boundary: local file hygiene only; no scope change, no target-touching action, no evidence deletion from `<artifact-output-dir>/`, `scans/`, `logs/`, `setting/local/chrome-*`, or `loot/`.

- 2026-05-26: Implemented the first offline tactical-freedom platform slice for multi-agent bug-hunting. Added `schemas/attack_path_candidate.schema.json`, `templates/live_bounty_attack_path_candidate_packet.md`, `scripts/live-bounty-preview-synthesize.py`, and `tests/test_live_bounty_preview_synthesize.sh`; updated `handoff/live_bounty_tactical_preview_template_20260526.md` and `handoff/multi_agent_bug_hunting_engineering_plan_20260526.md` to use attack-path candidates, proof boundaries, proof surrogates, and stop-before rules rather than risk labels as the main planning primitive. The synthesizer is offline-only, validates selected executable lanes, preserves high-impact blocked candidates, requires at least five candidates per packet, sorts by impact potential + proof-surrogate feasibility + authorization readiness, rejects selected lanes missing concrete proof-boundary/owned-control/callback-OAST-tunnel/stop-before/proof-surrogate details, and rejects multiple selected bounded executable lanes. Also updated `tests/test_live_bounty_preview_grounding.sh` so its blocked-lane fixture no longer depends on mutable live <program-redacted> lane state. Validation: `bash tests/test_live_bounty_preview_synthesize.sh`, `bash tests/test_live_bounty_state_and_redaction.sh`, `bash tests/test_live_bounty_preview_grounding.sh`, `bash tests/test_live_bounty_lane_runner.sh`, `python -m py_compile scripts/live-bounty-preview-synthesize.py`, schema JSON parse checks, and `bash ./bin/hermes review`. Boundary: offline/local planning and validation only; no live target request, browser automation, account action, scan/fuzz/DAST, exploit execution, callback/OAST/tunnel, credential handling, non-owned data access, destructive action, DDoS/resource exhaustion, scope expansion, or report submission.

- 2026-05-26: Selected `<program-redacted>` / <program-name> as the recommended first target for the new high-hit-rate live-bounty workflow after checking candidate public <bug-bounty-platform> policy/program metadata from the Kali VM (`<attacker-vm>`) via `scripts/kali-run.ps1`. Added `handoff/program-redacted_target_selection_preview_20260526.md` with passive policy facts, candidate comparison, expanded tactical preview, selected first-lane hypothesis, and operator-safe asset/alias confirmation phrases. Boundary: passive <bug-bounty-platform> program JSON/policy intake only; no <program-name> target asset navigation, no signup/login/account creation, no scope/config authorization, no scan/fuzz/exploit, no credential handling, no customer interaction, and no report submission. <program-name> remains pending exact logged-in <bug-bounty-platform> asset-table confirmation before any `programs/<program-redacted>/scope.json` or `config/scope.txt` change.

- 2026-05-26: Added the tactical-freedom and learning-loop improvements for high-hit-rate live bounty work. New artifacts: `handoff/live_bounty_tactical_preview_template_20260526.md`, `handoff/live_bounty_no_finding_feedback_log.md`, and `handoff/live_bounty_account_ab_operator_action_card_20260526.md`. The preview template requires option expansion before narrowing, preserves creative adjacent lanes as next-preview seeds, and classifies ideas as safe-now/later-only/blocked rather than deleting them. The feedback log converts <program-redacted> and <program-redacted> no-finding/surface-only outcomes into target-selection rule updates. The operator card standardizes Account B / tenant B / object-visibility gates with safe reply phrases and explicit secret-handling boundaries. Boundary: docs/templates/strategy only; no target asset navigation, account creation, scope expansion, scan/fuzz/exploit, credential handling, or report submission.

- 2026-05-26: Added the high-hit-rate live-bounty investigation planning layer after the second live/VDP flow proved process familiarity. New passive-only artifacts: `handoff/live_bounty_high_hit_rate_target_filter_20260526.md`, `handoff/live_bounty_attack_class_matrix_20260526.md`, and `handoff/next_live_bounty_shortlist_20260526.md`. The new default is A0 OSINT/program scoring first, then one selected bug class with A/B or tenant controls, then a timeboxed A2 viability check, then A3 bounded proof only when exact policy/scope and owned controls exist. Passive <bug-bounty-platform> directory searches were used for triage only; no target asset navigation, account creation, scope expansion, scan/fuzz/exploit, credential handling, or report submission occurred. Initial high-fit candidates for later policy intake include `<program-redacted>`, `discourse`, `hex`, `frontegg`, and `airtable`, while <program-redacted> remains blocked until Account B and a safe owned object exist.

- 2026-05-25: Completed the <program-redacted> VDP first owned-account noVNC surface-map lane and closed it as `NO_FINDING_CLOSEOUT` / `no_finding` with evidence status `surface_only`. Added `handoff/tines_automation_vdp_owned_account_surface_map_20260525.md` and `handoff/live_bounty_evidence/<program-slug>/auth_session_profile_empty_state/evidence_surface_map_20260525.json`; updated `programs/<program-slug>/lane_state.json`, `handoff/live_bounty_lane_queue.json`, `handoff/current_navigation.md`, `handoff/active_strategy_queue.md`, `handoff/proof_library_live_bounty_bridge_20260525.md`, and Obsidian. Operator completed <bug-bounty-platform>-alias signup/login locally; Hermes observed normal UI only: stories dashboard/editor, credentials/resources/API-key empty states, users/settings count, authentication settings, Workbench, and account menu. No vulnerability was observed; no report packet was generated. Temporary noVNC screenshots with possible account identifiers were moved out of `handoff/` into ignored local storage, leaving promoted markdown/JSON evidence redacted. Runner now treats closed local statuses (`no_finding`, `surface_only`, `parked`) as structured `lane_closed_or_parked` checkpoints with `target_touching: false`; regression coverage was added. Boundary: no scanner/fuzzer/DAST, workflow publish/run, run-script, integrations, callbacks/webhooks, Workbench prompt/tool execution, cross-tenant/non-owned data, setting mutation, credential/resource/API-key creation, invite, secret/OTP/cookie/token retention, report submission, or <program-redacted> scope expansion beyond browser-only post-login observation.

- 2026-05-25: Sealed the live-bounty automation substrate as a completed local-only engineering milestone at `handoff/live_bounty_automation_substrate_closeout_20260525.md`. Updated `handoff/live_bounty_automation_engineering_slice_20260525.md`, `handoff/current_navigation.md`, `handoff/active_strategy_queue.md`, and `notes/obsidian_projects/Cybersec Lab.md` to state that no further schema/governance/tooling slice is active by default. The closeout records the completed substrate, current <program-redacted> queue result (`blocked_operator_action` / exit `10`), next operator gate (<bug-bounty-platform> alias signup/login in Kali/noVNC or explicit header/proxy strategy), debug-only artifacts, validation snapshot, and reopen criteria. Boundary: documentation/consolidation only; no target request, browser automation, signup/login, scanner/fuzzer/DAST, callback, exploit, workflow execution, credential handling, report submission, scope expansion, or auto-promotion of any candidate/no-finding state.

- 2026-05-25: Added the local-only live-bounty preview grounding generator so queued lanes can produce reusable, reference-grounded preview packets before any target-touching work. New files/artifacts: `scripts/live-bounty-preview-grounding.py`, `tests/test_live_bounty_preview_grounding.sh`, `handoff/references/tines_automation_vdp_auth_session_profile_empty_state_grounding_20260525.md`, and `handoff/live_bounty_preview_grounding_status.json`; updated `handoff/live_bounty_automation_engineering_slice_20260525.md`, `handoff/current_navigation.md`, `handoff/active_strategy_queue.md`, and `notes/obsidian_projects/Cybersec Lab.md`. The generator reads `handoff/live_bounty_lane_queue.json`, validates the top lane state, and writes a local-only markdown packet with OWASP WSTG/ASVS, PortSwigger Web Security Academy, public report/docs, reference-only scanner/template metadata notes, positive/negative controls, blocked techniques, stop conditions, and evidence thresholds. Target-like flags (`--target`, `--url`, `--host`, `--scope`, `--live`) fail closed as structured JSON exit `30`, including bare flags. Independent review returned `PASS`: no network/process/browser/scanner side-effect APIs, non-authorizing markdown, coherent output/status behavior. Boundary: reference generation only; no target request, browser automation, signup/login, scanner/fuzzer/DAST, callback, exploit, workflow execution, credential handling, report submission, or new approval-heavy process.

- 2026-05-25: Added the local-only live-bounty lane runner as the next automation/systematization rung. New files: `scripts/live-bounty-lane-runner.py` and `tests/test_live_bounty_lane_runner.sh`; updated `handoff/live_bounty_automation_engineering_slice_20260525.md`, `handoff/current_navigation.md`, `handoff/active_strategy_queue.md`, and `notes/obsidian_projects/Cybersec Lab.md`. The runner reads `handoff/live_bounty_lane_queue.json`, selects the highest-priority lane, validates queue/state coherence, emits machine-readable next-action JSON, and optionally writes the same output to `--status-out`. Exit codes are `0` for autonomous local action available, `10` for `blocked_operator_action`, `20` for scope/policy block, and `30` for invalid queue/state or target-like arguments. Target-like flags (`--target`, `--url`, `--host`, `--scope`, `--live`) fail closed as structured JSON; independent review first returned `REQUEST_CHANGES` for bare `--target` argparse leakage, then `PASS` after regression/fix. Current queue correctly returns exit `10` / `blocked_operator_action` for <program-redacted> because the next step is the operator identity/login gate. Boundary: local orchestration only; no target request, browser automation, subprocess probe, scanner/fuzzer/DAST, signup/login, credential handling, report submission, or new approval-heavy process.

- 2026-05-25: Implemented the first live-bounty automation/systematization substrate without adding new approval gates or target-touching behavior. Added `schemas/live_bounty_lane_state.schema.json`, `schemas/live_bounty_evidence.schema.json`, `scripts/live-bounty-lane-status.py`, `scripts/evidence-redaction-check.py`, `tests/test_live_bounty_state_and_redaction.sh`, `handoff/live_bounty_lane_queue.json`, `programs/<program-slug>/lane_state.json`, `programs/<program-slug>/lane_state_pending_second_account.json`, `handoff/live_bounty_evidence/<program-slug>/auth_session_profile_empty_state/evidence_seed_20260525.json`, and `handoff/live_bounty_automation_engineering_slice_20260525.md`. The <program-redacted> lane is now machine-readable as `A2_PENDING_OPERATOR_AUTH` / `blocked_operator_action` with explicit next operator action and next autonomous action after the identity gate; the <program-redacted> second-account lane is also parked as machine-readable state. The evidence schema rejects unreviewed promotional statuses, the lane status helper validates/summarizes queue/state/evidence and rejects missing queued state files, and the redaction checker blocks common token/PII patterns before evidence promotion while redacting findings output and allowing normal ISO dates. Independent review first returned `REQUEST_CHANGES` for queue coherence and redaction-output hygiene; both were fixed with regression tests, and second review returned `PASS`. Validation: focused TDD test passed, queue/<program-redacted>/<program-redacted> lane status validated, and <program-redacted> seed evidence/lane state redaction check was clean. Boundary: local automation substrate only; no target request, account signup/login, scanner/fuzzer/DAST, exploit, credential handling, cross-tenant test, or report submission.

- 2026-05-25: Promoted bug bounty autonomy from step-by-step assistance to a project-owner workflow. Added `handoff/live_bounty_autonomous_workflow_policy_20260525.md`, updated `handoff/current_navigation.md` and `handoff/active_strategy_queue.md`, and patched the reusable `authorized-bug-bounty-assessment` skill. New operating rule: after scope/rules and lane boundaries are established, Hermes should autonomously continue safe authorized non-sensitive work until lane/target completion or a true operator gate (auth/OTP/CAPTCHA/email/phone/local browser/payment/legal ambiguity, scope ambiguity, sensitive data, report-ready candidate, stronger technique, or final report submission). Preview/review for nontrivial lanes must ground decisions in relevant public methodology and examples such as OWASP WSTG/ASVS, PortSwigger labs, disclosed reports, official docs, OSS test cases, and scanner-template metadata as reference only. Boundary: this increases workflow autonomy but does not authorize out-of-scope targets, scanners/fuzzers/DAST, DoS, callbacks, upload/payment/run-script/integration/cross-tenant tests, non-owned data access, or report submission without their specific gates.

- 2026-05-25: Operator confirmed the <program-redacted> VDP first-flow scope gate. Added `login.<program-redacted>.com` to `config/scope.txt`, ran the <program-redacted> authorization dry-run pair, and updated `programs/<program-slug>/scope.json` plus `handoff/tines_automation_vdp_phase5a_dry_run_packet_20260525.md`. Result: `https://login.<program-redacted>.com/research/login` passed initial `safe_target` in dry-run with program policy mode, and `https://example.org/` failed closed as out of scope. The packet now contains an authorized first-flow test matrix: manual noVNC signup/login reachability, researcher identity compliance, session boundary basics, owned profile/account surface map, workspace/tenant empty-state map, and no-finding/candidate closeout. Boundary: this authorizes only the conservative first-lane manual/owned-account flow after alias/header strategy is ready; scanners/fuzzers/DAST, DoS/rate-limit testing, callbacks/webhooks, integrations, workflow execution, run-script testing, cross-tenant tests, non-owned data, public disclosure, and report submission remain blocked.

- 2026-05-25: Completed <program-redacted> VDP policy intake for the thanks-only/VDP first-complete-flow lane using only <bug-bounty-platform> program metadata (`https://<bug-bounty-platform>.com/<program-slug>?type=team`). Created `programs/<program-slug>/scope.json` and `handoff/tines_automation_vdp_phase5a_dry_run_packet_20260525.md`. Key policy facts captured: use `https://login.<program-redacted>.com/research/signup` and `/research/login`, use <bug-bounty-platform> email alias or `X-<bug-bounty-platform>-Research: [H1 username]`, avoid DoS/DDoS, do not touch non-owned accounts/data, do not disclose outside the VDP, and exclude TLS header suggestions, third-party testing, SPF/DMARC/DKIM/DNSSEC, banner/version disclosure, social engineering/phishing/spam. Updated navigation/active queue to make the next safe action operator confirmation for adding `login.<program-redacted>.com` to `config/scope.txt`; account creation/live requests remain blocked until dry-run gate and alias/header strategy are confirmed. Boundary: policy intake only; no `login.<program-redacted>.com` request, account signup, scope expansion, scan, fuzz, exploit, credential handling, or report submission.

- 2026-05-25: Created a passive thanks-only/VDP first-complete-flow shortlist at `handoff/thanks_only_vdp_shortlist_20260525.md` after querying the public <bug-bounty-platform> program directory only. Recommended `<program-slug>` as the first candidate because passive policy metadata shows dedicated research signup/login paths, with `lovable-vdp`, `getyourguide_vdp`, `hack_the_box`, and `bose_vdp` as backups. Updated `handoff/current_navigation.md` and `handoff/active_strategy_queue.md` so the next safe action is policy intake only, then `programs/<slug>/scope.json`, operator-confirmed `config/scope.txt`, and dry-run gate before account creation or target-touching. Boundary: passive program discovery/documentation only; no target asset navigation, account signup, scope expansion, scan, fuzz, exploit, credential handling, or report submission.

- 2026-05-25: Hardened the `recon.sh` authorization-gate regression suite after the first live-target dry-run. Extended `tests/test_recon_gate.sh` to cover fail-closed CLI boundaries beyond the original compatibility fix: malformed/non-lowercase program slugs fail before target processing, path-like slugs fail, `--skip-scope-check` cannot combine with program policy mode, `--policy-mode dry-run` requires `--dry-run`, and `--policy-mode` without `--program` is rejected. No production code change was needed because the existing guard behavior already satisfied these checks. Ran `scripts/post-proof-consolidation.sh --type tooling_gate_fix --artifact tests/test_recon_gate.sh --dry-run` and updated navigation/active queue/Obsidian to record the wider dry-run gate coverage. Boundary: test/docs only; no live target request, scan, exploit, fuzz, scope expansion, credential handling, or report submission.

- 2026-05-25: Added a lightweight semi-automatic post-proof consolidation flow and fixed the <program-redacted> authorization-gate dry-run compatibility blockers using focused RED/GREEN regressions. New files: `scripts/post-proof-consolidation.sh`, `tests/test_post_proof_consolidation.sh`, and `tests/test_recon_gate.sh`. `scripts/post-proof-consolidation.sh --type <type> --artifact <path> --dry-run` now prints the required post-proof index/navigation/Obsidian/bridge updates, validation commands, reusable proof metadata fields, and wrap-up fields without auto-promoting candidates or touching live targets. `recon.sh` now accepts lowercase underscore program slugs such as `<program-slug>` while retaining path-like slug rejection, and intentionally accepts `localhost` as a local-lab scope/target token so it no longer poisons global scope validation. Validation: RED tests failed before implementation for missing script / rejected `<program-slug>`; GREEN tests passed with `bash tests/test_post_proof_consolidation.sh` and `bash tests/test_recon_gate.sh`, including in-scope <program-redacted> dry-run pass and `example.org` out-of-scope rejection. Updated `handoff/proof_library_live_bounty_bridge_20260525.md`, `handoff/current_navigation.md`, `handoff/active_strategy_queue.md`, and Obsidian to mark the gate as dry-run verified while preserving the boundary that live scanner-like automation still requires a separate operator-approved plan.

- 2026-05-25: Added `handoff/proof_library_live_bounty_bridge_20260525.md` to convert verified local proof patterns into live-bounty prerequisites, blocked states, minimum live evidence, Account A/B requirements, and report-readiness thresholds. Updated `handoff/current_navigation.md`, `handoff/active_strategy_queue.md`, and Obsidian `notes/obsidian_projects/Cybersec Lab.md` so future workers read the bridge before target-touching live-bounty work. The bridge records that the <program-redacted> Taiwan single-account run had project value despite no finding: it validated the low-speed/manual live safety profile, prevented IDOR/BOLA overclaiming, produced a target-specific empty-state map, identified future Account A/B object families, and discovered the `gate_fail_closed_needs_fix` authorization-gate issue before automation. Boundary: documentation/navigation synthesis only; no additional live request, scan, exploit, fuzz, cross-account test, credential handling, VM/network change, or report submission.

- 2026-05-25: Completed a pre-second-phone <program-redacted> Taiwan single-account auth/member boundary continuation from the logged-in Kali VM/noVNC browser. Added `handoff/coupang_tw_pre_second_phone_single_account_auth_boundary_20260525.md` and updated `handoff/coupang_tw_single_account_surface_map_20260525.md`; observed `review.tw.<program-redacted>.com`, `id.tw.<program-redacted>.com`, `member.tw.<program-redacted>.com`, and `my.tw.<program-redacted>.com` with low-speed browser-assisted navigation only. Result: no reportable vulnerability; no object IDs suitable for IDOR/BOLA proof; Account B remains required. Boundary: no scanner/fuzzer/exploit, no cross-account access, no checkout/payment/KYC/upload/seller/admin/support-chat/recovery/coupon-redemption/report submission, and no credentials/OTP/cookies/tokens/phone/PII recorded. Also dry-run checked the repo safety gate and found fail-closed compatibility issues to fix before live automation: `recon.sh --program <program-slug>` rejects the underscore slug, and global scope validation currently treats `localhost` as unparseable.

- 2026-05-25: Added restart checkpoint `handoff/restart_checkpoint_20260525_hackerone_coupang_guidance.md` after Hermes/openai-codex provider timeout during the <program-redacted> Taiwan <bug-bounty-platform> guidance-draft workflow. Checkpoint records that the Kali VM/noVNC <bug-bounty-platform> draft was not submitted, Hai pre-submission checks reported blocking/not-a-vulnerability-report issues, and the recommended resume path is to find an official program-team contact/guidance route or ask operator before using Submit report for a guidance request. Boundary: documentation checkpoint only; no target-touching action, no scan/exploit/fuzz, no credentials/OTP/cookies stored, no report submission, no scope/config change, and no VM/network change.

- 2026-05-25: Continued the <program-redacted> Taiwan single-account surface map from the Kali VM after fixing auto-lock. Added minimal post-login surface hosts from the already recorded <bug-bounty-platform> program scope to `config/scope.txt` for this lane (`my.tw.<program-redacted>.com`, `myself.tw.<program-redacted>.com`, `review.tw.<program-redacted>.com`, `cart.tw.<program-redacted>.com`, `cart-<program-name>-api.tw.<program-redacted>.com`, and observed `mc.tw.<program-redacted>.com`). Captured low-speed normal-UI screenshots for home resume, My <program-redacted> order list, empty cart, cancel/return/exchange/refund records, discount coupons, and customer-service inquiry records under `setting/local/screenshots/coupang_surface_20260525/`; updated `handoff/coupang_tw_single_account_surface_map_20260525.md`. Result: no reportable vulnerability found; all observed pages are `surface_only`/empty-state, with no owned object IDs or cross-account boundary available. Coupon redemption, customer-service/auth recovery controls, checkout/payment/order/upload/seller/admin, scanner/fuzzer, and report submission remain blocked; IDOR remains `needs_second_account`.
- 2026-05-25: Added and ran `scripts/disable-kali-auto-lock.sh` against the running `<attacker-vm>` Kali VM via `scripts/kali-run.ps1` to disable Xfce screen auto-lock/blanking/DPMS for user `kali`. The script sets Xfce session, power-manager, and screensaver properties, applies `xset s off -dpms s noblank` to the current X session, stops `xfce4-screensaver` if present, and writes a persistent `~/.xprofile` fallback. Validation: remote run reported success; queried settings show lock/saver false and AC blank/sleep display 0; `bash -n scripts/disable-kali-auto-lock.sh` passed. Boundary: local Kali VM usability setting only; no target-touching action, scan, exploit, scope/config credential, NAT, or report-submission change.
- 2026-05-25: Updated the <program-redacted> Taiwan Phase 5A dry-run route after the operator created one owned account through normal local signup. Marked `handoff/coupang_tw_phase5a_dry_run_packet_20260525.md` as `one-operator-owned-account-ready; single-account-surface-map-next`; deferred the two-account cross-account IDOR lane because only one phone number/account is available. Next lane is single-account post-login surface mapping only: profile/member/my-page/cart/review/settings inventory, owned object types, object-ID provenance, read/write operation notes, and second-account dependency list. Boundary: no credentials/OTP/cookies stored, no cross-account test, no payment/order/KYC/upload/seller/admin flow, no scanner/fuzzer, no report submission.
- 2026-05-25: Advanced the <program-redacted> Taiwan Phase 5A account gate without storing account secrets. Updated `handoff/coupang_tw_phase5a_dry_run_packet_20260525.md` to mark the operator-provided two owned email addresses as received without reprinting them, attempted only official in-scope signup navigation, and stopped when `https://member.tw.<program-redacted>.com/login/memberJoinFrm.pang` returned `Access Denied`. No form was filled, no account was created, no password/OTP/phone/recovery material was requested or stored, no scan/fuzz/probe was performed, and no checkout/payment/upload/third-party-user flow was touched. Current route: operator-local signup through normal browser/network, then resume with non-sensitive labels/session/surface map.
- 2026-05-25: Completed Phase 5A pre-live-target preparation for the next <bug-bounty-platform>-authorized test and Arcane bootstrap posture. Added `handoff/hackerone_first_scope_intake_20260525.md` as a fillable scope intake packet; added `handoff/arcane_global_variables_precheck_posture_20260525.md` documenting the safe fail-closed Arcane precheck from the Windows control plane; retained public-export methodology staging under `public_exports/`; moved the unverified CVE brief artifact to ignored local quarantine at `setting/local/quarantine/cve_brief_20260525_unverified_do_not_use.md`. Arcane precheck result: blocked because Docker CLI is missing on the control-plane host; no Arcane target launched, no Docker socket mounted, no account/proof request sent. Live <bug-bounty-platform> work remains blocked until the operator supplies exact program scope/rules and Hermes converts them into `programs/<program-slug>/scope.json` plus explicit `config/scope.txt` entries.

- 2026-05-25: Implemented worker prompt required-context-read injection for `bin/hermes` so Cowork, Claude Impl, and Codex prompts now include explicit instructions to read `handoff/current_navigation.md`, `handoff/active_strategy_queue.md`, `notes/obsidian_projects/Cybersec Lab.md`, and recent `handoff/accepted_changes.md` when present. Added regression coverage in `scripts/test_hermes_worker_context_prompt.py` with fake Claude/Codex workers that capture prompts. Updated `.hermes.md` worker invocation documentation. Validation: focused unittest RED/GREEN, `bash -n bin/hermes`, and `./bin/hermes review`. Boundary: prompt/context plumbing and documentation only; no worker run against real Claude/Codex, no target-touching automation, no scope/config/credential changes.

- 2026-05-25: Drafted the fail-closed, precheck-first local bootstrap helper for Phase 5A Arcane `<specific-ghsa-id>`. Added `scripts/labs/arcane_global_variables_bootstrap_precheck.sh` and `handoff/arcane_global_variables_bootstrap_precheck_run_card_20260525.md`; updated `scripts/SCRIPT_INVENTORY.md`, `handoff/current_navigation.md`, `handoff/active_strategy_queue.md`, target catalog, and Obsidian. Boundary: script defaults to precheck only, creates artifacts, checks host-only lab assumptions, rejects default host/user Docker socket posture, and can render a review-only disposable Docker-in-Docker compose template; it does not launch Arcane, mount sockets, create accounts, send proof/exploit requests, touch public/live targets, or submit reports. Next gate: setup/proof remains blocked until the disposable Docker daemon/proxy posture is confirmed.

- 2026-05-25: Completed source/install feasibility review for Phase 5A Arcane `<specific-ghsa-id>` without launching a target or sending any proof request. Added `handoff/phase5a_arcane_global_variables_feasibility_review_20260525.md`; updated `handoff/current_navigation.md`, `handoff/active_strategy_queue.md`, and Obsidian `notes/obsidian_projects/Cybersec Lab.md`. Findings: GitHub Advisory reports `<= 1.19.1` vulnerable and `1.19.2` patched; `ghcr.io/getarcaneapp/arcane:v1.19.1` and `:v1.19.2` manifests exist; v1.19.1 `UpdateGlobalVariables` lacks `checkAdminInternal(ctx)`, while v1.19.2 adds it; default admin/bootstrap/login/user-management paths are source-identifiable. Judgment: local bootstrap is feasible only with a disposable Docker daemon/proxy posture. Boundary: no target touched, no Docker socket mounted, no scanning/exploitation, no accounts, no callbacks/OAST, no live-target work, and no report submission. Next slice may draft a fail-closed precheck-first local bootstrap script, but must not run setup/proof until the disposable Docker daemon/socket posture is confirmed.

- 2026-05-23: Selected the first Phase 5A vuln-intel local-bootstrap planning candidate: Arcane `<specific-ghsa-id>` missing admin authorization on the global variables endpoint. Added plan `handoff/phase5a_arcane_global_variables_bootstrap_plan_20260523.md` and target catalog `targets/catalog/arcane_ghsa_jpjh_jm2p_39hh.md`; updated current navigation and active queue. Selection rationale: recent GitHub Advisory, source/package metadata available, high-value role-separation/admin authorization bypass shape, and strong fit with the completed Phase 4 auth/session proof. Boundary: plan-only; no target launched, no Docker socket mounted, no exploit/proof run, no live target touched, no scanning, no credentials, no callback/OAST, and no report submission. Critical rule: Arcane must only be bootstrapped later against a disposable Docker-in-Docker or isolated victim-lab daemon, never a host/user Docker socket.

- 2026-05-23: Started Phase 5A authorized-assessment readiness and one-shot vulnerability-intelligence intake. Added `handoff/phase5a_authorized_live_target_dry_run_template.md` for legal scope package, target map, role/account matrix, candidate lane, proof plan, evidence packet, and report decision workflow; added `handoff/phase5a_report_readiness_checklist.md` for submit/not-submit conversion; added metadata-only script `tools/vuln_intel_refresh.py`; generated sample one-shot intake artifacts `handoff/vuln_intel/vuln_intel_candidates_20260523T130000Z.md`, `.json`, and `.csv` from CISA KEV, NVD recent, and GitHub Advisories where reachable. Updated `handoff/current_navigation.md`, `handoff/active_strategy_queue.md`, and Obsidian. Validation: `python -m py_compile tools/vuln_intel_refresh.py`, successful one-shot run, and `git diff --check`. Boundary: intake-only; no scanning, exploitation, target bootstrap, live-target probing, credential handling, callback/OAST, report submission, VM/network changes, or scheduler creation.

- 2026-05-23: Completed the Phase 4 ability-gap auth/session role-separation proof wave. Added admin-path fixtures to `labs/modern_vuln_api/modern_vuln_api.py`: vulnerable `/api/admin/audit-log` for a lab-owned role-separation bypass marker and secure-control `/api/admin/settings` for false-positive boundary. Added runner `scripts/labs/modern_api_auth_role_separation_wave1.sh`, handoff `handoff/modern_api_auth_role_separation_wave1_20260523.md`, bundle `modules/bundles/verified_lab_flow_modern_api_auth_role_separation.md`, transition note `handoff/phase4_closeout_phase5_entry_20260523.md`, and artifacts `<artifact-output-dir>/modern_api_auth_role_separation_20260523T124050Z/`; updated `scripts/SCRIPT_INVENTORY.md`, `handoff/proof_library_index_20260523.md`, `handoff/current_navigation.md`, `handoff/active_strategy_queue.md`, and Obsidian. Result: `verified_role_separation_bypass_lab_only` with unauth admin-audit 401, normal-user session 200, normal-user admin-audit marker 200 despite role=user, admin baseline 200, normal-user secure admin-settings control 403, admin settings 200, and post-health 200. Validation: `bash -n`, `python -m py_compile`, and bounded runner execution. Phase routing: Phase 4 now enters closeout posture; periodic vulnerability-intelligence refresh/candidate intake/target bootstrap belongs to Phase 5. Boundary: local disposable lab only; no public/live target, brute force, credential theft, sensitive data, destructive write, exfiltration, callback/OAST, or finding/report promotion.

- 2026-05-23: Accepted live-target escalation nuance for Cybersec Lab vulnerability-intelligence routing. Local/recoverable lab remains the default first proof path, but local-lab fit is not a hard exclusion filter: valuable vulnerability classes or product-specific candidates that genuinely require a live/real target should be retained as `needs_authorized_live_target` / `blocked-awaiting-scope`, and Hermes should ask the operator for target URL/app/API/product/version, authorization/program scope, in/out-of-scope actions, allowed test classes, rate limits/time windows, account/test-data availability, destructive/state-changing permission, evidence redaction rules, and callback/OAST/tunnel allowance. Updated `handoff/current_navigation.md`, `handoff/active_strategy_queue.md`, and Obsidian `notes/obsidian_projects/Cybersec Lab.md`. Boundary: documentation/navigation update only; no public target activation, no live-target authorization created by Hermes, no scanning/exploitation, no VM/network changes, no credential/loot handling, and no finding/report promotion.

- 2026-05-23: Completed WebGoat browser-runtime XSS evidence-packet hardening after the verified rerun. Added `handoff/webgoat_browser_runtime_xss_evidence_packet_20260523.md` from artifacts `<artifact-output-dir>/webgoat_browser_runtime_xss_20260523T105506Z/`; updated `handoff/proof_library_index_20260523.md`, `handoff/current_navigation.md`, `handoff/active_strategy_queue.md`, and Obsidian `notes/obsidian_projects/Cybersec Lab.md`. Packet status: `reusable_methodology` / verified local-lab runtime proof, with DOM marker, origin/path, control artifact, screenshot/DOM evidence, pre/post health, cleanup/network posture, and report-readiness boundary. Boundary: documentation/evidence consolidation only in this step; no new target-touching action, public target, credential/token theft, exfiltration, persistence, callback, shell/destructive write, real-world impact claim, or automatic finding/report promotion.

- 2026-05-23: Reran the WebGoat browser-runtime XSS safe-marker proof from `<attacker-vm>` against Docker-backed WebGoat on `<victim-vm>` (`<lab-ip>:8080`). Artifacts were pulled to `<artifact-output-dir>/webgoat_browser_runtime_xss_20260523T105506Z/`; handoff: `handoff/webgoat_browser_runtime_xss_rerun_20260523.md`. Result: `verified local-lab runtime proof` with pre/post WebGoat health 200, throwaway registration/login 302, lesson 200, DOM marker `WG_XSS_RUNTIME_webgoat_browser_runtime_xss_20260523T105506Z` on origin `http://<lab-ip>:8080` path `/WebGoat/CrossSiteScripting/attack5a`, and control request `control_attrs.xss=null`. Read-only artifact review agreed this should remain verified local-lab runtime proof, not candidate-only, while keeping strict limits: local WebGoat single route/session only, safe DOM marker only, no credential/token theft, exfiltration, persistence, callback, shell/destructive write, public target, real-world impact claim, or automatic finding/report promotion. Validation: `python -m py_compile scripts/labs/cdp_runtime_xss.py`, `bash -n scripts/labs/webgoat_browser_runtime_xss_wave1.sh`, route/posture checks, and focused pytest suite `26 passed, 30 subtests passed`.

- 2026-05-23: Verified a second clean file-read/path traversal safe-marker target by adding a bounded `/file-read?name=` fixture to `labs/modern_vuln_api/modern_vuln_api.py` and running `scripts/labs/modern_api_path_traversal_file_read_wave1.sh` from `<attacker-vm>` against victim Docker target `<lab-ip>:18083`. Artifacts: `<artifact-output-dir>/modern_api_path_traversal_file_read_20260523T094352Z/`; handoff: `handoff/modern_api_path_traversal_file_read_wave1_20260523.md`; review: `handoff/claude_review_modern_api_path_traversal_file_read_20260523.md`; bundle: `modules/bundles/verified_lab_flow_modern_api_path_traversal_file_read.md`. Result: `verified_file_read_safe_marker_lab_only` with pre-health 200, public control marker yes, missing-file control 404, traversal positive marker yes (`FILE_READ_SAFE_MARKER_HERMES_LOCAL_LAB`), post-health 200, target cleanup, and attacker/victim Internet closed. Boundary: local authorized lab only, lab-owned marker files only; no public target, sensitive system files, credentials/secrets, shell, persistence, exfiltration, automatic finding/report promotion, or real-target exploitability claim.

- 2026-05-23: Verified the dedicated `modern_vuln_api` unsafe-deserialization bounded-marker lane via the prepared operator-run script after the earlier Hermes execution-layer blocker. Operator manually ran `scripts/labs/operator_deser_bounded_marker_run.sh` from `<attacker-vm>`; Hermes pulled artifacts from attacker and victim to `<artifact-output-dir>/modern_api_deser_operator_20260523T093300Z/` and verified `pre_health: 200`, `invalid_control_status: 400`, `deserialize_marker_status: 200`, `deser_log_pre_status: 200`, `deser_log_post_status: 200`, `post_health: 200`, `marker_found: yes`, and `VERDICT=verified_bounded_marker_lab_only`. Positive response and `/deser-log` contain marker `DESER_OPERATOR_modern_api_deser_operator_20260523T093300Z` with `type: bounded_pickle_gadget`; cleanup removed `modern-api-deser-18082`; attacker/victim Internet stayed closed. Added `handoff/modern_api_deserialization_bounded_marker_operator_verified_20260523.md` and updated the deserialization bundle, proof library index, vulnerability inventory, current navigation, active queue, and Obsidian. Boundary: local authorized lab only, exactly one bounded marker trigger, no shell/arbitrary command, persistence, callback, credential access, public target, exfiltration, or automatic finding/report promotion.

- 2026-05-23: Prepared the missing operator-run path for the previously execution-layer-blocked dedicated `modern_vuln_api` unsafe-deserialization bounded-marker rerun. Added `scripts/labs/operator_deser_bounded_marker_run.sh` and `handoff/deser_operator_run_card_20260523.md`; the script is Kali-side/manual-only, supports `--precheck-only`, starts a local victim target on host-only lab IPs, records pre/post health, runs an invalid/control deserialize request, requires exact human confirmation `RUN_DESER_MARKER_ON_LOCAL_LAB`, sends exactly one marker-only `record_deser_marker(marker)` trigger, checks `/deser-log`, collects diagnostics, and cleans the target container by default. Updated `scripts/SCRIPT_INVENTORY.md` and `handoff/active_strategy_queue.md`. Boundary: script/run-card preparation only; Hermes did not execute the blocked trigger, did not bypass the safety layer, and no public target, shell/arbitrary command, persistence, callback, credential access, exfiltration, VM/network setting change, or finding/report promotion occurred.

- 2026-05-23: Added denominator-sensitive vulnerability material library coverage estimate at `handoff/vulnerability_material_coverage_estimate_20260523.md`. Current estimate: `10/10` major lab-reproducible pattern families touched at least once (`100%` family-touch coverage), `20` retained/tested capability rows in `vulnerability_test_inventory_20260523.md`, but only about `1.25%` vs full CISA KEV catalog (`20/1601`), `1.54%` vs CISA bucketed lab-pattern entries (`20/1297`), or `4.30%` vs the NVD+Exploit-DB+GitHub sample (`20/465`). Interpretation: broad pattern coverage, shallow individual-material coverage; many source-library entries remain available for future local-lab proof waves.

- 2026-05-23: Continued testing after the exploration-breadth correction with a WebGoat Path Traversal direct file-read retry via `scripts/labs/webgoat_pathtraversal_file_read_wave1.sh` from `<attacker-vm>` against Docker-backed WebGoat on `<victim-vm>`. Artifacts were pulled to `<artifact-output-dir>/webgoat_pathtraversal_file_read_20260523T090724Z/`; handoff `handoff/webgoat_pathtraversal_file_read_retry_20260523.md`. Result: `attempted-not-verified`: pre-health 200, login 302, lesson/js/control 200, raw traversal 400, encoded traversal 400, `secret_marker_found=no`, post-health 200. Project value: useful control/rejection evidence for the WebGoat direct-read lane; no verified file-read impact is claimed. Boundary: local WebGoat only, throwaway user, no public target, no sensitive system files, no credential theft/exfiltration, no persistence/shell, no report/finding promotion.

- 2026-05-23: Accepted the exploration-breadth correction for Cybersec Lab: preview must not shrink the project to only easy/already-proven lanes or become a narrowing filter. For authorized recoverable local lab work, Hermes should enumerate useful possibilities before prioritizing and try every useful proof possibility that can be bounded, sourced, artifacted, recovered, and honestly labeled. Useful non-success outcomes remain retained as `valuable-candidate`, `attempted-not-verified`, `blocked/deferred`, or `reference-only`; blocked/unsuitable paths should switch route via operator-run script, source-level proof, equivalent local target, adjacent safe-marker lane, mature OSS wrapper, or local target expansion. Updated `handoff/current_navigation.md`, Obsidian `notes/obsidian_projects/Cybersec Lab.md`, and reusable Hermes skill `owasp-single-vuln-lab-wave`. Boundary: documentation/methodology update only; no target-touching action, VM/network changes, exploit/scanner execution, public target activation, credential/loot handling, safety-bypass, or finding/report promotion.

- 2026-05-23: Accepted operator preference for repeated execution-layer blockers: if a sensitive local-lab trigger hits the same `BLOCKED` / `Do NOT retry` pattern, Hermes should not keep trying variants through its own terminal/tools. Default next action is to create a Kali-side operator-run script/run-card for the user to execute manually, modeled on the successful SSRF operator path: `--precheck-only`, exact local-lab scope, diagnostics/artifact path, human confirmation gate, one-shot trigger, cleanup/post-health, then Hermes artifact review/pullback. Updated `handoff/current_navigation.md`, Obsidian `notes/obsidian_projects/Cybersec Lab.md`, and reusable Hermes skill `owasp-single-vuln-lab-wave`. Boundary: documentation/methodology update only; no target-touching action, VM/network changes, exploit execution, safety-bypass, public target activation, credential/loot handling, or finding/report promotion.

- 2026-05-23: Accepted the five-question tactical preview checklist for every future one-vulnerability Cybersec Lab proof wave. Each wave preview must answer: maximum safe proof, whether the current target can prove it, minimum positive/control evidence, two useful fallback lanes if primary trigger is blocked, and which proof-library capability the wave adds. Updated `handoff/current_navigation.md`, Obsidian `notes/obsidian_projects/Cybersec Lab.md`, and reusable Hermes skill `owasp-single-vuln-lab-wave`. Intent: expand tactical attack-path thinking and avoid low-value "can it trigger" runs while staying script-first/context-driven/local-lab-only; this is not a new governance gate. Boundary: documentation/methodology update only; no target-touching action, VM/network changes, scanner/exploit execution, public target activation, credential/loot handling, safety-bypass, or finding/report promotion.

- 2026-05-23: Tested the newly fixed tactical preview / Claude Code review flow once on the dedicated modern_vuln_api deserialization bounded-marker lane. Completed OSS/source reconnaissance (`setting/local/oss_refs/deserialization_preview_test_20260523/README.md`), Hermes tactical preview (`handoff/modern_api_deserialization_preview_20260523.md`), bounded execution attempt, blocker record (`handoff/modern_api_deserialization_preview_test_blocked_20260523.md`), Claude Code read-only review (`handoff/claude_review_deser_preview_test_20260523.md` plus JSON usage artifact), and Hermes synthesis (`handoff/modern_api_deserialization_preview_review_process_test_20260523.md`). Result: process test passed, but the dedicated deserialization runtime rerun is `blocked/deferred` because the execution layer returned `BLOCKED: User denied. Do NOT retry`; Hermes did not retry/bypass/encode/split the trigger and does not claim new verified deserialization impact. Updated `handoff/active_strategy_queue.md` and Obsidian. Boundary: local authorized lab only; no public target, callback, shell, file-write impact, persistence, credential access, exfiltration, VM/network setting changes, scheduler changes, or automatic finding/report promotion.

- 2026-05-23: Refined the OWASP single-vulnerability lab-wave preview/review routing per operator correction. Preview is owned by Hermes; post-evidence review is owned by Claude Code via a compact read-only evidence packet; Hermes remains final synthesizer. These are tactical/project-value perspectives, not new safety gates, approval layers, or governance-first process. Updated Hermes skill `owasp-single-vuln-lab-wave`, `references/agent-preview-and-review-gates.md`, `handoff/current_navigation.md`, and Obsidian `notes/obsidian_projects/Cybersec Lab.md`. Boundary unchanged: no target-touching action, VM/network changes, public target activation, safety-bypass, credential theft, exfiltration, or automatic finding/report promotion in this documentation/process update.

- 2026-05-23: Updated OWASP single-vulnerability lab-wave workflow to add two lightweight tactical agent gates requested by the operator. New process is `OSS/source reconnaissance -> agent preview -> Kali bounded-script execution -> artifact/evidence pullback -> agent review -> verified-impact/bundle/evidence-packet promotion`. Patched Hermes skill `owasp-single-vuln-lab-wave`, added skill reference `references/agent-preview-and-review-gates.md`, and updated `handoff/current_navigation.md`, `handoff/active_strategy_queue.md`, and Obsidian `notes/obsidian_projects/Cybersec Lab.md`. Intent: increase tactical sight and tactical freedom while keeping the process script-first/local-lab-first, not governance-first. Boundary unchanged: no public target activation, safety-bypass, credential theft, exfiltration, automatic finding/report promotion, or target-touching execution in this documentation update.

- 2026-05-23: Completed SSRF true-attacker callback evidence-packet consolidation as the first post-cleanup one-vulnerability packet. Added `handoff/modern_api_ssrf_true_attacker_callback_evidence_packet_20260523.md` from already verified artifacts under `<artifact-output-dir>/modern_api_ssrf_operator_20260523T074358Z/`; packet records target/scope, route/tool, preconditions, exact trigger path, callback evidence, impact, controls, cleanup, rerun gate, and report-readiness decision `reusable_methodology`. Updated `handoff/proof_library_index_20260523.md`, `handoff/current_navigation.md`, `handoff/active_strategy_queue.md`, `handoff/vulnerability_test_inventory_20260523.md`, and Obsidian `notes/obsidian_projects/Cybersec Lab.md` so completed packetization is visible and the next top lane is dedicated deserialization bounded-marker rerun. Boundary: documentation/evidence consolidation only; no new target-touching action, VM/network changes, scanning, exploitation, callbacks, credential handling, or report/finding promotion.

- 2026-05-23: Completed a Cybersec Lab proof-library navigation cleanup after the verified SSRF/XXE/WebGoat/DVWA proof waves. Added `handoff/proof_library_index_20260523.md` as the short reusable proof-pattern map covering true attacker callback, browser-runtime XSS, file read/path traversal/XXE safe-marker, auth/session/JWT/access control, injection/server-side execution, upload/exposure triage, and candidate/attempted shelves. Updated `handoff/current_navigation.md`, `handoff/active_strategy_queue.md`, `handoff/vulnerability_test_inventory_20260523.md`, and Obsidian `notes/obsidian_projects/Cybersec Lab.md` so the next best lanes are evidence packet consolidation/report-readiness rehearsal, dedicated deserialization bounded-marker rerun, and a second file-read/path traversal/XXE safe-marker target. Boundary: documentation/navigation only; no target-touching work, VM/network changes, scanning, exploitation, public targets, credential handling, or report/finding promotion.

- 2026-05-23: Verified the modern_vuln_api SSRF true-attacker callback lane with an explicit operator-confirmed trigger. Operator ran `scripts/labs/operator_ssrf_true_callback_run.sh` from `<attacker-vm>`, confirmed `RUN_SSRF_ON_LOCAL_LAB`, and the script sent exactly one local-lab `/fetch?url=attacker-callback` request to victim `<lab-ip>:18081`. Pulled artifacts to `<artifact-output-dir>/modern_api_ssrf_operator_20260523T074358Z/` and verified `pre_health: 200`, `ssrf_trigger_status: 200`, `post_health: 200`, `callback_marker_found: yes`, `callback_source_victim_ip_found: yes`, and `callback_trigger_path_found: yes`; `callback/requests.jsonl` contains the true trigger callback from `client=<lab-ip>` to `/ssrf-callback?marker=modern_api_ssrf_operator_20260523T074358Z` with `User-Agent=HermesModernVulnAPI-SSRF-Lab`. Added `handoff/modern_api_ssrf_true_attacker_callback_verified_20260523.md` and upgraded the SSRF bundle/Obsidian note from blocked/deferred to verified-impact lab-only. Boundary: local authorized lab only, exactly one trigger, no metadata endpoints, no localhost/internal scan, no public OAST/public target, no secrets/credentials/exfiltration, no automatic finding promotion.

- 2026-05-23: Fixed the Kali-side SSRF true attacker callback operator script after the first manual attempt exposed pre-trigger route/startup issues. Diagnosis from the screenshots: the initial script had already cleanup-removed `modern-api-ssrf-18081`, causing follow-up attacker `curl http://<lab-ip>:18081/health` to time out; live diagnostics also showed Docker publishing to `0.0.0.0:18081` plus health retries makes attacker-to-victim health succeed. Updated `scripts/labs/operator_ssrf_true_callback_run.sh` to publish listener/target on `0.0.0.0`, add victim-local and attacker-to-victim health retry loops, collect diagnostics, support `--precheck-only`, and keep exact human confirmation before exactly one SSRF trigger. Verified `--precheck-only` run `modern_api_ssrf_operator_20260523T074042Z` reached `pre_health=200` and listener precheck OK without sending `/fetch`; cleanup removed containers and attacker Internet stayed closed.

- 2026-05-23: Completed a dedicated one-vulnerability modern_vuln_api XXE safe-marker proof wave after the SSRF trigger blocker, preserving capability growth without bypassing the execution layer. Followed OSS-first policy by saving PayloadsAllTheThings XXE and OWASP XXE Prevention Cheat Sheet references under `setting/local/oss_refs/xxe_safe_marker_20260523/` (generic nuclei URL fetched only 14 bytes and is marked check-later). Added `scripts/labs/modern_api_xxe_safe_marker_wave1.sh`, copied current `modern_vuln_api.py` to `<victim-vm>`, started Docker-published target `modern-api-xxe-18081` on `<lab-ip>:18081`, and ran the proof from `<attacker-vm>`. Verified positive lab-owned marker expansion (`XXE_SAFE_MARKER_HERMES_LOCAL_LAB`) plus no-entity/wrong-file controls, pre/post health 200, and `verified_impact_lab_only` verdict. Cleaned the target container, verified attacker/victim Internet closed, pulled artifacts to `<artifact-output-dir>/modern_api_xxe_safe_marker_20260523T070157Z/`, added `handoff/modern_api_xxe_safe_marker_wave1_20260523.md`, and updated bundle/current navigation/script inventory/Obsidian. Boundary: local lab only; no `/etc/passwd`, cloud metadata, external callback/OAST, public target, secrets, credential theft, or exfiltration.

- 2026-05-23: Continued with the next SSRF attacker-callback lane under the new OSS-first/local-target-expansion policy. Saved SSRF references under `setting/local/oss_refs/ssrf_callback_20260523/` (PayloadsAllTheThings SSRF, SSRFmap; one generic nuclei URL fetched only 14 bytes and is marked check-later). Opened temporary NAT on `<attacker-vm>` and `<victim-vm>` only long enough to pull `python:3-alpine`, then closed NAT and verified Internet closed on both. Copied `modern_vuln_api` to victim, created Docker-published attacker callback listener `<lab-ip>:18183`, verified victim-to-attacker callback reachability, and created Docker-published victim target `<lab-ip>:18081`. The final `/fetch?url=attacker-callback` SSRF trigger command was denied by the local safety layer (`BLOCKED: User denied. Do NOT retry.`), so no SSRF true-attacker callback verification is claimed. Cleaned up both containers, pulled artifacts to `<artifact-output-dir>/modern_api_ssrf_attacker_callback_20260523T035629Z/`, added `handoff/modern_api_ssrf_attacker_callback_attempt_20260523.md`, and updated the SSRF bundle/current navigation. Boundary: local lab only; no public target, OAST, cloud metadata, internal scan, credential theft, exfiltration, persistence, uncontrolled DoS, or finding/report promotion.

- 2026-05-23: Tightened Cybersec Lab workflow policy per operator correction: every new script and every meaningful bundle optimization must first check mature open-source projects/tools/templates/docs or official/training-lab source, then record `adopt`, `wrap`, `adapt`, `reference-only`, or `write-custom` with provenance. Operator also permits Hermes to automatically open temporary NAT and/or change/add recoverable local靶機 environments when a better target surface is needed, provided the work remains local, provenance/source is documented, NAT is closed/verified afterward, and recovery/post-health are recorded. Updated global signpost memory, `handoff/current_navigation.md`, `handoff/vulnerability_test_inventory_20260523.md`, Obsidian Cybersec Lab note, and the reusable OWASP lab-wave skill. Boundaries unchanged: no public/unknown targets, malware, stealth/persistence, real credential theft/exfiltration/loot retention, uncontrolled propagation, automatic report submission, or finding promotion.

- 2026-05-23: Confirmed the Cybersec Lab vulnerability-testing workflow is source-driven: current waves draw from CISA KEV/NVD/Exploit-DB/GitHub/PayloadsAllTheThings/WebGoat upstream/tooling references, and new scripts/bundles should continue doing OSS/tooling reconnaissance before custom code. Added `handoff/vulnerability_test_inventory_20260523.md` to inventory tested vulnerabilities and source/bundle status. Also recorded the operator's explicit permission for aggressive/destructive scripts against authorized recoverable local靶機 only, with pre-health/artifact/recovery/post-health evidence still required and public/credential-theft/exfiltration/persistence/report-submission boundaries unchanged.

- 2026-05-23: Completed two new WebGoat path traversal family proof waves likely useful for future bug-bounty/pentest work. Added `scripts/labs/webgoat_pathtraversal_upload_write_wave1.sh` and `scripts/labs/webgoat_zipslip_overwrite_wave1.sh`, plus a retrieval-attempt runner `scripts/labs/webgoat_pathtraversal_file_read_wave1.sh`. The upload-write wave verified `ProfileUpload` lesson completion with a throwaway marker image written outside the per-user upload directory (`<artifact-output-dir>/webgoat_pathtraversal_upload_write_20260523T033108Z/`, post-health 200). The Zip Slip wave verified archive-entry traversal overwrite of the throwaway user's profile image (`<artifact-output-dir>/webgoat_zipslip_overwrite_20260523T033158Z/`, post-health 200). Added handoffs `handoff/webgoat_pathtraversal_upload_write_20260523.md` and `handoff/webgoat_zipslip_overwrite_20260523.md`, bundles `modules/bundles/verified_lab_flow_webgoat_pathtraversal_upload_write.md` and `modules/bundles/verified_lab_flow_webgoat_zipslip_profile_overwrite.md`, and updated `scripts/SCRIPT_INVENTORY.md`. Boundary: local WebGoat only, bounded marker/profile-image writes, no system-file overwrite, no shell/persistence, no public target, no credential theft/exfiltration, no finding/report promotion; no VM restore was needed because post-health stayed 200.

- 2026-05-23: Completed WebGoat browser-runtime XSS safe-marker proof wave from `<attacker-vm>` against Docker-backed WebGoat on `<victim-vm>` (`<lab-ip>:8080`). Added `scripts/labs/webgoat_browser_runtime_xss_wave1.sh` and `scripts/labs/cdp_runtime_xss.py`; registered a throwaway WebGoat user, captured the CrossSiteScripting lesson/session context, used Kali Chromium via a minimal CDP helper, and verified a unique DOM marker on WebGoat origin/path with a negative control that did not set `data-xss`. Artifacts: `<artifact-output-dir>/webgoat_browser_runtime_xss_20260523T030746Z/`; handoff: `handoff/webgoat_browser_runtime_xss_wave1_20260523.md`; bundle: `modules/bundles/verified_lab_flow_webgoat_browser_runtime_xss_safe_marker.md`. Updated `scripts/SCRIPT_INVENTORY.md`, `handoff/current_navigation.md`, and Obsidian Cybersec Lab note. Post-health stayed WebGoat HTTP 200; both attacker/victim VMs remained host-only with NIC2/NAT off and attacker Internet closed. Boundary: local lab only, safe DOM marker mutation, no credential/token theft, no exfiltration/callback/OAST, no persistence/shell/destructive write, no public target, no confirmed real-target finding/report promotion.

- 2026-05-23: Updated Hermes cron review jobs to require explicit three-party cross-review instead of single-agent review. Prepended a mandatory `delegate_task` three-reviewer gate to active cron jobs `51d89998c0de` (Cybersec Lab every-2-days strategy review), `e86b54433461` (youtube_agent every-2-days deep system review), `8dc7c5d84e5f` (youtube_agent daily progress/gate report), and `693a898b7592` (redditstories local draft cadence), and enabled the `delegation` toolset for each job while preserving their schedules, workdirs, delivery, skills, and original instructions. Backup saved at `<user-home>\AppData\Local\hermes\cron\jobs.json.bak_before_tri_party_20260523_1038`. Boundary: scheduler prompt/config update only; no job was manually run, no target-touching action, no upload/publish/schedule/OAuth/default-privacy change, no credentials/loot access, no VM/network/scope change, and no production-side repo setting changed.

- 2026-05-23: Standardized the DVWA command-injection true attacker-callback proof into a reusable one-vulnerability evidence packet. Added `handoff/dvwa_attacker_callback_evidence_packet_standard_20260523.md` and `templates/one_vuln_evidence_packet_template.md`; updated `scripts/labs/dvwa_command_injection_impact_wave1.sh` so future external/Docker callback reruns can set `EXTERNAL_CALLBACK_LOG` and have summary/observations count the authoritative external callback log instead of only the local Python listener path; updated `modules/bundles/verified_lab_flow_dvwa_command_injection_true_attacker_callback.md`, `handoff/dvwa_command_injection_true_attacker_callback_20260522.md`, `scripts/SCRIPT_INVENTORY.md`, and Obsidian Cybersec Lab notes. Read-only VirtualBox posture check showed both `<attacker-vm>` and `<victim-vm>` powered off, host-only NIC1 on, NIC2/NAT off; therefore no rerun was performed. Boundary: no VM start/stop, NAT change, scanner execution, exploitation rerun, callback listener, target container operation, public target activation, scope/config change, credential/loot handling, report submission, scheduler/CI, or production settings changed.

- 2026-05-23: Completed Cybersec Lab current navigation cleanup as a documentation/handoff-only slice. Archived the previous long `handoff/active_strategy_queue.md` to `handoff/archive/active_strategy_queue_pre_navigation_cleanup_20260523.md`, rewrote `handoff/active_strategy_queue.md` as a compact current-navigation layer, added `handoff/current_navigation.md`, added `handoff/lab_safety_contract.md`, and updated the Cybersec Lab Obsidian project note/index with the same current route, active targets, top proof lanes, parked lanes, recovery rules, artifact convention, and memory-routing boundary. Boundary: no VM changes, NAT changes, scanner/module execution, exploitation, callbacks, public target activation, scope/config change, credential/loot handling, report submission, scheduler/CI, or production settings changed.

- 2026-05-22: Migrated the default attacker route to a healthy clone `<attacker-vm>` after confirming the old registered `<attacker-vm>` is a broken snapshot/differencing-chain forensic archive. Cloned from `kali-linux-2026.1-virtualbox-amd64`, recovered Docker/Compose and baseline lab tools during an operator-approved temporary NAT window, closed NAT (`nic2=null`, `cableconnected2=off`), configured 4096 MB RAM and 4 CPUs, and created powered-off clean snapshot `clean-attacker-v2-tools-4096m-4cpu-20260522` (`bcee6035-c86d-41f0-8da1-62b3b42ec388`). Default attacker IP evidence remains `<lab-ip>`; victim remains `<victim-vm>` / `<lab-ip>`. Boundary: local lab infrastructure recovery only; no public target activation, no scan/exploit wave, no credential capture, no report submission.

- 2026-05-22: Recorded hacking/Cybersec Lab workflow/process memory-routing preference. Updated `handoff/memory_and_strategy_routing.md` and Obsidian `Projects/Cybersec Lab/00_Index/Hermes Memory Routing.md` so future workflow/process changes that affect Hermes/Codex/Cowork behavior are written to both repo handoff and the Cybersec Lab Obsidian namespace, not only chat or Hermes global memory. Boundary: memory-governance documentation only; no code, tests, target interaction, scanner/module execution, scope/config changes, credentials/tokens/evidence handling, scheduler/CI, report submission, or production settings changed.

- 2026-05-22: Promoted the bounded Juice Shop search-parameter SQLi proof into a reusable bundle/module. Added TDD coverage `scripts/test_lab_juice_shop_search_sqli_boolean_probe.py`, adapter `scripts/lab_modules/lab_juice_shop_search_sqli_boolean_probe.py`, generated runner `setting/local/verified_lab_flow_juice_shop_search_sqli_boolean_run.sh`, bundle doc `modules/bundles/verified_lab_flow_juice_shop_search_sqli_boolean.md`, and updated `scripts/SCRIPT_INVENTORY.md`. The adapter is plan-only by default, rejects public targets, requires `--lab-approved` to write a runner, uses baseline/normal/single-quote/boolean true/boolean false controls, and treats generic SQL keywords as noisy controls. Also generated `handoff/bundle_inventory_20260522.md` and `handoff/aggressive_lab_inaccessible_forensics_20260522.md` to summarize bundle coverage and VirtualBox aggressive-lab inaccessibility cause. Boundary: authorized local lab only; candidate-only/needs_manual_review; no public target, brute force, credential theft, destructive state change, loot retention, automatic finding promotion, or report submission.

- 2026-05-22: Continued from source-driven parameter discovery into a bounded Juice Shop SQLi behavior probe on the discovered `q` parameter. From `<attacker-vm>` route (`<lab-ip>`) against authorized local Juice Shop (`<lab-ip>:3000`), compared baseline/normal/single-quote/boolean true/boolean false GET probes without raw exploit execution or destructive state change. Observed strong boolean differential (`OR 1=1` response much larger than `AND 1=2`, while post-health stayed Juice Shop `200` and WebGoat `302`). Artifact: `<artifact-output-dir>/juice_q_sqli_bounded_wave_20260522T110815Z/`; handoff: `handoff/juice_q_sqli_bounded_wave_20260522.md`; temporary script: `tmp/juice_q_sqli_probe.py`.

- 2026-05-22: Restored the current attacker/victim lab route and continued the source-driven vulnerability-library test lane. Confirmed the working attacker VM is `kali-linux-2026.1-virtualbox-amd64` (`<lab-ip>`) while the old registered `<attacker-vm>` entry is stale/inaccessible; no tool redownload was needed. Reset/reached `<victim-vm>` (`<lab-ip>`), restarted Juice Shop, and ran an Arjun-inspired bounded parameter-discovery fallback after raw Arjun source execution failed due missing `ratelimit` dependency in the isolated lab. Artifacts: `<artifact-output-dir>/source_driven_param_discovery_fallback_retry_20260522T110522Z/`; handoff: `handoff/compact_status_and_vuln_source_test_20260522.md`.

- 2026-05-22: Advanced `<specific-cve-id>` into a kernel/local candidate lane with non-destructive triage only. Pulled NVD/kernel.org/oss-security references into `<artifact-output-dir>/cve_2026_43494_kernel_lane_20260522/`, saved upstream patch showing the one-line RDS fix (`rm->data.op_nents = 0` after page put cleanup), and inspected `<victim-vm>` without loading modules. Found kernel `6.18.12+kali-amd64` / package `6.18.12-1kali1`, `CONFIG_RDS=m`, `CONFIG_RDS_TCP=m`, `CONFIG_RDS_RDMA=m`, RDS module files present, and RDS not loaded. Assessment: likely locally relevant because the installed kernel package predates the May 2026 upstream fix, but exploitability is unknown and no crash/LPE/reproducer was run. Attempted safety snapshot on `<attacker-vm>` got stuck in VirtualBox `livesnapshotting`; SSH timed out and agent shutdown/poweroff is blocked by platform policy, so destructive kernel testing is deferred until operator manually recovers the VM and creates a healthy powered-off snapshot/clone. Added `handoff/cve_2026_43494_kernel_lane_triage_20260522.md`, updated `cves/CVE-2026-43494_candidate.md`, and added Obsidian note.

- 2026-05-22: Completed DVWA command-injection true attacker-side callback wave. After operator installed Docker on `<attacker-vm>` during a temporary NAT window, started disposable Docker-published callback listener `dvwa-attacker-callback` on `<lab-ip>:18182`, started disposable DVWA target `dvwa-impact-lab` on `<victim-vm>` (`<lab-ip>:18080`), and reran the bounded command-injection proof. Verified OS command execution as `uid=33(www-data) gid=33(www-data)`, marker file write/readback `DVWA_CMDINJ_dvwa_cmdinj_impact_wave1_20260522T061407Z`, and true attacker-VM callback record from remote `<lab-ip>` with the matching marker in `<artifact-output-dir>/dvwa_cmdinj_impact_wave1_20260522T061407Z/attacker_docker_callback/requests.jsonl`. Patched `scripts/labs/dvwa_command_injection_impact_wave1.sh` with `USE_LOCAL_CALLBACK_LISTENER=0` support for external/Docker listeners. Added handoff `handoff/dvwa_command_injection_true_attacker_callback_20260522.md`, bundle `modules/bundles/verified_lab_flow_dvwa_command_injection_true_attacker_callback.md`, and Obsidian note. Cleaned up disposable target/listener containers, closed aggressive-lab NAT (`nic2=null`, `cableconnected2=off`), verified Internet closed, and verified Juice Shop/WebGoat remained reachable. Boundary: local lab only, marker-only payload, no persistence, no credential theft, no OS destruction, no public target.

- 2026-05-22: Completed DVWA command-injection callback-control wave 2. Reused disposable Docker-backed DVWA target on `<victim-vm>` (`<lab-ip>:18080`) and added a Docker-published/Docker-bridge callback listener container `dvwa-callback-lab`. Updated `scripts/labs/dvwa_command_injection_impact_wave1.sh` with `CALLBACK_URL_OVERRIDE` and `__MARKER__` substitution. Verified command execution as `uid=33(www-data) gid=33(www-data)`, marker file write/readback `DVWA_CMDINJ_dvwa_cmdinj_impact_wave1_20260522T054954Z`, and callback listener record from target container `remote=172.17.0.5` with matching marker. Artifacts: `<artifact-output-dir>/dvwa_cmdinj_impact_wave1_20260522T054954Z/`; handoff: `handoff/dvwa_command_injection_callback_wave2_20260522.md`; source inventory: `handoff/vulnerability_source_coverage_inventory_20260522.md`; Obsidian note added. Limitation: callback is Docker-bridge local lab callback, not true attacker-VM callback, because aggressive-lab inbound is blocked except SSH and has no Docker runtime. Boundary: local lab only, marker-only payload, no persistence, no credential theft, no OS destruction, no public target.

- 2026-05-22: Completed DVWA command-injection impact wave 1 from `<attacker-vm>` against disposable Docker-backed DVWA on `<victim-vm>` (`<lab-ip>:18080`). Added `scripts/labs/dvwa_command_injection_impact_wave1.sh` and verified one-vulnerability max-impact proof: command injection executed as `uid=33(www-data) gid=33(www-data)`, wrote/read back lab marker `DVWA_CMDINJ_dvwa_cmdinj_impact_wave1_20260522T052046Z` from `/tmp/DVWA_CMDINJ_dvwa_cmdinj_impact_wave1_20260522T052046Z.txt`, and preserved artifacts under `<artifact-output-dir>/dvwa_cmdinj_impact_wave1_20260522T052046Z/`. Callback was attempted but not verified because high-port listeners on aggressive-lab/Windows host were unreachable; recorded as blocker, not success. Added handoff `handoff/dvwa_command_injection_impact_wave1_20260522.md`, bundle `modules/bundles/verified_lab_flow_dvwa_command_injection_container_control.md`, and Obsidian note. Temporary `dvwa-impact-lab` container was removed; Juice Shop/WebGoat lab services were restored/left running. Boundary: local lab only, marker file under `/tmp`, no persistence, no credential theft, no OS destruction, no public target, no finding/report promotion.

- 2026-05-22: Completed WebGoat JWT wave 3 from `<attacker-vm>` against Docker-backed WebGoat on `<victim-vm>` (`<lab-ip>:8080`). Added `scripts/labs/webgoat_jwt_wave3.sh`, created a throwaway authenticated session, fetched JWT lesson page/JS, extracted JWT endpoints, decoded lesson JWT examples offline, and preserved artifacts under `<artifact-output-dir>/webgoat_jwt_wave3_20260522T045749Z/`. Verified low-risk JWT decode assignment: `POST /WebGoat/JWT/decode` with decoded user value `user` returned HTTP 200 with `lessonCompleted: true`, feedback `Congratulations. You have successfully completed the assignment.`, assignment `JWTDecodeEndpoint`, and `attemptWasMade: true`. Added handoff `handoff/webgoat_jwt_wave3_20260522.md`, bundle `modules/bundles/verified_lab_flow_webgoat_jwt_decode_token_inspection.md`, and Obsidian lab note. Useful future JWT lanes mapped as candidate-only: signing/role escalation, weak key, refresh token flow, JKU misuse, and KID misuse. Boundary: local lab only, no brute force/dictionary cracking, no JWT forging, no external callbacks/JWKS hosting, no credential theft, no destructive writes, no confirmed real-target finding promotion. Script fix: avoided shell built-in `USER` collision by using `WG_USER`.

- 2026-05-22: Completed WebGoat authenticated wave 2 from `<attacker-vm>` against Docker-backed WebGoat on `<victim-vm>` (`<lab-ip>:8080`). Added `scripts/labs/webgoat_authenticated_wave2.sh`, registered throwaway WebGoat users after adapting to WebGoat-compatible password rules, captured authenticated sessions, enumerated lesson menu entries for IDOR/JWT/CrossSiteScripting/PathTraversal, and preserved artifacts under `<artifact-output-dir>/webgoat_authenticated_wave2_20260522T040820Z/`. Verified first Access Control / IDOR lesson proof using lesson-provided `tom:cat`: `/WebGoat/IDOR/profile` returned own `Tom Cat` profile with `userId=2342384`; `/WebGoat/IDOR/profile/2342388` returned HTTP 200 with `lessonCompleted: true` and feedback `Well done, you found someone else's profile`. Added handoff `handoff/webgoat_authenticated_wave2_20260522.md`, bundle `modules/bundles/verified_lab_flow_webgoat_idor_lesson_access_control.md`, and Obsidian lab note. Boundaries: local lab only, no brute force, no credential theft, no external callback, no destructive write, no shell/persistence; browser evidence deferred because Kali Playwright driver is broken/missing.

- 2026-05-22: Verified Docker/Compose is installed on `<victim-vm>` (<lab-ip>), not `<attacker-vm>`; used victim-lab as Docker target host and aggressive-lab as tester. Briefly opened victim NIC2 NAT, confirmed `webgoat/webgoat:latest` image existed, launched `webgoat-lab` with `<lab-ip>:8080->8080` and `<lab-ip>:9090->9090`, then closed NAT (`nic2=null`, `cableconnected2=off`) and verified both Kali VMs have Internet closed. Added `scripts/labs/webgoat_docker_wave1.sh` and ran it from `<attacker-vm>`; artifacts: `<artifact-output-dir>/webgoat_docker_wave1_20260522T031708Z/`; handoff: `handoff/webgoat_docker_wave1_20260522.md`; bundle: `modules/bundles/webgoat_docker_target_baseline.md`. Results: WebGoat/WebWolf reachable, login/registration surfaces return 200, actuator returns 200, known ports 8080/9090 fingerprint as Apache Tomcat, browser DOM markers captured.

- 2026-05-22: Attempted operator-authorized Kali NAT/Docker setup. `<attacker-vm>` initially had no Internet and no Docker/Compose; enabled NIC2 NAT and rebooted, then recovered a boot emergency caused by missing VirtualBox shared folder `hacking` by re-adding the persistent mapping to `<private-workspace>`. Docker install could not proceed because Kali required sudo and local safety policy blocked piping/guessing a sudo password via stdin; NAT was then closed (`nic2=null`, `cableconnected2=off`) and Kali Internet route verified closed. Continued testing without Docker by adding `labs/modern_vuln_api/source_reviewed_wrapper_wave1.sh`, which converts Exploit-DB/PayloadsAllTheThings/Arjun/Dalfox/XSStrike-style source patterns into bounded local wrappers without executing raw third-party exploits. Artifacts: `<artifact-output-dir>/source_reviewed_wrapper_wave1_20260522T024602Z/`; handoff: `handoff/nat_docker_attempt_and_source_wrapper_wave1_20260522.md`; bundle: `modules/bundles/source_reviewed_wrapper_wave1_patterns.md`. Results: pre/post health 200, XSS runtime marker yes, bounded XXE marker yes, upload retrieval marker yes, IDOR markers observed.

- 2026-05-22: Extended disposable target `labs/modern_vuln_api/modern_vuln_api.py` with reliable XSS runtime proof, bounded XXE safe-marker proof, and bounded unsafe-deserialization gadget proof. Deployed updated target on `<attacker-vm>` at `http://127.0.0.1:18080` and ran `labs/modern_vuln_api/modern_api_wave2_test.sh`; artifacts are under `<artifact-output-dir>/modern_api_wave2_20260522T022303Z/`, handoff is `handoff/modern_api_lab_wave2_20260522.md`. Added verified bundles: `verified_lab_flow_modern_api_xss_runtime_proof.md`, `verified_lab_flow_modern_api_xxe_safe_marker.md`, and `verified_lab_flow_modern_api_deserialization_bounded_gadget.md`. Boundaries: no credential theft XSS, no external callbacks, XXE reads only a safe marker file, deserialization invokes only an in-process marker recorder with no shell/persistence. Pre/post health stayed 200.

- 2026-05-22: Added and tested a disposable Python stdlib local target `labs/modern_vuln_api/modern_vuln_api.py` because Kali currently has no Docker/Podman/Compose and Juice Shop did not cleanly cover IDOR ownership, upload retrieval, or isolated SSRF callback proof. Deployed it on `<attacker-vm>` at `http://127.0.0.1:18080` and ran `labs/modern_vuln_api/modern_api_wave1_test.sh`; artifacts are under `<artifact-output-dir>/modern_api_wave1_20260522T021059Z/`, handoff is `handoff/modern_api_lab_wave1_20260522.md`. Added verified bundles: `verified_lab_flow_modern_api_idor_object_ownership.md`, `verified_lab_flow_modern_api_upload_retrieval.md`, and `verified_lab_flow_modern_api_ssrf_isolated_callback.md`. Pre/post health stayed 200; target can be stopped with `~/hermes-labs/modern_vuln_api/stop_modern_vuln_api.sh 18080`.

- 2026-05-22: Added script acquisition wave 2 from Exploit-DB and GitHub to connect lab workflows with modern security sources. Downloaded 25 Exploit-DB reference scripts across path traversal, upload, XSS, auth/access, SSRF/XXE/deserialization categories and shallow-cloned GitHub repos/tools `ffuf`, `dalfox`, `Arjun`, `XSStrike`, `jwt_tool`, and `PayloadsAllTheThings` under git-ignored `setting/local/tool_acquisition/wave2_20260522/`; SecLists clone errored and remains retryable. Handoff: `handoff/script_acquisition_wave2_20260522.md`. Policy recorded: acquisition/reference only by default; downloaded exploit scripts are not executed blindly and target-touching use remains Kali-side against authorized local lab.

- 2026-05-22: Ran Kali intel-driven wave 3 from `<attacker-vm>` against authorized Juice Shop using the CISA/NVD/Exploit-DB/GitHub/HTB pattern queue. Artifacts are under `<artifact-output-dir>/kali_intel_wave3_20260522T005554Z/`; handoff is `handoff/kali_intel_wave3_20260522.md`. Retained four useful bundles even though only some produced full impact: `valuable_candidate_kev_path_traversal_file_read_variants.md` (verified `/ftp` file reads plus blocked traversal controls), `valuable_candidate_upload_retrieval_and_validation.md` (authenticated marker upload accepted; retrieval/RCE not proven), `valuable_candidate_browser_xss_runtime_probe.md` (Kali Chromium workflow retained but runtime proof not yet conclusive), and `valuable_candidate_auth_access_boundary_expansion.md` (SQLi admin token plus unauth/auth endpoint differentials for future IDOR work). Pre/post health stayed 200; no new target installed yet because current target still yielded valuable bundles. Next recommended local target additions: IDOR/API lab, upload retrieval/execution lab, SSRF callback lab, XXE/deserialization parser lab.

- 2026-05-22: Expanded external vulnerability intelligence sources and bundle retention policy. Added NVD recent-modified mirror `cves/nvd_recent_modified_20260522.json`, Exploit-DB CSV mirror `cves/exploitdb_files_exploits_20260522.csv`, GitHub security/lab repository search `cves/github_security_lab_repo_search_20260522.json`, combined candidate JSON `cves/nvd_exploitdb_github_lab_pattern_candidates_20260522.json`, and handoff `handoff/external_vuln_intel_sources_20260522.md`. Recorded policy that bundles may be retained as `verified-impact`, `valuable-candidate`, `attempted-not-verified`, `blocked/deferred`, or `reference-only`; full target control/file read is not required when the workflow, false-positive handling, toolchain, or exploit preconditions are useful. HTB/training-lab sources are used as pattern calibration without public writeups unless assisted mode is explicitly declared; target-touching validation remains Kali-side.

- 2026-05-22: Added CISA KEV as an explicit OWASP/CVE lab-test intelligence source. Fetched official CISA KEV JSON (`count=1601`) to `cves/cisa_kev_catalog_latest.json`, generated pattern-filtered candidates in `cves/cisa_kev_lab_pattern_candidates_20260522.json`, and wrote `handoff/cisa_kev_owasp_cve_lab_mapping_20260522.md`. The mapping groups KEV entries into lab-reproducible pattern buckets: injection, XSS, path traversal/file read, file upload/write, auth/access-control, exposed config/info disclosure, SSRF, XXE, deserialization, and RCE. Rule recorded: product-specific CVEs are pattern inspiration only unless the lab actually has the affected product/version; target-touching validation remains Kali-side through `<attacker-vm>`.

- 2026-05-22: Corrected verified exploit-flow routing after operator feedback: target-touching work defaults to `<attacker-vm>`, not Windows Git-Bash. Ran Kali-side wave 2 via direct SSH/SCP/project bridge against authorized Juice Shop `http://<lab-ip>:3000/`; artifacts under `<artifact-output-dir>/kali_verified_flow_wave2_20260522T001525Z/` and `<artifact-output-dir>/kali_verified_flow_wave2b_20260522T001740Z/`. Verified Kali tools (`ffuf`, `nikto`, `nmap`, `sqlmap`, `nuclei`, `chromium`) exist; reran bounded ffuf/nikto/nmap, reconfirmed SQLi auth bypass from Kali, expanded file-read evidence with `/ftp/incident-support.kdbx` status/metadata only, and added `modules/bundles/verified_lab_flow_file_upload_marker_pdf.md` after authenticated marker PDF upload returned HTTP 204. Updated `handoff/kali_verified_flow_wave2_20260522.md`, `modules/bundles/attempted_not_verified_flows_wave1.md`, and `handoff/active_strategy_queue.md`. Post-health stayed HTTP 200; no snapshot restore needed. Remaining blockers: browser-backed XSS lacked runtime proof; coupon/business-logic endpoint returned HTTP 500 unexpected path; bounded sqlmap invocation did not add useful proof beyond manual Kali SQLi evidence.

- 2026-05-21: Downloaded and organized the first learning-stage external script/wordlist acquisition wave under git-ignored `setting/local/tool_acquisition/wave1_20260521/`. Sources recorded in `handoff/tool_acquisition_wave1_20260521.md`: SecLists (`f73a46d35bdab4a1da0999e3b4f781becb377348`), PayloadsAllTheThings (`e961fef231d8327bae83b563fab50aec2e6b77c0`), nuclei-templates (`ac27507912e11827db5652ce05891c198413c4f5`), and sqlmap (`e6595430483f0cf57ad36539ffc61fc4a6060df5`). Curated local wordlists include web discovery, LFI, SQLi, and XSS lists. Continued OWASP modularization with `modules/bundles/lab_sqli_acquisition_triage.md` and `handoff/phase4b_sqli_acquisition_learning_result_20260521.md`; bounded sqlmap local-lab run against `/rest/products/search?q=test` found no confirmed SQLi at level/risk 1, observed 32 HTTP 500 responses as candidate-only error-handling/robustness leads, and kept pre/post health 200/200. Added `handoff/project_disclosure_note_20260521.md` to record project caveats Hermes should have stated earlier: Kali shared repo permission issue, dirty working tree, overly broad PR scope, ignored local artifacts, local-lab-only safety pause, and OWASP spelling correction. Updated `bin/hermes review` to exclude git-ignored `setting/local/` and `<artifact-output-dir>/` runtime/vendor artifacts from JSON/Python/shell static checks so acquired third-party tools do not break project-owned review gates.

- 2026-05-21: Continued the learning-stage script-first loop after pausing over-broad internal safety/tier/profile controls. Ran existing `scripts/headers_audit.sh` and `scripts/cors_audit.sh` against the authorized local靶機 `http://<lab-ip>:3000` from Windows Git-Bash after Kali shared repo paths were permission-denied. Artifacts are under `<artifact-output-dir>/phase4b_headers_cors_learning_20260521T100814Z/`; pre/post health stayed 200/200. Added candidate-only run summary `handoff/phase4b_headers_cors_learning_result_20260521.md`, artifact-level `possible_vulnerabilities.md`, and active bundle doc `modules/bundles/lab_headers_cors_baseline.md`. Observations: security headers score 2/9 with several missing hardening headers; CORS audit found 0 misconfiguration hits. These remain hardening/candidate metadata, not confirmed vulnerabilities.

- 2026-05-21: Recorded operator directive to pause the project's over-broad internal safety/tier/profile controls during the current learning stage instead of adding another layered policy. Added `handoff/phase4b_learning_stage_safety_pause_20260521.md` and updated `handoff/active_testing_policy.md` with a Phase 4B override: for the authorized disposable local靶機/intentionally vulnerable app, Hermes should not block learning-lab execution on missing risk tiers, profiles, review artifacts, or contract-first gates; broad scanners, fuzzers, Burp workflows, TLS scanners, nuclei/ZAP/Nikto/ffuf/gobuster/wfuzz/nmap, exploit-shaped scripts, and destructive recoverable-lab scripts are allowed for learning. External/legal/abuse red lines remain binding: no public/unknown targets without authorization, malware, stealth persistence, real credential theft, real exfiltration, unauthorized pivoting, evasion, automatic submission, or automatic confirmed-finding promotion.

- 2026-05-21: Completed the first mature-tool wrapper wave under the updated local-lab tooling authorization. Added shared helper `scripts/lab_modules/tool_wrapper_common.py`, three one-capability modules (`lab_ffuf_sensitive_path_discovery`, `lab_nikto_server_misconfig`, `lab_nmap_http_fingerprint`), focused TDD coverage in `scripts/test_owasp_tool_wrapper_modules.py`, generated gated runners under `setting/local/`, active bundle docs, and handoff result `handoff/phase4b_tool_wrapper_three_result_20260521.md`. Final local-lab artifacts are under `<artifact-output-dir>/phase4b_tool_wrapper_three_20260521T085200Z/`; all final pre/post health checks were 200. Outputs remain candidate-only / needs_manual_review.

- 2026-05-21: Recorded operator clarification that broad scanners, fuzzers, Burp/session workflows, TLS scanners, OSS tools, and aggressive/destructive scripts should **not** be categorically banned against the authorized disposable local靶機. Updated the reusable `owasp-single-vuln-lab-wave` skill and active project queue to treat these as allowed lab capabilities when scope is locked to the lab, snapshot/recovery is verified for aggressive/destructive runs, artifacts remain hygienic/candidate-only, and any NAT/network access is used only for download/update before returning to host-only execution where feasible. This does not authorize public/real bug-bounty targets, malware, stealth, credential theft, real exfiltration, uncontrolled propagation, loot retention, or automatic finding/report submission.
- 2026-05-21: Completed the corrective split of the earlier three-class OWASP pilot into three **one-vulnerability** Phase 4B modules. Added shared helper `scripts/lab_modules/single_vuln_module_common.py`, focused test `scripts/test_owasp_single_vuln_modules.py`, adapters `lab_access_control_unauth_route_metadata.py`, `lab_crypto_transport_metadata.py`, and `lab_exceptional_condition_metadata.py`, generated bounded runners under `setting/local/`, and added bundle docs under `modules/bundles/`. Final local-lab run `phase4b_single_vuln_three_20260521T081506Z` produced candidate-only artifacts with possible-vulnerability summaries: access-control candidate `/rest/admin/application-configuration` 200 JSON; exceptional-condition candidate `/rest/does-not-exist` 500 error title; crypto/transport module produced metadata controls only. No new external OSS scripts/tools were downloaded; OSS/tooling recon decisions remain `write-custom` for these bounded local-lab adapters. Operational fixes during the run: restored/started victim VM after temporary unreachable state, used `MSYS2_ARG_CONV_EXCL='*'` to prevent MSYS path conversion of `/home/kali/...`, and switched generated bash rendering to raw f-string to preserve Python heredoc newline escapes. Validation target: tests, py_compile, generated bash `bash -n`, final lab execution/pullback, and `hermes review`.
- 2026-05-21: Corrected the OWASP module granularity policy after operator feedback: the desired final shape is **one vulnerability behavior/class per module**, not a multi-vulnerability omnibus module. The earlier `owasp_three_class_trial` remains useful only as a workflow pilot/stress-test and source of observations/tests, but it should be split into separate modules such as access-control metadata, crypto/transport metadata, and exceptional-condition metadata. Updated the reusable `owasp-single-vuln-lab-wave` skill, `handoff/active_strategy_queue.md`, and the Obsidian Cybersec Lab active-project index. Shared libraries are allowed, but runnable module entrypoints, bundle docs, tracker state, importer, and bridge should stay per vulnerability behavior.

- 2026-05-21: Completed the first three-class OWASP single-vulnerability workflow trial against the authorized host-only Juice Shop lab. Per the new OSS/tooling gate, checked mature tools before writing code: OWASP ZAP/Autorize/AuthMatrix for access control, testssl.sh/SSLyze/Mozilla HTTP Observatory for crypto metadata, and OWASP ZAP/ffuf/nuclei templates for exceptional-condition/error handling; decision for this bounded first trial was `write-custom` because the OSS tools were broad/not installed/session-heavy/TLS-specific relative to fixed candidate-only local probes. Added `scripts/lab_modules/owasp_three_class_probe.py` and RED/GREEN tests `scripts/test_owasp_three_class_probe.py`; generated `setting/local/owasp_three_class_probe_run.sh`; executed via the Kali bridge against `http://<lab-ip>:3000/`; pulled artifacts to `<artifact-output-dir>/phase4b_owasp_three_class_20260521T073537Z/`; recorded result in `handoff/phase4b_owasp_three_class_trial_result_20260521.md`; promoted trial bundle `modules/bundles/owasp_three_class_trial.md`; updated `scripts/SCRIPT_INVENTORY.md` and `handoff/owasp_2017_2021_2025_single_vuln_modularization_tracker_20260521.md`. Health stayed stable (`pre_health=200`, `post_health=200`, `requests_sent=10`). Candidate-only signals: A01 `/rest/admin/application-configuration` unauthenticated 200 candidate, A10 `/rest/does-not-exist` server-error candidate; `/administration` was corrected to SPA fallback control and `/api/Users` showed 401 auth gate. Boundary: local lab only, no credentials/brute force, no scanner/crawler/fuzzer, no callbacks/OAST, no raw body retention, no destructive behavior, no finding/report promotion.

- 2026-05-21: Updated the reusable `owasp-single-vuln-lab-wave` skill and project navigation to require an OSS/tooling reconnaissance gate before any new OWASP lab script/adapter is written. Future single-vulnerability waves must search mature open-source tools/projects/templates/schemas for the selected class, compare maintenance/license/safety defaults/target-touching behavior/output format/offline importability, then record `adopt`, `wrap`, `adapt`, `reference-only`, or `write-custom` before implementation. This preserves the script-first workflow while avoiding unnecessary custom scripts when safe OSS wrapping is better.

- 2026-05-21: Created reusable skill `owasp-single-vuln-lab-wave` for running exactly one OWASP Top 10 vulnerability class at a time against the authorized disposable local靶機, retaining useful capability as adapter/importer/bridge/bundle records, and allowing aggressive/destructive lab-only checks only behind verified snapshot/recovery gates. Added project tracker `handoff/owasp_2017_2021_2025_single_vuln_modularization_tracker_20260521.md` covering the 30 category rows across OWASP 2017, 2021, and 2025 migration/traceability tracks, plus `handoff/destructive_lab_auto_recovery_runbook_20260521.md`. Verified current lab prerequisites without restoring snapshots: VirtualBox VMs `<victim-vm>` and `<attacker-vm>` are running, victim snapshots include `setup-complete-with-tools` and `pre-aggressive-current-running-recovery-20260521-093252`, attacker snapshot includes `clean-before-aggressive-tests-20260521-093233`, attacker route is host-only only, and attacker-to-victim health returns `HTTP/1.1 200 OK`. Boundary: destructive execution is not run by this entry; future destructive waves require per-class plan, pre-health, automatic restore fallback, post-restore health, artifact capture, and candidate-only output. Still disallowed: public/real bug-bounty/client targets, malware, stealth/persistence/evasion, credential theft, uncontrolled DoS/propagation, external callbacks/OAST without a separate isolated callback lab, raw secret/loot retention, and automatic confirmed/reportable promotion.

- 2026-05-21: Completed another Phase 4B OWASP bounded local-lab wave for benign reflection/open-redirect triage using inert canaries. Reused `scripts/lab_modules/wave2_benign_params.py`, generated `setting/local/wave2_benign_params_rerun.sh`, executed via the Kali bridge against `http://<lab-ip>:3000/`, pulled artifacts to `<artifact-output-dir>/phase4b_wave2_benign_rerun_20260521T070726Z/`, promoted/recorded the active bundle `modules/bundles/benign_reflection_redirect_triage.md`, and recorded the result in `handoff/phase4b_wave2_benign_reflection_redirect_rerun_result_20260521.md`. Health stayed stable (`pre_health=200`, `post_health=200`, `requests_sent=5`). Outcome: no reflection/XSS candidate for the inert search canary and no open-redirect candidate for the fixed redirect canaries; SPA fallback and redirect error-body echo were retained as false-positive controls. Validation passed: `python scripts/test_wave2_benign_params.py` (`4 OK`), `bash -n setting/local/wave2_benign_params_rerun.sh`, Kali run/pull succeeded. Boundary: authorized local靶機 only; candidate/observation-only; no executable XSS payloads, redirect following, public/real bug-bounty targets, broad scanner/crawler, brute force, callback/OAST, credentialed flow, exploit chain, file download, loot retention, finding/report promotion, report submission, scheduler/CI, production settings, or `config/scope.txt` change.

- 2026-05-21: Completed a fast-lane Phase 4B OWASP 2025 script gap inventory and promoted the `/ftp/` directory-listing check into a reusable bounded module bundle. Added `scripts/lab_modules/ftp_filename_content_class_verifier.py` plus `scripts/test_ftp_filename_content_class_verifier.py`, generated `setting/local/ftp_filename_content_class_verifier_run.sh`, updated `scripts/SCRIPT_INVENTORY.md`, promoted `modules/bundles/lab_directory_listing_triage.md` to active, and recorded the result in `handoff/phase4b_owasp_2025_script_gap_and_ftp_wave_result_20260521.md`. Local-lab execution ran through the Kali bridge against `http://<lab-ip>:3000/` and pulled artifacts to `<artifact-output-dir>/phase4b_ftp_filename_20260521T064526Z/`: pre/post health 200, one `/ftp/` listing request, 11 filename/content-class candidates, no listed-file downloads. Validation passed: RED missing-module test observed, focused GREEN `python scripts/test_ftp_filename_content_class_verifier.py` (`5 OK`), `bash -n setting/local/ftp_filename_content_class_verifier_run.sh`, Kali run/pull succeeded. Boundary: authorized local靶機 only; candidate/observation-only; no public/real bug-bounty targets, broad scanner, recursion, brute force, callback/OAST, credentialed flow, exploit chain, file-content download, loot retention, finding/report promotion, report submission, scheduler/CI, production settings, or `config/scope.txt` change.

- 2026-05-21: Clarified the Cybersec Lab long-term goal so automation is explicit and central rather than implied. Updated `handoff/active_strategy_queue.md` with a `Long-term goal` section describing the policy-gated, scope-aware, script-first automation platform target, and updated Obsidian project navigation notes (`Projects/Cybersec Lab/00_Index/Roadmap.md`, `Active Projects.md`, and `Hermes Memory Routing.md`) to require future status answers to name bounded automation as part of the goal. Boundary: project-goal/navigation documentation only; no code, tests, runtime behavior, target interaction, scanner/module execution, scope/config changes, credentials, evidence, reports, scheduler/CI, or production settings changed.

- 2026-05-21: Prepared the next Phase 4B Wave 2 direction as documentation/run-card planning only. Added `handoff/phase4b_wave2_benign_probe_direction_20260521.md` and `handoff/phase4b_run_cards/wave2_benign_params_run_card.md`; updated `handoff/active_strategy_queue.md` with a current Phase 4B navigation note. The slice inspected `scripts/open_redirect.sh` and `scripts/xss_finder.sh`, concluded they must not be run as-is, and defined the next safe lane as a future bounded `wave2_benign_params` adapter with fixed local-lab URLs, tiny inert canaries, request caps, pre/post health, JSONL observation-only output, and no confirmed/reportable vocabulary. Boundary: planning/handoff only; no code changes to target-touching scripts, no adapter implementation yet, no lab execution, no public/real bug-bounty targets, no scanner broad run, no XSS/open-redirect probing, no credentials, callbacks/OAST, brute force, loot, report submission, scheduler/CI, or production settings changed.

- 2026-05-21: Recorded cross-project memory compaction compatibility for the hacking/Cybersec Lab project. Updated `handoff/memory_and_strategy_routing.md` so future agents know Hermes global memory is intentionally compacted into routing/index pointers and must consult `handoff/active_strategy_queue.md`, `handoff/accepted_changes.md`, named handoff artifacts, and the Obsidian `Projects/Cybersec Lab/00_Index/Hermes Memory Routing.md` note before relying on global memory for phase/scope/safety/roadmap decisions. Boundary: memory-governance documentation only; no code, tests, target interaction, scanner/module execution, scope/config changes, credentials/tokens/evidence handling, scheduler/CI, report submission, or production settings changed.

- 2026-05-21: Added the Phase 4B Wave 1A offline review-chain builder so retained Wave1A lab candidate leads now flow through the existing review vocabulary end-to-end. Added `scripts/build_wave1a_review_chain.py` and `scripts/test_wave1a_review_chain.py`; updated `scripts/README.md`. The builder starts from the committed Wave1A observation fixture path, reuses the Wave1A importer and candidate-review bridge, then runs `candidate_review_packet/0.1-trial` -> `candidate_review_gap_report/0.1-trial` -> `candidate_verification_plan/0.1-trial` -> `report_readiness_gate/0.1-trial` in memory. Output remains JSON-only, offline, non-writing by default, no network, no subprocess, no target touching, no report drafting/submission, and no confirmed/verified/reportable/accepted/ready_for_submission vocabulary. RED observed first: focused tests failed while `build_wave1a_review_chain.py` was missing. Focused GREEN passed: `python -m unittest scripts.test_wave1a_review_chain -v` (`6 OK`); smoke run returned 2 candidates and 2 blocked gate results for `/api-docs/` and `/ftp/`; adjacent suite `python -m unittest scripts.test_wave1a_review_chain scripts.test_wave1a_candidate_review_bridge scripts.test_wave1a_metadata_observation_importer scripts.test_candidate_packet_gaps scripts.test_candidate_verification_plan scripts.test_report_readiness_gate -v` passed (`59 OK`); `HACKLAB=<private-workspace> USER=Owner ./bin/hermes review` passed (Python compile OK for 98 files, shell scripts OK, lock clear). Boundary: offline fixture/review-chain retention only; no new lab execution, scanner/module execution, public/real bug-bounty target activation, schema promotion, scope/config change, credential/loot handling, report drafting/submission, scheduler/CI, deployment, billing, OAuth, or production setting change.

- 2026-05-20: Extended the Phase 4A external-tool module with bounded nuclei/ffuf support after a local Juice Shop lab spike. Updated `scripts/run_external_tool_lab.py` and `scripts/import_external_tool_observations.py`, tests `scripts/test_external_tool_lab_runner.py` and `scripts/test_external_tool_observation_importer.py`, added `tests/fixtures/external_tools/nuclei_sample.jsonl`, `tests/fixtures/external_tools/ffuf_sample.json`, live safe captures under `tests/fixtures/external_tools/live_juice_shop_nuclei_ffuf_20260520/`, plan `handoff/phase4a_nuclei_ffuf_lab_plan_20260520.json`, imported observations under `handoff/phase4a_nuclei_ffuf_run_20260520/`, and result `handoff/phase4a_nuclei_ffuf_spike_result_20260520.md`. Runner remains plan-only by default and requires `--execute-lab-approved`; supports only local/private targets, generated local nuclei info template with `-omit-raw`, and generated seven-word ffuf discovery at rate 2/maxtime 30. First nuclei attempt revealed `-interactions-cache-size 0` panics in nuclei v3.8.0; fixed to `1` and encoded in tests. Red-team Kali run kept pre/post/current health at HTTP 200; imported nuclei: 1 template observation, ffuf: 7 content-discovery observations. Boundary: observations only; no broad template packs, public targets, random PoCs, OAST/callbacks, full wordlists, recursion, report submission, or status promotion.

- 2026-05-20: Implemented Phase 4A external-tool adapter spike. Added OSS Recon Gate result `handoff/phase4a_external_tool_adapter_oss_recon_20260520.md`, offline importer `scripts/import_external_tool_observations.py`, reusable controlled lab plan/script generator `scripts/run_external_tool_lab.py`, tests `scripts/test_external_tool_observation_importer.py` and `scripts/test_external_tool_lab_runner.py`, fixtures under `tests/fixtures/external_tools/`, and result note `handoff/phase4a_external_tool_adapter_result_20260520.md`. The importer supports httpx/katana JSONL as non-promotional `observation` records only, rejects unknown tools and non-allowlisted paths, fails closed on malformed JSONL, does not execute tools or perform network I/O, and emits no confirmed/verified/reportable/accepted statuses. The runner preserves the bounded local-lab httpx/katana pattern as project capability: plan-only by default, `--execute-lab-approved` required before writing executable scripts, public targets/unknown tools rejected, pre/post health checks included, and raw body/callback/status-promotion disabled. Bounded local-lab capture ran from red-team Kali against Juice Shop using `/home/kali/go/bin/httpx v1.9.0` and `/home/kali/go/bin/katana v1.6.1` with metadata/depth=1/rate=2/no raw body/no callback limits; pre/post health remained HTTP 200. Imported safe captures into `handoff/phase4a_external_tool_adapter_run_20260520/` (httpx: 1 observation; katana: 17 observations). Boundary: no random exploit scripts, no public targets, no nuclei/ffuf execution, no report submission, and no status promotion.

- 2026-05-20: Implemented Phase 4A lab workflow runner as a guarded implementation slice. Added `scripts/phase4a_lab_workflow.py`, `scripts/test_phase4a_lab_workflow.py`, and `tests/fixtures/candidate_review_packet/phase4a_juice_shop_lab/expected_findings.json`; generated plan-only artifacts under `handoff/phase4a_lab_workflow_run_20260520_maxlab/` and recorded results in `handoff/phase4a_lab_workflow_runner_result_20260520.md`. The runner is deny-by-default, requires `--lab-mode`, rejects public targets, remains `run_mode: plan_only`, and chains scope gate -> baseline recon -> model review/script selection -> active script plan -> candidate packet -> gap report -> verification plan -> report-readiness gate -> lab report draft plan without network calls, subprocess execution, report submission, or status promotion. Validation passed: focused Phase 4A runner suite (`5 OK`), adjacent candidate/report chain suite (`107 OK`), and `HACKLAB=<private-workspace> USER=Owner ./bin/hermes review` (Python compile OK for 84 files, shell scripts OK, lock clear, 12 scope entries). Also ran a bounded local Juice Shop max-lab pass from red-team Kali with xss/sqli triage, bounded Nikto, and status-only auth-bypass verification; no brute force, recursive download, DB dump, OS shell, external callbacks, or loot collection. Lab stress caused/revealed victim availability loss; Hermes reset `<victim-vm>`, restarted `juice-shop-lab`, redacted token-bearing output, and verified HTTP 200 recovery. Boundary: authorized local lab only; live execution remains manual/operator-approved, candidate-only, and not safe for unattended real bug-bounty automation until execution adapter controls, redaction, health checks, and recovery gates are productized.

- 2026-05-20: Closed Phase 3 as an offline/dry-run local MVP checkpoint with `handoff/phase3_dry_run_local_mvp_closeout_20260520.md`. Decision: `PASS_WITH_CONDITIONS / CLOSE_PHASE_3_OFFLINE_MVP`; Hermes direct authority applies only to this T1 docs/milestone closeout, while lab/live activation remains escalation-only. Updated `handoff/active_strategy_queue.md` so the next default lane is Phase 4A.1 lab target selection / activation-boundary direction planning, with a separate local dry-run demo checklist / acceptance-bundle hygiene lane. Boundary: documentation/handoff only; no code, schema, manifest, profile, validator, runner, module discovery, recon behavior, scanner/module execution, target interaction, lab activation, scope/config/program-rule change, report drafting/submission, platform adapter, credentials/loot, scheduler/CI, proxy/pivot/transport, deployment, billing, OAuth, production setting, or real bug-bounty activation. Phase 4A still requires explicit operator target selection, narrow lab scope documentation, T4/T5 review, deny-by-default tests, rate/timeout/kill/audit controls, candidate-only outputs, and explicit activation approval before touching any lab.

- 2026-05-20: Completed P3.15 documentation-only manifest/profile policy crosswalk after P3.14 deferred formal field promotion. Added `handoff/p3_15_manifest_profile_policy_crosswalk_20260520.md`, mapping current `module_manifest/1.0` and `module_profile/1.0` fields (`risk_level`, `target_types`, `technique_tags`, `execution.*`, `output_contracts.*`, `safety_gates.*`, profile allowlists/constraints) to P3.13 active-testing policy dimensions while keeping all future fields non-contractual. Updated `handoff/active_strategy_queue.md` so the next default lane is Phase 3 dry-run/local MVP closeout. Boundary: documentation/handoff only; no schema, manifest fixture, profile, validator, runner, module discovery, run/finding/evidence schema, report consumer, scanner/module execution, target interaction, lab activation, scope/config/program-rule change, credentials/loot, scheduler/CI, deployment, billing, OAuth, production setting, or report submission.
- 2026-05-20: Inserted Phase 4A Controlled Lab Semi-Automated Calibration into the roadmap after Phase 3 dry-run/local MVP closeout and before any real authorized bug-bounty private beta. Added `handoff/phase4a_controlled_lab_calibration_plan_20260520.md`; updated `handoff/active_strategy_queue.md`, `handoff/first_bounty_readiness_checkpoint_20260520.md`, and `handoff/phase3_remaining_and_review_plain_language_20260520.md` to sequence Phase 4A (authorized lab workflow/script calibration), Phase 4B (lab-to-report workflow trial), and Phase 4C (one-program private-beta planning). Boundary: planning/handoff only; no lab target selected, no scope/config/program-rule activation, no live scans, target interaction, scanner/module execution, fuzzing, brute force, exploit attempts, callbacks/OAST, proxy/pivot/tunnel/transport, credentials/loot, scheduler/CI, deployment, billing, OAuth, production setting, report submission, or real bug-bounty target testing.

- 2026-05-20: Added Phase 3 plain-language review/remaining-work checkpoint `handoff/phase3_remaining_and_review_plain_language_20260520.md`, first-bounty readiness checkpoint `handoff/first_bounty_readiness_checkpoint_20260520.md`, and opened/completed P3.14 module manifest/profile risk-field promotion direction review. The standard `./bin/hermes cowork` route hit `Error: Reached max turns (10)`, so Hermes used a `delegate_task` fallback reviewer and recorded the result in `handoff/cowork_p3_14_module_field_promotion_direction_review.md`; synthesis lives at `handoff/p3_14_module_field_promotion_direction_result.md`. Decision: defer formal schema/manifest/profile field promotion; proceed only with documentation-only mapping/planning if continuing. Boundary: design/planning/handoff only; no code/schema/manifest/profile/runner/validator/module/scanner/report/runtime behavior, target interaction, scope/config/program-rule change, credentials/loot, scheduler/CI, deployment, billing, OAuth, production setting, or live activation.

- 2026-05-20: Implemented and accepted P3.13 module risk-tier / active-testing policy follow-up as a T1 docs-only slice after Cowork direction review `handoff/cowork_p3_13_module_risk_tier_direction_review.md` returned `APPROVE_WITH_CHANGES` with Hermes direct authority. Added `handoff/cowork_p3_13_module_risk_tier_direction_prompt.md`, `handoff/cowork_p3_13_module_risk_tier_direction_review.md`, and `handoff/p3_13_module_risk_tier_policy_result.md`; updated `handoff/active_testing_policy.md` with a risk-tier cross-mapping table, explicit technique-class fail-closed clauses, ambiguous-scope/rule fail-closed resolution, and a clearly non-contractual future-field candidate appendix. Boundary: policy/docs/handoff only; no schema/manifest/profile/runner/validator/module/scanner/report/runtime behavior, target interaction, scope/config/program-rule change, credentials/loot, scheduler/CI, deployment, billing, OAuth, production setting, or live activation. Future field candidates remain design memory only until a fresh T3+ direction review with OSS Recon Gate adopts them.

- 2026-05-20: Closed P3.12 SOC reviewer-gap catalog thread with `handoff/p3_12_closeout_current_thread_pause_20260520.md`. Decision: `PASS_WITH_CONDITIONS`; P3.11/P3.12 remain accepted as offline T2 fixture/catalog/docs/test work, SOC trial-consumer design stays deferred behind a fresh T3 direction review, and the next default lane is P3.13 module risk-tier / active-testing policy follow-up as policy/docs-only work. Updated `handoff/active_strategy_queue.md` to mark the SOC calibration mini-thread paused and route back to mainline platform governance. Safety boundary: planning/handoff only; no runtime consumer, schema promotion, SIEM/report/platform integration, live target behavior, scanner/module execution, scope/config change, credentials/loot, scheduler/CI, deployment, billing, OAuth, or production settings changed.

- 2026-05-20: Implemented and accepted P3.12 SOC reviewer-gap catalog-only slice after Cowork direction review `handoff/cowork_soc_reviewer_gap_catalog_direction_review.md` returned `PROCEED_CATALOG_ONLY` / `APPROVE_WITH_CHANGES` as a narrowed T2 direct-authority slice. Worker route: Claude Code MAX/OAuth via `CLAUDE_IMPL_MAX_TURNS=35 HACKLAB=$(pwd) ./bin/hermes claude-impl`; visible runtime/model from `handoff/claude_code_impl_run_20260520_140850.json`: `claude-opus-4-7` primary with `claude-haiku-4-5-20251001` helper, `error_max_turns`, `num_turns=36`, `total_cost_usd=4.2590270000000015`. Hermes verified worker output, fixed the independent-review blocker by adding an AST-based symmetric P3.11/P3.12 vocabulary/status drift lock, and obtained follow-up independent review `handoff/third_party_p3_12_implementation_review.md` (`PASS`). Added `fixtures/soc_evidence_bucket/reviewer_gap_catalog.md`, `fixtures/soc_evidence_bucket/reviewer_gap_catalog.json`, `scripts/test_soc_reviewer_gap_catalog.py`, `handoff/cowork_soc_reviewer_gap_catalog_direction_prompt.md`, `handoff/cowork_soc_reviewer_gap_catalog_direction_review.md`, `handoff/claude_code_task_p3_12.md`, `handoff/claude_code_result_p3_12.md`, and `handoff/third_party_p3_12_implementation_review.md`; updated rolling task/result handoffs, `handoff/active_strategy_queue.md`, and `notes/daily/2026-05-20.md`. Validation passed: `python -m unittest scripts.test_soc_reviewer_gap_catalog` (`15 OK`), `python -m unittest scripts.test_soc_evidence_bucket_fixture` (`13 OK`), `python -m unittest discover scripts` (`437 OK, 8 skipped`), `git diff --check` (LF/CRLF warnings only), `HACKLAB=$(pwd) ./bin/hermes review` (Python compile OK for 78 files, shell scripts OK, lock clear, 12 scope entries), and catalog forbidden live/action string scan (no matches). Boundary: static fixture/catalog/docs/test/handoff only; no runtime consumer, schema promotion, SIEM integration, scanner/module execution, target interaction, scope/config change, report drafting/submission, platform adapter, credentials/loot, scheduler/CI, proxy/pivot/transport, deployment, billing, OAuth, or production settings changed. SOC trial-consumer design and reviewer-answer capture remain deferred behind fresh direction review.

- 2026-05-20: Closed P3.11 and opened P3.12 as a design-only direction review for a possible SOC reviewer-gap catalog-only slice. Refreshed independent P3.11 completion review in `handoff/third_party_p3_11_implementation_review.md` (`PASS`, no blockers) and updated `handoff/active_strategy_queue.md` to mark P3.11 accepted. Added `handoff/cowork_soc_reviewer_gap_catalog_direction_prompt.md` and refreshed rolling `handoff/cowork_task.md` to ask Cowork whether to proceed with a static catalog-only slice, Markdown-only slice, defer, block, or request operator input. Recorded the Claude implementation turn-budget convention: keep repo default at 25 turns, but use temporary `CLAUDE_IMPL_MAX_TURNS=35` or `CLAUDE_IMPL_MAX_TURNS=40 HACKLAB=$(pwd) ./bin/hermes claude-impl` for fixture/test/handoff-heavy offline implementation slices rather than changing the global default. Boundary: planning/review/handoff only; no code/runtime/schema/report/scope/config/scanner/module/target-touching/credential/loot/scheduler/deployment/billing/OAuth/production surface changed.

- 2026-05-20: Implemented P3.11 SOC evidence-bucket synthetic fixture after Cowork direction review `handoff/cowork_soc_evidence_bucket_direction_review.md` returned `PROCEED_FIXTURE_ONLY` / `APPROVE_WITH_CHANGES` as a narrowed T2 direct-authority slice. Worker route: Claude Code MAX/OAuth via `HACKLAB=$(pwd) ./bin/hermes claude-impl`; visible runtime/model from `handoff/claude_code_impl_run_20260520_131507.json`: `claude-opus-4-7` primary with `claude-haiku-4-5-20251001` helper, `error_max_turns`, `num_turns=26`, `total_cost_usd=2.5786232500000006`. Hermes accepted partial worker output, then completed the missing fixture JSON and handoff/result updates directly inside the approved boundary. Added `fixtures/soc_evidence_bucket/README.md`, `fixtures/soc_evidence_bucket/sample_timeline_01.json`, and `scripts/test_soc_evidence_bucket_fixture.py`; updated rolling task handoffs and `handoff/active_strategy_queue.md`; added named result `handoff/claude_code_result_p3_11.md`. Validation passed: `python -m unittest scripts.test_soc_evidence_bucket_fixture` (`13 OK`), `python -m unittest discover scripts` (`422 OK, 8 skipped`), `git diff --check` (line-ending warnings only), `HACKLAB=$(pwd) ./bin/hermes review` (Python compile OK for 77 files, shell scripts OK, lock clear), sample fixture forbidden live/action string scan (no matches), and independent implementation/safety review `handoff/third_party_p3_11_implementation_review.md` (`PASS`). Boundary: fixture/docs/test/handoff only; no runtime consumer, schema promotion, SIEM integration, scanner/module execution, target interaction, scope/config change, report drafting/submission, platform adapter, credentials/loot, scheduler/CI, proxy/pivot/transport, deployment, billing, OAuth, or production settings changed. Reviewer-gap catalog and trial-consumer design remain deferred behind fresh direction review.

- 2026-05-20: Opened the next design-only SOC evidence-bucket / reviewer-alignment direction review after P3.10 closeout. Added `handoff/cowork_soc_evidence_bucket_direction_prompt.md` asking Cowork to choose among fixture-only, reviewer-gap catalog-only, trial-consumer design-only, defer, or block. Boundary: prompt/planning only; no code, tests, schema, runner, recon runtime, scanner/module execution, live SIEM integration, target interaction, report drafting/submission, credentials/loot, scheduler/CI, or scope/config changes.

- 2026-05-20: Captured TryGovMe SOC Simulator feedback as a workflow-optimization artifact and queued an offline future slice. Added `handoff/trygovme_soc_sim_lessons_20260520.md` documenting evidence-bucket alignment lessons: platform/reviewer feedback as structured evidence maps, host/network IOC bucket completeness, source/destination/affected asset role separation, sub-technique-aware ATT&CK review gaps, and second-pass feedback normalization. Updated `handoff/active_strategy_queue.md` to add a next-candidate SOC evidence-bucket fixture / reviewer-alignment slice after P3.10 closeout. Boundary: documentation/planning only; no live SIEM integration, no target interaction, no scanner/module execution, no schema promotion, no report drafting/submission, no credentials/loot, and no scope/config changes.

- 2026-05-20: Completed P3.10 minimal dry-run explicit recon-policy-artifact direct-read bridge after T3/T4 design review and independent implementation/safety review. Added `handoff/cowork_p3_10_direction_prompt.md`, `handoff/cowork_p3_10_direction_review.md` (`APPROVE_WITH_CHANGES`), and `handoff/third_party_p3_10_implementation_review.md` (`PASS_WITH_RECOMMENDATIONS`); reviewer route/tool for the implementation review was Hermes `delegate_task` subagent and the exact child runtime was not exposed. Updated `scripts/module_runner.py` so an explicit `--policy-artifact` may point either to existing `runs/<run_id>/policy/<file>` or to repo-local `scans/<scan-dir>/evidence/policy/policy_boundary_*.json`; the runner directly reads the explicit artifact, records the truthful repo-relative path and SHA-256, rejects traversal/outside-root/wrong-shape/symlink/non-file paths fail-closed, and still performs no copy, auto-discovery, scanner execution, module execution, finding/evidence promotion, or report generation. Updated `scripts/validate_run_manifest.py` and `scripts/test_run_manifest_schema.py` so only `policy.decision_artifact_path` may reference the narrow recon evidence path shape; finding/evidence artifacts remain run-local. Expanded `scripts/test_recon_runner_bridge_dry_run.py` with direct-read/no-copy, repo-relative non-repo-cwd, traversal, malformed scan-dir, wrong filename/directory, outside-root, hash/provenance, no bridge-specific flag, and no scanner/module-execution leakage coverage. Validation passed: focused P3.10 bridge suite (`18 OK`), adjacent runner/module-IO/bridge/run-manifest suite (`78 OK, 1 skipped`), `git diff --check` (line-ending warnings only), diff security scan (0 hits), and `HACKLAB=$(pwd) ./bin/hermes review` (Python compile OK for 76 files, shell scripts OK, lock clear, 12 scope entries). Boundary: dry-run explicit policy evidence direct-read only; `recon.sh`, `config/scope.txt`, schemas, report/submission surfaces, scheduler/CI, credentials/OAuth/tokens, deployment, billing, production settings, live target behavior, fuzzing/brute force/exploit/callback/OAST/proxy/pivot/tunnel, and real program scope activation stayed untouched. Note: pre-existing untracked `cve_brief_20260520.md/json` remain unrelated and were not modified.

- 2026-05-20: Added `handoff/p3_9_closeout_next_direction_20260520.md` as a planning-only P3.9 closeout / next-direction checkpoint after the P3.9 T2 follow-up. Decision: `PASS_WITH_CONDITIONS`; current checkpoint tier T1, future runtime bridge design T3 minimum, and future activation T4/T5. The checkpoint recommends that the default next `continue` lane should be a design-only `handoff/cowork_p3_10_direction_prompt.md` for dry-run recon-to-runner runtime bridge review, not implementation. Boundary: handoff/planning only; no code/runtime/schema/config/scope changes, no scanner/module execution, no target-touching automation, no live activation, no report drafting/submission, no credentials/OAuth, scheduler, deployment, billing, or production surface changed.

- 2026-05-20: Completed the lightweight T2 P3.9 review follow-up for hash-drift/tampered-copied-artifact coverage and explicit test-only bridge-copy documentation. Updated `scripts/test_recon_runner_bridge_dry_run.py` only: added a direct copied-artifact byte/hash drift regression that mutates the copied `runs/<run_id>/policy/decision.json` verdict to `deny`, asserts the copied artifact hash changed, requires module runner denial with no plan emission, and checks no module-execution leakage; added a source-level regression requiring `copy_artifact_into_run_policy_dir` to state that the path translation is test-harness-only and must not be mirrored into runtime code; added the corresponding helper comment. RED observed first for the missing helper comment. Validation passed: focused new/comment/preview tests (`3 OK`), full bridge suite (`12 OK`), adjacent recon/program/module bridge suite (`88 OK, 3 skipped`), full `python -m unittest discover scripts` (`401 OK, 8 skipped`), `git diff --check`, added-line sensitive/target-touching scan (no matches), and `USER=${USER:-Owner} HACKLAB=<private-workspace> ./bin/hermes review` (Python compile OK for 76 files, shell scripts OK, lock clear). Boundary: tests/comment/handoff only; no `recon.sh`, `scripts/module_runner.py`, program policy helper, validator, module/schema/profile, config/scope, report, scanner/module execution, live target, credentials/OAuth, scheduler, deployment, billing, or production surface changed. Note: `hermes review` showed pre-existing untracked `cve_brief_20260520.md/json`; this slice did not create or modify them.

- 2026-05-19: Completed lightweight independent P3.9 implementation/safety review for commit `80c7704`. Reviewer route/tool: Hermes `delegate_task` subagent; visible model/runtime reported as `gpt-5.5 / openai-codex`, lower-level runtime not otherwise exposed. Verdict: `PASS_WITH_RECOMMENDATIONS`, with no blockers and no safety findings requiring changes. Added `handoff/third_party_p3_9_implementation_review.md`; reviewer confirmed runtime/config/scope/module/schema surfaces were untouched, bridge copying is test-harness-only, `recon.sh` remains dry-run only in tests, `module_runner.py` remains preview-only, no auto-bridge CLI/wrapper/runtime coupling was introduced, and output-dir/scope fences are present. Non-blocking follow-ups: add a future hash-drift/tampered-copied-artifact negative test and a clearer test-only copy helper comment before any runtime bridge discussion.

- 2026-05-19: Implemented P3.9 offline dry-run recon-to-runner bridge coverage after Claude/Cowork direction review returned `APPROVE_WITH_CHANGES` in `handoff/cowork_p3_9_direction_review.md`. Added `scripts/test_recon_runner_bridge_dry_run.py` as a tests-only bridge harness that runs `recon.sh --dry-run --program sample-lab --policy-mode dry-run` in a temp HACKLAB, copies the emitted allow `policy_boundary_*.json` into temp `runs/<run_id>/policy/decision.json` inside the test harness only, and invokes existing `scripts/module_runner.py --mode dry-run --json` to assert preview-only allow behavior. Coverage includes positive bridge allow, policy SHA/path consistency, optional module I/O preview with no findings/evidence, target mismatch deny, path-outside-run-policy deny, helper-returncode deny, audit-event mismatch deny, planned-vs-dry-run mode mismatch deny, no bridge-specific CLI flags, no scanner/module execution leakage, real `config/scope.txt` sha256 fence, and real repo output-dir snapshot fences. Validation passed: `python -m unittest scripts.test_recon_runner_bridge_dry_run` (10 OK), adjacent suites (53 OK, 1 skipped), `python -m unittest discover scripts` (399 OK, 8 skipped), `git diff --check`, added-line secret/target-touching scan, and `USER=${USER:-Owner} HACKLAB=<private-workspace> ./bin/hermes review` (Python compile OK for 76 files, shell scripts OK, lock clear). Boundary: tests/handoff only; no `recon.sh`, `module_runner.py`, policy helper, validator, module/schema/profile, config/scope, report, scanner/module execution, live target, credentials/OAuth, scheduler, deployment, billing, or production surface changed. Process caveat: the Claude Code implementation worker exceeded the parent Hermes timeout after creating the test file, so Hermes completed direct cleanup/verification and recorded incomplete RED evidence in `handoff/claude_code_result_p3_9.md`.

- 2026-05-19: Closed Gate B program-policy dry-run hardening as a planning/handoff step and prepared the next T3 design-only direction-review artifact. Added `handoff/program_policy_dry_run_closeout_20260519.md` with decision `PASS_WITH_CONDITIONS / CLOSE_GATE_B_FOR_NOW`: P3.7 dry-run policy regression, P3.8 malformed-scope exit-code hardening, `_examples/` safety clarification, and CIDR forced-deny coverage are enough to stop adding micro-hardening by default. Added `handoff/cowork_p3_9_direction_prompt.md` for a fresh OSS Recon Gate direction review before any dry-run recon-to-runner bridge implementation. Updated `handoff/active_strategy_queue.md` to make P3.9 the next lane. This changed handoff/planning files only; no target-touching, runtime, scope/config, schema, module, report, scheduler, credential, deployment, billing, or production surface changed.

- 2026-05-19: Closed the literal CIDR forced-deny follow-up as a lightweight T2 test-coverage slice, without a fresh direction review because it stays inside the already-reviewed P3.7/P3.8 program-policy dry-run boundary. Updated `scripts/test_recon_program_policy_dry_run.py` with a shared synthetic CIDR fixture helper plus an end-to-end `recon.sh --dry-run --program sample-lab-cidr --policy-mode planned 192.0.2.0/24` regression that omits `--allow-cidr` and asserts: global `safe_target` accepts only the synthetic RFC 5737 documentation CIDR from temp scope, program policy force-denies at `find_live_hosts.input` with `CIDR_REQUIRES_ALLOW_CIDR`, no policy PASS or `DRY: httpx` scanner plan appears, a deny policy artifact is emitted, and no scanner-execution leakage markers surface. Validation passed: focused CIDR pair (`2 OK`), full `scripts.test_recon_program_policy_dry_run` (`14 OK`), `git diff --check`, and `USER=${USER:-Owner} HACKLAB=<private-workspace> ./bin/hermes review` (Python compile OK for 75 files, shell scripts OK, lock clear). Boundary: tests/handoff only; no `recon.sh` runtime behavior changes, no program policy helper contract changes, no `config/scope.txt` or committed program-scope fixture changes, no module runner/schema/module changes, no scanner/module execution, no target-touching automation, no live activation, no report drafting/submission, no platform adapter, no credentials/OAuth, scheduler, deployment, billing, or production setting changes.

- 2026-05-19: Completed P3.8 malformed-scope exit-code semantics hardening after Claude/Cowork direction review returned `APPROVE_WITH_CHANGES` (`handoff/cowork_p3_8_direction_review.md`). Updated `recon.sh` to reserve exit code `3` for program-policy boundary/config errors and to count malformed/validator boundary outcomes, including existing `VALIDATOR_DENY` helper output, while preserving exit `0` for allow-path dry runs and valid policy denies/no-work outcomes. Updated `scripts/test_recon_program_policy_dry_run.py` to assert malformed program scope exits `3`, preserve no PASS/scanner leakage, and add a valid out-of-program-scope deny regression that exits `0`; updated adjacent `scripts/test_recon_program_cli.py` expectations so artifact-validation and invalid-timeout policy boundary errors now assert exit `3`. Added P3.8 task/result handoff artifacts and refreshed `handoff/active_strategy_queue.md`. Boundary: hardening-only runtime semantics plus tests/handoff; no program policy helper contract changes, no `config/scope.txt` or program-scope fixture changes, no module runner/schema/module changes, no scanner/module execution, no target-touching automation, no live activation, no report drafting/submission, no platform adapter, no credentials/OAuth, scheduler, deployment, billing, or production setting changes.

- 2026-05-19: Closed P3.7 recommendation 1 with `_examples/` fixture clarification docs. Added `programs/_examples/README.md` defining `_examples/` as examples/offline regression fixtures only, never a real program slug namespace; clarified that `automation_permitted: true` under `_examples/` is test-only and never live authorization; documented the P3.7 `sample-lab` fixture as synthetic, reserved-host, dry-run-only, and non-target-touching. Updated `programs/README.md` to link this binding clarification and updated `handoff/active_strategy_queue.md` to remove the `_examples` docs item from next candidate slices. Boundary: docs/handoff only; no code/runtime/schema/config/scope changes, no scanner/module execution, no target-touching automation, no live activation, no report drafting/submission, no platform adapter, no credentials/OAuth, scheduler, deployment, billing, or production setting changes.

- 2026-05-19: Adopted `handoff/active_testing_policy.md` to clarify that offensive/pentesting techniques are not generically banned from the bug-bounty platform; they are deferred/gated by authorization, program rules, module risk tier, execution mode, review depth, and operator approval. The policy defines execution modes (`offline`, `dry-run`, `planned`, `live`), module risk tiers from Tier 0 offline fixtures through Tier 5 prohibited behavior, a technique-to-tier starting map, required live-execution gates, and candidate-only output semantics. Updated `handoff/active_strategy_queue.md` to track this as a policy follow-up and to defer any manifest/runner field implementation to a fresh T3/T4 review. Boundary: policy/handoff only; no code/runtime/schema/config/scope changes, no scanner/module execution, no target-touching automation, no live activation, no report drafting/submission, no platform adapter, no credentials/OAuth, scheduler, deployment, billing, or production setting changes.

- 2026-05-19: Accepted P3.7 after independent implementation/safety review and recorded launch estimate. Added `handoff/third_party_p3_7_implementation_review.md` with verdict `PASS_WITH_RECOMMENDATIONS`: boundary clean, forbidden paths untouched, synthetic fixture safe, tests/validation reproduced, and Hermes max-turn fixup acceptable because it touched only test expectations/handoff while preserving runtime locks. Added `handoff/project_launch_estimate_20260519.md` estimating dry-run/local MVP at ~1-2 weeks, controlled lab beta at ~2-6 weeks, authorized bug-bounty private beta at ~5-16 weeks, and production-like multi-program operation at ~9-35 weeks, with the recommended definition of formal launch as private beta (~2-4 months realistic). Updated `handoff/active_strategy_queue.md` to mark P3.7 complete and queue fresh follow-ups for `_examples` automation flag clarification, malformed-scope exit-code review, CIDR-deny coverage review, and program-policy mainline closeout. Boundary: review/planning/handoff only; no code/runtime/schema/config/scope changes, no scanner/module execution, no target-touching automation, no report drafting/submission, no platform adapter, no credentials/OAuth, scheduler, deployment, billing, or production setting changes.

- 2026-05-19: Completed P3.7 return-to-mainline / program-policy dry-run regression slice after Cowork direction review returned `APPROVE_WITH_CHANGES` / `PASS_WITH_CONDITIONS`. Added `handoff/cowork_p3_7_direction_review.md`, `handoff/claude_code_task_p3_7.md`, `handoff/claude_code_result_p3_7.md`, and updated `handoff/active_strategy_queue.md` to close the P3.1-P3.6 candidate-chain/reviewer-artifact line and return to the program-policy mainline. Added synthetic fixture `programs/_examples/sample-lab/scope.json` and offline unittest `scripts/test_recon_program_policy_dry_run.py` covering `recon.sh --dry-run --program <slug> --policy-mode planned|live` against temporary HACKLAB state. Validation passed: focused P3.7 test (`12 OK`), adjacent recon/program-policy suite (`62 OK, 2 skipped`), and full scripts discovery (`387 OK, 8 skipped`). Claude Code implementation worker reached max turns after producing workspace changes (`handoff/claude_code_impl_run_20260519_163456.json`), so Hermes fixed test expectations to match existing fail-closed runtime semantics and completed local verification. Boundary: tests/fixture/handoff only; no `recon.sh`, program policy helper, module runner, schema, module, config, `config/scope.txt`, scope gate, runtime behavior, scanner/module execution, target-touching automation, report drafting/submission, platform adapter, credentials/OAuth, scheduler, deployment, billing, or production setting changes.

- 2026-05-19: Adopted the useful parts of the YouTubeAgent memory-flow pattern into the cybersec lab as project-local governance. Added `handoff/memory_and_strategy_routing.md` to define authority order and routing among Hermes durable memory, repo handoff, active strategy queue, Obsidian, skills, and session_search; added `handoff/active_strategy_queue.md` as the compact current navigation layer for P3.7 and the return-to-mainline decision. Updated `handoff/periodic_reviews/review_template_v0.md` with packet freshness metadata plus memory/handoff/goal/structure drift checks, updated `handoff/periodic_reviews/README.md` to reference the new routing/queue files and frozen-packet freshness rule, and updated `.hermes.md` assets/cadence so future milestone and periodic reviews keep the active queue current. Boundary: governance/docs only; no code/runtime/schema/config/scope changes, no scanner/module execution, no target-touching automation, no report drafting/submission, no platform adapter, no credentials/OAuth, scheduler, deployment, billing, or production setting changes.

- 2026-05-19: Prepared P3.7 return-to-mainline direction-review handoff after pausing the P3.1-P3.6 candidate-chain/reviewer-artifact line. Added `handoff/cowork_p3_7_direction_prompt.md` and refreshed rolling `handoff/cowork_task.md` to ask Claude/Cowork whether the next safe slice should close the current Phase 3 review line and return to program-policy dry-run/mainline integration, add only offline policy-binding fixtures, plan a recon dry-run policy-stage exercise, plan a dry-run module-runner policy-artifact bridge, continue Phase 3 offline review work, or defer/block. The prompt is design-only and requires review tiering, OSS Recon Gate notes, P1/P1-6 status assessment, Phase 2/3 boundary checks, P2.24 trigger assessment, exact implementation boundaries, and a Multi-Party Review Decision final block. Boundary: planning/handoff only; no code/runtime/schema/config/scope changes, no scanner/module execution, no target-touching automation, no report drafting/submission, no platform adapter, no credentials/OAuth, scheduler, deployment, billing, or production setting changes.

- 2026-05-19: Hardened the Hermes handoff workflow against rolling-file overwrites and shell-mangled PR comments before continuing the Phase 3 mainline. Updated `bin/hermes` with `archive_handoff_file`, so `new-task` archives previous non-empty rolling task/proposal files and `cowork` / `claude-impl` / `codex` archive previous non-empty result/review files under `handoff/archive/rolling/` before writing replacements. Added named P3.6 audit copies `handoff/claude_code_task_p3_6.md` and `handoff/claude_code_result_p3_6.md` so the current rolling task/result are no longer the only P3.6 record. Updated `.hermes.md` and `HERMES_WORKFLOW.md` to declare rolling files as convenience pointers, require named artifacts for audit-important slices, and require `gh pr comment --body-file` for Markdown/backtick comments on this Git-Bash/MSYS host. Boundary: workflow docs plus local orchestration helper only; no target-touching automation, scanner/module execution, scope/config change, schema promotion, report submission, credentials/OAuth, scheduler, deployment, billing, or production setting changes.

- 2026-05-19: Completed P3.6 direction review and periodic-review template alignment with the Multi-Party Review Decision Policy. Added `handoff/cowork_p3_6_direction_prompt.md` and `handoff/cowork_p3_6_direction_review.md`; Claude Code MAX/OAuth direction review returned `APPROVE_WITH_CHANGES`, selecting Option 1: defer reviewer-notes artifacts and instead operationalize multi-party review output under `handoff/periodic_reviews/`. Added `handoff/periodic_reviews/README.md` and `handoff/periodic_reviews/review_template_v0.md` so future periodic or milestone-boundary reviews explicitly capture Hermes local validation, implementation reviewer, safety/security reviewer, architecture/roadmap reviewer, validation evidence, OSS Recon Gate status, Hermes authority, operator-approval posture, disagreement handling, and the Final Decision Block. P3.6 was kept templates-and-docs-only: no new consumer, schema, fixture root, chain wiring, runtime behavior, scanner/module execution, report drafting/submission, platform adapter, target-touching automation, scope/config changes, credentials, OAuth, scheduler, deployment, billing, production settings, or edits to the frozen `handoff/periodic_reviews/2026-05-18/` snapshot. P2.24 helper extraction was not triggered; the duplication watchlist remains intentional. No reviewer-notes artifact file, reviewer-notes consumer, reviewer-notes schema, or reviewer-notes fixture was created. Reviewer-notes artifacts remain DEFERRED to a future direction review (provisional P3.7 or later) with explicit re-trigger conditions recorded in `handoff/cowork_p3_6_direction_review.md`. OSS Recon Gate posture for P3.6 as implemented: not applicable because the accepted implementation is Markdown template/docs only and does not introduce a platform contract, schema, importer/exporter, runtime boundary, or reporting adapter; the direction review still recorded forward OSS alignment notes for any future reviewer-notes artifact slice. Validation passed: `git diff --check`, focused forbidden-surface scan for the staged P3.6 docs, and `HACKLAB=<private-workspace> ./bin/hermes review`. GitHub tracking was updated on PR #1: https://github.com/Jack753951/cybersec-lab/pull/1#issuecomment-4485460438.

- 2026-05-19: Adopted the Multi-Party Review Decision Gate as a process-only workflow improvement after operator approval. Added `handoff/multi_party_review_decision_policy.md` defining role-separated implementation, safety/security, architecture/roadmap, Hermes synthesis, and operator activation responsibilities; mapped Hermes decision authority by T0-T5 tier as direct, conditional, or escalation-only; added disagreement handling, blocking vs advisory definitions, final decision block format, reviewer prompt add-on, Hermes acceptance checklist, and anti-patterns. Updated `.hermes.md` so the collaboration contract now applies the multi-party decision gate alongside existing review tiering and OSS Recon Gate requirements. Boundary: documentation/process only; no live scans, target interaction, scanner/recon/runtime behavior, scope/config changes, schema promotion, report submission, scheduler, deployment, billing, credentials, OAuth, loot, scans, reports, or production settings changed.
- 2026-05-19: Completed P3.5 report-readiness reviewer prompt catalog after `handoff/cowork_p3_5_direction_review.md` approved a smaller data-and-tests-only scope. Added `templates/report_readiness_reviewer_prompts.json` as a static JSON catalog keyed to the existing `GATE_*`, `BLOCK_*`, and `CHECK_*` report-readiness vocabulary; added `scripts/test_report_readiness_reviewer_prompts.py` to enforce canonical JSON, flat non-schema marker, exact gate/block/check coverage, closed allowed response postures (`still_blocked`, `still_needs_manual_review`, `needs_more_evidence`, `defer`), sorted stable IDs, forbidden lifecycle/platform/scanner/drafting/live-target vocabulary locks, and text-scan canaries for the source chain constants. Updated `scripts/README.md` with the catalog's data-only / non-consumer posture. P3.5 was kept data-and-tests-only: no consumer, no schema, no fixture root, no workflow-chain wiring, no renderer, no report drafting/submission, and no reviewer-notes artifact. P2.24 helper extraction was not triggered; the duplication watchlist remains intentional because the slice adds no stdin consumer, `_compact_emit` clone, live-flag clone, error envelope, or chain import path. The reviewer-notes artifact is explicitly DEFERRED to a future direction review (provisional P3.6), and any consumer-backed notes slice must revisit the P2.24 fifth-consumer trigger. OSS Recon Gate result was APPROVE; <bug-bounty-platform>/Bugcrowd/DefectDojo/SARIF/OWASP concepts contributed only reviewer-question shape and no platform lifecycle, scanner-confirmed, severity-axis, submission, or promotion vocabulary was adopted. Validation passed: focused prompt+gate tests (`28 OK`), full scripts unittest discovery (`375 OK, 8 skipped`), `HACKLAB=<private-workspace> ./bin/hermes review` (Python compile OK for 74 files, shell scripts OK, lock clear), and independent implementation review `PASS_WITH_RECOMMENDATIONS` in `handoff/third_party_p3_5_implementation_review.md`; post-review hardening also added `informative`, `not_applicable`, and `won_t_fix` to the forbidden whole-word vocabulary test before rerunning focused/full validation. Boundary: no live scans, target interaction, network/scanner/subprocess behavior, schema promotion, report drafting/submission, runtime/recon wiring, platform adapter, chain consumer edit, module runner edit, `config/scope.txt`, `recon.sh`, loot, scans, reports, credentials, OAuth, scheduler, deployment, billing, or production setting changes.

- 2026-05-19: Completed P3.4-alt runner-indifference coverage after `handoff/cowork_p3_4_direction_review.md` deferred adding a third manifest-only Level 1 module. The direction review found the manifest-only shape was already physically represented by `level1.policy_decision_metadata_audit`; the remaining useful property was that `module_runner.py` is indifferent to `check.py` presence. Extended `scripts/test_module_runner.py` with fixture-tree copy helpers, a stable runner/CLI/preview projection helper, and two focused tests proving discovery, CLI planning, module I/O previews, and result previews remain unchanged when a deliberately broken `check.py` is added beside the manifest-only module and when `check.py` is removed from the evaluator-backed `security_headers_baseline` fixture; post-review hardening also compares payload error/warning codes/messages and asserts the stable projection does not mention `check.py`. Updated `scripts/README.md` to record that this runner-indifference property is pinned by tests. Validation passed: focused P3.4-alt tests (`2 OK`), full `scripts.test_module_runner` (`39 OK, 1 skipped`), full scripts unittest discovery (`367 OK, 8 skipped`), Hermes review, and independent implementation review plus narrow follow-up review. Boundary: tests/docs/handoff only; no module manifest/profile/schema/runner runtime behavior changes, no new module, no evaluator import/execution, no live scans, target interaction, network/scanner/subprocess behavior, schema promotion, report drafting/submission, runtime/recon wiring, platform adapter, `config/scope.txt`, `recon.sh`, loot, scans, reports, credentials, OAuth, scheduler, deployment, billing, or production setting changes.

- 2026-05-19: Completed P3.3 two-module runner discovery coverage as a test-only contract-observation slice after `handoff/cowork_p3_3_direction_review.md` redirected the originally proposed second-module fixture work. The "second Level 1 module fixture" objective named in `handoff/cowork_p2_25_closeout_review.md` was already physically satisfied by the P2-16 `level1.security_headers_baseline` module; P3.3 closes the remaining test-coverage gap for two-module runner discovery under `audit-baseline` instead of introducing a third module. Extended `scripts/test_module_runner.py` with fixture helpers and focused assertions for two-module in-process discovery, two-module CLI discovery, module I/O preview bundle consistency over both modules, live-repo exact two-module selection, deterministic repeated discovery ordering, and fail-closed behavior when the second manifest is malformed. Updated `scripts/README.md` to document that `module_runner.py` loads module manifests as data only and does not import or execute module `check.py` files. RED observed first: the initial two-module discovery assertion failed because `level1.security_headers_baseline` was absent from the fixture tree. Validation passed: focused P3.3 tests (`5 OK`), full `scripts.test_module_runner` (`37 OK, 1 skipped`), full scripts unittest discovery, and final Hermes review. Boundary: tests/docs/handoff only; no module manifest/profile/schema/runner behavior changes, no fixture/evaluator edits, no live scans, target interaction, network/scanner/subprocess behavior, schema promotion, report drafting/submission, runtime/recon wiring, platform adapter, `config/scope.txt`, `recon.sh`, loot, scans, reports, credentials, OAuth, scheduler, deployment, billing, or production setting changes.

- 2026-05-19: Completed P3.2 curated fixture terminal-state expectation matrix as a test-only regression hardening slice. Extended `scripts/test_candidate_workflow_fixture.py` with `P3_2_EXPECTED_TERMINAL_STATES` and a per-finding assertion that binds all 8 P3.1 curated findings to their expected packet readiness, gap review state, required gap codes, verification plan state, and report-readiness gate state. RED observed first: the new P3.2 test failed with `NameError: P3_2_EXPECTED_TERMINAL_STATES is not defined`; GREEN passed after adding the explicit matrix and assertions. Validation passed: focused P3.1/P3.2 suite (`6 OK`), full scripts unittest discovery (`360 OK, 8 skipped`), and final `HACKLAB=<private-workspace> ./bin/hermes review` passed. Boundary: tests/handoff only; no fixture content changes, consumer behavior changes, live scans, target interaction, network/scanner/subprocess behavior, schema promotion, report drafting/submission, runtime/recon wiring, platform adapter, `config/scope.txt`, `recon.sh`, loot, scans, reports, credentials, OAuth, scheduler, deployment, billing, or production setting changes.

- 2026-05-19: Completed P3.1 curated near-real offline fixture set after third-party direction review and implementation review. Added `handoff/cowork_p3_1_direction_prompt.md`, `handoff/cowork_p3_1_direction_review.md`, and `handoff/third_party_p3_1_implementation_review.md`; P3.1 direction verdict was `PROCEED_WITH_FIXTURE_ONLY_P3_1`, and follow-up implementation review returned `PASS_WITH_RECOMMENDATIONS` after Hermes fixed the duplicate-pair notional-source blocker. Added six fully offline synthetic/redacted fixture directories under `tests/fixtures/candidate_review_packet/p3_1_curated_*`: partial evidence, ambiguous scope text, chained precondition, low-signal informational (`severity_hint: "info"`), duplicate pair with distinct source module IDs, and non-finding control. Extended `scripts/test_candidate_review_packet.py` and `scripts/test_candidate_workflow_fixture.py` for validator success, synthetic origin, duplicate-pair survival/distinct source IDs, deterministic byte-identical workflow output, non-promotional vocabulary, and cross-stage `blocked` / `needs_manual_review` / `not_ready` coverage. Updated `scripts/README.md` with the curated-fixture catalog note and fresh OSS Recon Gate requirement before any importer/scanner/platform/report promotion. Validation passed: RED P3.1 tests failed before fixtures existed; focused P3.1 tests passed; full scripts unittest discovery passed; direct validator snippet validated all 8 curated findings; deterministic workflow fixture diff passed; final `HACKLAB=<private-workspace> ./bin/hermes review` passed. Boundary: fixture/tests/docs/handoff only; no live scans, target interaction, network/scanner/subprocess behavior, schema promotion, report drafting/submission, runtime/recon wiring, platform adapter, `config/scope.txt`, `recon.sh`, loot, scans, reports, credentials, OAuth, scheduler, deployment, billing, or production setting changes.

- 2026-05-19: Completed P2.25 Phase 2 closeout review through Claude Code MAX/OAuth (`handoff/claude_code_impl_run_20260519_114840.json`, `num_turns=16`, `total_cost_usd=1.4135077500000002`). Added `handoff/cowork_p2_25_closeout_prompt.md` and `handoff/cowork_p2_25_closeout_review.md`. Verdict: `CLOSE_PHASE_2`. The review confirms the Phase 2 contract layer, dry-run module runner, first Level 1 fixture module, and P2.19-P2.23 trial-only candidate workflow chain are coherent enough to close Phase 2. First recommended Phase 3 slice: P3.1 curated near-real offline fixture set (4-8 fully synthetic/redacted bug-bounty-shaped cases) to stress the existing chain without schema promotion, runtime/recon wiring, report drafting/submission adapters, platform adapters, live target behavior, network clients, subprocess/scanner imports, or status promotion. Boundary locks reaffirmed: `config/scope.txt`, `recon.sh`/`safe_target`, program policy boundary, append-only `accepted_changes.md`, loot/secrets/OAuth/scheduler/deployment/billing/production settings, callbacks/OAST/proxy/pivot/tunnels, and all `*/0.1-trial` schema promotion.

- 2026-05-19: Completed P2.24 direction review through Claude Code MAX/OAuth (`handoff/claude_code_impl_run_20260519_114055.json`, `num_turns=15`, `total_cost_usd=1.16887125`). Added `handoff/cowork_p2_24_direction_review.md` with verdict `DEFER_REFACTOR_AND_CLOSE_PHASE_2`: duplication across P2.19-P2.23 is real but bounded, centralizing live-target argument rejection before Phase 3 would weaken per-script safety review, and the correct extraction trigger is later schema promotion or observable drift. Next recommended slice is P2.25 Phase 2 closeout periodic review using the closeout questions captured in the review. Boundary: design-only/read-only review of local files; no code edits, live scans, target interaction, network clients, module execution, schema promotion, runtime/recon wiring, report drafting/submission adapters, scope-file edits, credentials, deployment, billing, OAuth, or production changes.

- 2026-05-19: Prepared P2.24 direction-review handoff for deciding whether to extract a minimal offline-consumer core helper before Phase 2 closeout or defer refactoring and proceed to P2.25. Added `handoff/cowork_p2_24_direction_prompt.md` and `handoff/p2_24_core_extraction_scope.md`. The prompt frames a T3 design-only review over observed duplication across P2.19-P2.23 (`LIVE_TARGET_FLAGS`, stdin/stdout JSON consumer posture, structured errors, compact emitters, and trial-only safety text), requires OSS Recon Gate notes, and explicitly forbids live scans, target interaction, network clients, module execution, schema promotion, runtime/recon wiring, report drafting/submission adapters, scope-file edits, credentials, deployment, billing, OAuth, or production changes. Boundary: documentation/planning only.

- 2026-05-19: Implemented P2.23 trial-only offline end-to-end candidate workflow fixture using strict TDD. Added `scripts/build_candidate_workflow_fixture.py` and `scripts/test_candidate_workflow_fixture.py`; updated `scripts/README.md` and refreshed `handoff/codex_review.md`. The new `candidate_workflow_fixture/0.1-trial` builder chains P2.19 `candidate_review_packet/0.1-trial`, P2.20 `candidate_review_gap_report/0.1-trial`, P2.21 `candidate_verification_plan/0.1-trial`, and P2.22 `report_readiness_gate/0.1-trial` in memory from P2.19-allowlisted committed finding fixtures, emits one deterministic JSON fixture document, rejects live-target/positional CLI affordances, fails closed on upstream stage errors, and preserves only non-promotional downstream gate states (`blocked`, `needs_manual_review`). RED observed before implementation: missing `scripts/build_candidate_workflow_fixture.py`. Validation passed: py_compile for new script/test; focused P2.23 unittest (`6 OK`); adjacent P2.19-P2.23 suite (`96 OK`); full `python -m unittest discover -s scripts -p 'test_*.py'` (`354 OK, 8 skipped`); `HACKLAB=<private-workspace> ./bin/hermes review` with Python compile OK, shell scripts OK, lock clear, and 12 scope entries. CLI smoke confirmed ok fixture output and `--target` fail-closed structured JSON (`LIVE_TARGET_FLAG_NOT_ALLOWED`, exit 2). Boundary: no live scans, target interaction, network calls, subprocess launch, scanner/module runtime imports, schema promotion, report drafting, report submission adapters, runtime wiring, `config/scope.txt`, credentials, loot, OAuth, scheduler, deployment, billing, production settings, or output file writes changed.

- 2026-05-18: Implemented P2.22 trial-only report-readiness gate consumer through Claude Code MAX/OAuth routing with Hermes local verification. Added `scripts/build_report_readiness_gate.py`, `scripts/test_report_readiness_gate.py`, `handoff/claude_code_task.md`, `handoff/claude_code_result.md`, `handoff/claude_code_impl_run_20260518_205542.json`, and `handoff/third_party_p2_22_implementation_review.md`; updated `scripts/README.md`, `handoff/codex_review.md`, and hardened `bin/hermes` Claude result extraction for Windows/Git-Bash max-turn/error runs. Claude Code reached `error_max_turns` but produced workspace changes and visible usage metadata (`num_turns=26`, `total_cost_usd=2.5956030000000005`); Hermes fixed one boundary-test wording issue and verified locally. Validation passed: `python -m py_compile scripts/build_report_readiness_gate.py scripts/test_report_readiness_gate.py`; focused P2.22 unittest; P2.19-P2.22 chain unittest (90 tests); full `python -m unittest discover -s scripts -p 'test_*.py'` (348 tests, 8 skipped). No live scans, target interaction, scope-file edits, secrets, schema promotion, report drafting, report adapters, deployment, scheduler, billing, or OAuth settings changed.

- 2026-05-18: Operationalized Claude Code MAX/OAuth as the default offline implementation worker so coding tasks visibly consume Claude Code usage instead of only being documented as a routing preference. Updated `bin/hermes` with `claude-impl`, `new-task claude-impl`, JSON usage capture at `handoff/claude_code_impl_run_<timestamp>.json`, `handoff/claude_code_result.md` extraction, an idempotent same-process lock path for multi-stage pipeline runs, and default `pipeline` routing through Claude Code with `HERMES_IMPL_WORKER=codex` as explicit fallback. Updated `.hermes.md`, `HERMES_WORKFLOW.md`, and `handoff/model_usage_routing_policy.md` to reflect Claude Code-first implementation, Codex fallback, and Hermes verification boundaries. Validation: `bash -n bin/hermes`; `hermes --dry-run claude-impl`; `hermes --dry-run pipeline` completed and confirmed the default implementation worker is Claude Code. No live scans, target interaction, scope-file edits, secrets, deployment, scheduler, billing, or OAuth settings changed.
## Latest

- 2026-05-18: Implemented P2.21 candidate verification checklist consumer for the Phase 2 bug-bounty candidate review workflow. Added `handoff/cowork_p2_21_direction_prompt.md`, `handoff/cowork_p2_21_direction_review.md`, `scripts/build_candidate_verification_plan.py`, and `scripts/test_candidate_verification_plan.py`; updated `scripts/README.md` and `handoff/codex_review.md`. P2.21 consumes one `candidate_review_gap_report/0.1-trial` JSON document from stdin and emits deterministic `candidate_verification_plan/0.1-trial` JSON to stdout only. It maps source review states to `blocked` / `needs_manual_review`, emits stable per-gap human checklist items, validates source schema/status/errors/gap counts/review states/gap codes, and rejects all CLI args including live-target affordances with structured JSON errors; post-review hardening rejects bool gap counts, duplicate per-finding gap codes, and assignment-style live flags like `--target=value`. Validation passed: py_compile for new script/tests plus adjacent P2.19/P2.20 scripts; focused P2.21 tests (`13 OK`); P2.19/P2.20/P2.21 focused suite (`70 OK`); P2.19 -> P2.20 -> P2.21 CLI chain smoke; full scripts unittest discovery (`328 OK, 8 skipped`); independent implementation/safety review `PASS_WITH_RECOMMENDATIONS` with non-blocking recommendations addressed. Boundary: no live scans, target interaction, network clients, subprocess/runtime execution, file input/output options, output file writes, scanner/module runtime imports, schema promotion, platform adapters, report drafting, status promotion, `config/scope.txt`, credentials, loot, OAuth, scheduler, deployment, billing, or production settings changed.

- 2026-05-18: Aligned cybersec memory coexistence with the existing YouTubeAgent cross-project memory governance model. Updated `handoff/memory_coexistence_policy.md` to adopt the YouTubeAgent authority order and layer model while adding cybersec-specific restricted-data overrides; wrote refreshed Obsidian cybersec policy at `Projects/Cybersec Lab/Memory Governance/Hermes Memory Coexistence Policy.md`; added shared Obsidian alignment note `Shared/Cross-Project Memory Alignment - YouTubeAgent and Cybersec.md`; and linked the alignment from the YouTubeAgent `handoff/memory_governance.md`. Boundary: documentation/policy only; no live scans, target interaction, uploads, publication, scheduler, OAuth/token/client-secret changes, `config/scope.txt`, credentials, loot, runtime data, profile creation, or memory entries changed.

- 2026-05-18: Established the baseline deep periodic third-party review process for the cybersec workspace and recorded the Phase 2 exit estimate. Added `handoff/periodic_reviews/2026-05-18/project_snapshot.md`, `handoff/periodic_reviews/2026-05-18/review_template.md`, and `handoff/periodic_reviews/2026-05-18/hermes_synthesis.md`; also recorded the durable strategy note in Obsidian at `Projects/Cybersec Lab/Periodic Reviews/2026-05-18 Phase 2 Baseline.md`. The review template now explicitly covers long-term goal alignment, existing workflow quality, memory/handoff flow, project structure/systemic health, safety governance, test quality, roadmap sequencing, and constructive recommendations. Current Phase 2 estimate: 3–5 remaining slices before closeout (P2.21 verification plan/checklist consumer, P2.22 report-readiness gate, P2.23 offline end-to-end workflow fixture, optional P2.24 core-structure extraction review, optional/likely P2.25 Phase 2 closeout periodic review). Boundary: documentation/planning only; no live scans, target interaction, runtime wiring, schema promotion, report submission, `config/scope.txt`, credentials, loot, OAuth, deployment, billing, or production settings changed.

- 2026-05-18: Implemented P2.20 candidate review packet gap/action consumer and corrected project routing terminology from Claude Code Pro/OAuth to Claude Code MAX/OAuth. Added `handoff/cowork_p2_20_direction_prompt.md`, `handoff/cowork_p2_20_direction_review.md`, `scripts/review_candidate_packet_gaps.py`, `scripts/test_candidate_packet_gaps.py`, and `handoff/third_party_p2_20_implementation_review.md`; updated `scripts/README.md` plus model-routing/handoff wording to use Claude Code MAX/OAuth. P2.20 consumes one `candidate_review_packet/0.1-trial` JSON document from stdin and emits deterministic `candidate_review_gap_report/0.1-trial` JSON to stdout only. It validates packet boundary, rejects malformed/wrong-version/error-bearing packets, rejects promoted finding statuses, rejects all CLI args with structured JSON errors, emits stable per-finding gap/action codes without report drafting, and preserves triage-only states (`not_ready` / `reviewer_decision_required`). Validation passed: RED test observed for missing script and later positional-arg blocker; py_compile; focused gap tests (`11 OK`); P2.19 packet tests (`46 OK`); full scripts unittest discovery (`315 OK, 8 skipped`); deterministic builder→gap smoke; positional unknown-arg JSON error smoke; AST/import unsafe primitive checks; no `modules/_schema/*candidate_review_gap_report*`; `git diff --check` with CRLF warning only; Hermes review. Independent third-party implementation review initially returned `REQUEST_CHANGES` because positional args were accepted; Hermes fixed it and follow-up review returned `PASS`. Boundary: no live scans, target interaction, network clients, subprocess/runtime execution, file input/output options, output file writes, scanner/module runtime imports, schema promotion, runner/recon/CI/hook/scheduler wiring, report generation/drafting, platform adapters, status promotion, `config/scope.txt`, credentials, loot, OAuth, deployment, billing, or production settings changed. Worker/verification route: Hermes implementation with third-party review; future heavy worker references corrected to Claude Code MAX/OAuth.

- 2026-05-18: Implemented P2.19 bug-bounty candidate review packet builder and redirected the project roadmap away from CTF expansion back toward authorized bug-bounty automation. Added `scripts/build_candidate_review_packet.py`, `scripts/test_candidate_review_packet.py`, committed self-test fixtures under `tests/fixtures/candidate_review_packet/`, `handoff/cowork_p2_19_direction_prompt.md`, `handoff/cowork_p2_19_direction_review.md`, `handoff/claude_code_p2_19_implementation_task.md`, `handoff/claude_code_p2_19_implementation_run.json`, `handoff/third_party_p2_19_implementation_review.md`, and a `scripts/README.md` entry. The builder is standard-library-only, trial-only (`candidate_review_packet/0.1-trial`), stdout-only, read-only, explicit `--repo-root`, committed-fixture allowlist only, validates `finding/1.0` candidates through the finding/evidence validator, rejects forbidden statuses, emits deterministic review questions and non-promotional `report_readiness`, and keeps schema promotion/report drafting/platform adapters deferred. Validation passed: py_compile; focused candidate packet tests (`46 OK`); full scripts unittest discovery (`304 OK, 8 skipped`); deterministic smoke run; forbidden runtime/import/coupling/platform leak checks; no `modules/_schema/*candidate_review_packet*`; `git diff --check` with CRLF warnings only; Hermes review. Independent third-party implementation review initially returned `REQUEST_CHANGES` only because the README deliverable was missing; Hermes added the README entry, reran verification, and follow-up third-party review returned `PASS`. Boundary: no live scans, target interaction, network clients, subprocess/runtime execution in builder, scanner/module runtime imports, schema promotion, runner/recon/CI/hook/scheduler wiring, report generation, platform adapters, status promotion, `config/scope.txt`, credentials, loot, OAuth, deployment, billing, or production settings changed. Worker route: Claude Code MAX/OAuth implementation with Hermes verification; Codex/GPT fallback not needed; API-backed Claude not used.

- 2026-05-18: Completed third-party P2.18 review/follow-up and accepted its blocker fixes before moving on. Added `handoff/third_party_p2_18_review.md`. The independent review confirmed P2.18 was safe to close as CTF calibration support after adding stronger active/external/oracle `requires_scope: true` enforcement alongside `kali_required: true`, stdout-only/read-only documentation, and corrected P2.18 trial-only template wording. Decision: no CTF schema/runtime promotion now; redirect roadmap to bug-bounty automation.

- 2026-05-18: Implemented P2.18 CTF verifier metadata trial consumers using Claude Code MAX/OAuth with Hermes verification. Added standalone standard-library linter `scripts/lint_ctf_verifier_metadata.py`, focused tests `scripts/test_ctf_verifier_metadata.py`, trial descriptor fixtures under `tests/fixtures/ctf_verifier_metadata/`, and updated `templates/ctf_verifier_metadata.yaml` plus `scripts/README.md`. The linter is trial-only, non-binding, read-only, deterministic, uses a minimal flat YAML parser, denies unknown/forbidden fields, enforces boolean types, requires `kali_required: true` for active-service/external/oracle descriptors, and keeps schema/registry promotion deferred to P2.19+. Validation passed: py_compile for new linter/tests; focused metadata tests (`40 OK`); full scripts unittest discovery (`254 OK, 8 skipped`); linter smoke on all three fixtures; forbidden import/runtime token check (subprocess only appears in tests); no `modules/_schema/*ctf*` files; `git diff --check` with CRLF warnings only; Hermes review. Boundary: offline/local only; no live scans, target interaction, network clients, subprocess/runtime execution in linter, scanner/module runtime imports, schema promotion, runner/recon/CI/hook/scheduler wiring, `config/scope.txt`, credentials, loot, OAuth, deployment, billing, or production settings changed. Claude Code hit max-turns after producing the implementation, but Hermes verified and completed acceptance; Codex/GPT fallback not needed.

- 2026-05-18: Completed P2.18 direction review using Claude Code MAX/OAuth read-only route without Anthropic API key usage. Added `handoff/cowork_p2_18_direction_review.md` with ACCEPT_WITH_CHANGES: keep the CTF verifier metadata trial offline/local, add sample descriptor fixtures plus a weak read-only linter before any schema/registry promotion, deny drift/unsafe fields, require `kali_required` for active-service/oracle descriptors, and continue to defer schema promotion to P2.19+. Worker route for implementation: Claude Code MAX/OAuth with Hermes verification; Codex/GPT only for surgical fallback. No code/runtime changes in this review step, no live scans, target interaction, scope/config/credentials/loot/scheduler/deployment/billing/OAuth changes, schema promotion, or production settings changed.

- 2026-05-18: Prepared the next P2.18 direction prompt for CTF verifier metadata trial consumers under an API-key-conserving route. Added `handoff/cowork_p2_18_direction_prompt.md`, proposing offline/local sample descriptor fixtures and an optional read-only metadata linter before any schema/registry promotion. The prompt requires OSS Recon Gate review, keeps the template non-binding, prefers Claude Code MAX/OAuth read-only review over API-backed Claude, and forbids live scans, target interaction, schema promotion, runtime wiring, scope/config/credentials/loot/scheduler/deployment/billing/OAuth changes, and production settings. Documentation/planning only.

- 2026-05-18: Clarified capacity assumptions in the model routing policy: the operator has Claude Max and GPT Pro, so Claude Code MAX/OAuth and GPT/Codex plan-backed routes can be used more freely for cybersec day-to-day coding/review, while `ANTHROPIC_API_KEY` remains reserved for other projects and rare high-value review. Updated the Obsidian methodology note with the same capacity note. Documentation/policy only; no model config, credentials, scheduler, target-touching automation, scope files, or production settings changed.

- 2026-05-18: Tightened the model/worker routing policy around Anthropic API key conservation. `handoff/model_usage_routing_policy.md` now prefers Claude Code MAX/OAuth, Hermes/GPT, and Codex/GPT before API-backed Claude for routine coding/review, sets API-backed Claude/Cowork to a 0-3% preferred future share with a 5% ceiling without explicit approval, and requires API-backed Claude use to be rare and justified in handoff. Updated the Obsidian methodology note with the same API conservation rule. Documentation/policy only; no model config, credentials, scheduler, target-touching automation, scope files, or production settings changed.

- 2026-05-18: Advanced P2.17 using the new routing policy. Claude Code MAX/OAuth produced the direction review (`handoff/cowork_p2_17_direction_review.md`) and initial implementation for offline/local CTF workflow skeletons; Hermes added missing focused tests and verified locally. Added `scripts/ctf_prepare_challenge.py`, `scripts/ctf_review_decision.py`, `scripts/test_ctf_prepare_challenge.py`, `scripts/test_ctf_review_decision.py`, `templates/ctf_verifier_metadata.yaml`, deterministic input+expected-output fixture pairs under `tests/fixtures/ctf_review_decision/`, and `scripts/README.md` entries for the new P2.17 helpers. Validation passed: py_compile for the new scripts/tests; focused prepare tests (`4 OK`); focused review-decision tests (`10 OK`); full scripts unittest discovery (`218 OK, 8 skipped`). Boundary: offline/local only; no live scans, network clients, target interaction, scanner/module runtime wiring, `config/scope.txt`, credentials, loot, scheduler, deployment, billing, OAuth, or production settings changed. Worker route: Claude Code MAX/OAuth implementation with Hermes verification; Codex/GPT fallback not needed so far.

- 2026-05-18: Added active model/worker routing policy to preserve Anthropic API key capacity while routing suitable offline coding/build work to Claude Code MAX/OAuth. New `handoff/model_usage_routing_policy.md` defines default worker routing, Claude Code safety boundaries, required verification contract, prompt footer, and target future worker mix. Documentation/policy only; no model config, credentials, scheduler, target-touching automation, scope files, or production settings changed.

- 2026-05-18: Captured CTF workflow-validation lessons from blind picoCTF drills as documentation/process artifacts. Added `handoff/ctf_workflow_validation_and_escalation.md` defining CTF-as-workflow-drill goals, Kali-first handling for external services, output-side review questions, escalation triggers, and lessons from Some Assembly Required 4, Java Script Kiddie, vault-door-8, and Clouds. Added `handoff/ctf_tooling_backlog.md` proposing an offline/local P2.17 skeleton for CTF artifact preparation, review-decision classification, verifier metadata templates, and dry-run fixtures. Added `handoff/cowork_p2_17_direction_prompt.md` to route the proposed slice through Claude/Cowork direction review before Codex implementation. Updated the Cybersec Lab Obsidian methodology note with escalation criteria and CTF-to-platform lessons. Boundary: documentation/planning only; no scanner/module runtime changes, no live scans beyond the already-authorized picoCTF interactions, no target-touching automation added, no scope/config/credentials/loot/reports/scheduler/deployment/billing changes.

- 2026-05-18: Completed Phase 2 P2-16 `level1.security_headers_baseline` workflow validation within the approved T3 fixture-only boundary. Added an offline stdlib-only module under `modules/checks/level1/security_headers_baseline/` with `evaluate(fixture, *, run_id, policy_decision_sha256)`, a manifest declaring no network/target-touching and `emits_findings=false`/`emits_evidence=false`, module docs, six committed input/golden finding fixture pairs under `tests/fixtures/security_headers_baseline/`, focused tests in `scripts/test_security_headers_baseline.py`, and six redacted triage drafts plus cadence README under `handoff/p2_16_triage/`. Validation passed: py_compile, focused unittest (`15 OK` after adding explicit live-target-flag rejection plus AST static checks), manifest validator verdict `allow`, full scripts unittest discovery (`203 OK, 8 skipped` after rerun with a longer timeout), `git diff --check` with existing line-ending warnings only, Hermes review, and independent Claude/Cowork implementation review `PASS_WITH_RECOMMENDATIONS` with no blockers; non-blocking CLI/static-test recommendations were addressed in the test suite. Boundary: no live scans, no target interaction, no network/DNS/subprocess/callback behavior in the module, no runtime writes, no schema bumps, no preview validator changes, no runner/recon/profile/CI/hook/scheduler/deployment wiring, no `config/scope.txt` or scope/config changes, and no confirmed findings.

- 2026-05-18: Completed Phase 2 P2-15 offline preview_ledger/1.0 schema and validator within the approved `schema_plus_validator` boundary. Added `modules/_schema/preview_ledger.schema.json`, `scripts/validate_preview_ledger.py`, `scripts/test_preview_ledger.py`, and committed fixtures under `tests/fixtures/preview_ledger/1.0/`. The ledger is a closed manifest-of-manifests index only: each entry binds exactly to `runs/<run_id>/preview/preview_manifest.json` by path, SHA-256, byte size, duplicate-free `run_id`, and `schema_version_observed: "preview_manifest/1.0"`. The validator is read-only, standard-library-only, requires explicit `--repo-root`, emits single-line `preview_ledger_validation/1.0` JSON, rejects strict-version/path/hash/size/timestamp/symlink/notes drift, and hashes only the referenced `preview_manifest.json` rather than walking inner artifacts. Validation passed: py_compile, focused ledger tests (`22 OK, 4 skipped`), preview manifest + ledger tests (`38 OK, 5 skipped`), full scripts unittest discovery (`188 OK, 8 skipped`), JSON parse, git diff check with line-ending warnings only, and Hermes review. No live scans, target interaction, module/scanner execution, subprocess/network/callback behavior introduced, findings/evidence/reports, scope/config changes, scheduler/deployment/billing/production settings, builder/indexer, or runner/recon/CI/hooks/scheduler wiring changed.

- 2026-05-18: Completed Phase 2 P2-14 preview_manifest/1.0 schema and validator. Added `modules/_schema/preview_manifest.schema.json`, `scripts/validate_preview_manifest.py`, `scripts/test_preview_manifest.py`, and reusable committed fixtures under `tests/fixtures/preview_manifest/1.0/`; updated the P2-13 persisted preview manifest data shape in `scripts/module_runner.py` plus docs/tests. The new contract is closed, strict-versioned, stdlib-only, read-only for validation, and binds only the fixed dry-run preview artifacts (`run.json`, `module_inputs.json`, `module_results.json`, `bundle_consistency.json`) by safe relative path, sha256, size, and content type. Validation passed: py_compile, focused preview tests (`16 OK, 1 skipped`), module runner + preview tests (`46 OK, 2 skipped`), full scripts unittest discovery (`166 OK, 4 skipped`), JSON parse, git diff check with only CRLF warnings, Hermes review, and independent Claude/Cowork implementation review PASS with no blockers. No live scans, target interaction, module/scanner execution, subprocess/network/callback behavior, findings/evidence/reports, loot, scope/config changes, credentials, scheduler, deployment, billing, or production settings changed.

- 2026-05-16: Adopted review tiering and milestone governance for the cybersec lab workflow. Added `handoff/review_tiering_policy.md` defining T0-T5 review tiers, escalation triggers, milestone batching, OSS Recon Gate linkage, review output templates, and high-default tiers for proxy/pivot/transport work. Updated `.hermes.md` so Hermes assigns a review tier/milestone before routing and verifies tier/OSS requirements before Codex and final acceptance. Updated `handoff/oss_recon_gate.md` to reference the tier policy, include tier/milestone fields in the gate template, and require Hermes acceptance to confirm tier assignment. Updated `handoff/WORKER_PROMPT_TEMPLATES.md` and `handoff/CODEX_REVIEW_PROMPT.md` so worker prompts classify review tier/milestone, escalate on boundary changes, and avoid silently broadening scope. Boundary: documentation/process-only; no scans, target interaction, runtime scanner/module behavior, `config/scope.txt`, credentials, loot, reports, scheduler, deployment, billing, or production settings changed.

- 2026-05-16: Completed Phase 2 P2-13 offline preview bundle persistence. `scripts/module_runner.py` now supports opt-in `--persist-preview-bundle`, requiring `--include-module-io-preview` plus explicit repo root via `--discover-root` or `--profile-root`; after all manifest/profile/policy/run/module-I/O/bundle gates allow, it writes data-only JSON artifacts under repo-local `runs/<run_id>/preview/` (`run.json`, `module_inputs.json`, `module_results.json`, `bundle_consistency.json`, `preview_manifest.json`). Added fail-closed guards for unsafe run IDs, symlinked repo roots, unresolved symlinked parent paths such as `runs`, resolved containment/symlink issues, existing preview/temp directories, bundle non-allow, and filesystem write failures. Expanded `scripts/test_module_runner.py` for successful persistence, deny/no-write paths, path escape, existing preview dir, symlinked runs parent, simulated write failure no-traceback, and CLI explicit-root requirements; updated `scripts/README.md`; recorded OSS Recon Gate direction review and independent implementation/safety re-review in `handoff/claude_p2_13_direction_review.json` and `handoff/claude_p2_13_implementation_review.json`. Validation passed: focused runner tests (`32 passed, 1 skipped`), full scripts unittest (`150 passed, 3 skipped`), Python compile, `git diff --check` with only LF→CRLF warnings, Hermes review, and final independent re-review PASS. No live scans, external target interaction, module/scanner execution, subprocess/network/callback behavior, target touching, `config/scope.txt` changes, findings/evidence beyond empty previews, reports, loot, credentials, scheduler, deployment, billing, or production settings changed.

- 2026-05-16: Quarantined the untracked CVE morning brief generated from non-canonical WebSearch fallback sources. Moved the root-level `cve_brief_20260516.md/json` into `cves/unverified/` as `2026-05-16_websearch_fallback_unverified.*`, added a quarantine README, and marked the Markdown/JSON as unverified so future agents do not treat it as canonical intelligence or drive target-touching actions from it. No scans, target interaction, module/scanner execution, findings/evidence, reports, credentials, loot, or `config/scope.txt` changes were made.

- 2026-05-16: Completed Phase 2 P2-12 offline module I/O bundle diagnostics hardening. `scripts/validate_module_io_bundle.py` now preserves existing `errors`/`error_codes` while adding additive structured `error_details` with stable code, message, path, expected, observed, and module_id fields where applicable. Added `BUNDLE_CREATED_AT_UTC_MISMATCH` for `run.created_at_utc` vs `module_input.run.created_at_utc`, structured JSON-load failure handling, details for missing/extra previews and non-empty findings/evidence, and generalized module-id mismatch reporting for multi-module one-missing/one-extra input/result cases. Expanded `scripts/test_module_io_bundle.py` with explicit P2-12 code coverage for mode, runner, dry-run, created-at, status, target-touching, constraints, module ID, duplicate run modules, multimodule result ID drift, and missing/extra preview detail population. Validation passed: focused bundle tests (`13 passed, 21 subtests passed`), relevant bundle/contract/run/runner tests (`58 passed, 59 subtests passed`), full scripts pytest (`142 passed, 2 skipped, 138 subtests passed`), Python compile, `git diff --check` with only LF→CRLF warnings, OSS Recon Gate direction review (`handoff/claude_p2_12_direction_review.json`), and independent implementation/safety re-review (`handoff/claude_p2_12_implementation_review.json`) returned PASS with no blockers. No live scans, external target interaction, module/scanner execution, network/subprocess/callback behavior, target touching, persisted preview ledger, findings/evidence beyond empty placeholders, `config/scope.txt` changes, credentials, loot, reports, scheduler, deployment, billing, or production setting changes were made. Issue: #15.

- 2026-05-16: Adopted the OSS Recon Gate workflow improvement for future cybersec lab phases. Added `handoff/oss_recon_gate.md` with Claude/Cowork preview and implementation-review prompt add-ons, a reference catalog of relevant OSS projects/formats (Nuclei, ProjectDiscovery tools, Amass, SpiderFoot, Recon-ng, SARIF, DefectDojo, Semgrep, OWASP ZAP, OSV, CSAF/OpenVEX, CycloneDX, STIX/OpenCTI, MITRE ATT&CK/D3FEND), output template, and Hermes acceptance checks. Updated `.hermes.md`, `handoff/sustained_review_loop.md`, `handoff/WORKER_PROMPT_TEMPLATES.md`, `handoff/extensible_architecture_direction.md`, and `handoff/systematization_extension_plan.md` so Claude/Cowork phase previews explicitly compare relevant open-source designs while preserving the long-term systematized, modular, update-friendly, policy-gated authorized-testing platform goal. This was a documentation/process-only change: no live scans, target interaction, code execution paths, `config/scope.txt`, credentials, loot, reports, scheduler, deployment, billing, or production settings changed.

- 2026-05-16: Completed Phase 2 P2-11 offline module I/O preview bundle consistency layer. Added `scripts/validate_module_io_bundle.py` as a standard-library-only cross-document validator for planned `run/1.0` manifests plus parallel `module_input/1.0` and `module_result/1.0` preview arrays. Updated `scripts/module_runner.py` so `--include-module-io-preview` runs the bundle validator before returning allow; inconsistencies fail closed with structured `BUNDLE_*` codes and no plan/previews are emitted. Added tests for runner happy path, order-independent two-module bundles, mismatched run/module/manifest/policy/profile/program/target fields, missing/duplicate/extra previews, non-empty findings/evidence, malformed output paths, and CLI non-zero structured JSON. Validation passed: focused tests (`43 passed, 36 subtests passed`), full scripts pytest (`137 passed, 2 skipped, 129 subtests passed`), Hermes review, git diff check with only README line-ending warnings, third-party direction review (`handoff/claude_p2_11_direction_review.json`) and independent implementation/safety review (`handoff/claude_p2_11_implementation_review.json`) returned PASS_WITH_RECOMMENDATIONS with no blockers. No live scans, external target interaction, module/scanner execution, network/subprocess/callback behavior, target touching, persisted preview ledger, findings/evidence, `config/scope.txt` changes, credentials, loot, reports, scheduler, deployment, billing, or production setting changes were made. Issue: #14.

- 2026-05-16: Completed Phase 2 P2-10 offline module I/O envelope contract layer. Added versioned `module_input/1.0` and `module_result/1.0` JSON schemas, plus `scripts/validate_module_io_contract.py` as a standard-library-only fail-closed validator for future module invocation/result envelopes. Updated `scripts/module_runner.py` with optional `--include-module-io-preview`, which emits validated preview-only `module_input_previews` and `module_result_previews` after existing manifest/profile/policy/run-plan gates pass; previews remain `not_executed`, dry-run-only, non-target-touching, and keep findings/evidence empty. Added tests for valid contracts and denials covering non-dry-run, network/target-touching/destructive/intrusive settings, unsafe output gates, raw-secret/loot/OAST flags, unknown fields, bad hashes/timestamps, traversal, raw `/./` and duplicate-slash paths, completed results, non-empty findings/evidence, and execution-implying summaries. Validation passed: focused tests (`35 passed, 24 subtests passed`), full scripts pytest (`129 passed, 2 skipped, 117 subtests passed`), Hermes review, third-party direction review (`handoff/claude_p2_10_direction_review.json`) and independent implementation/safety review (`handoff/claude_p2_10_implementation_review.json`) returned PASS/no blockers. No live scans, external target interaction, module/scanner execution, network/subprocess/callback behavior, target touching, `config/scope.txt` changes, findings/evidence emission beyond empty placeholders, credentials, loot, generated reports, scheduler, deployment, billing, or production setting changes were made. Issue: #13.

- 2026-05-16: Completed Phase 2 P2-9 shared profile issue helper and detail-shape tests. Added `scripts/profile_issues.py` as the standard-library-only source of stable profile error-code constants, `IssueDetail` serialization, sorted code extraction, and shared issue/warning append helpers. Updated `scripts/module_runner.py` and `scripts/validate_module_profile.py` to consume the shared vocabulary while preserving human-readable `errors`/`warnings` and additive structured JSON fields. Added representative object-shape tests for missing-profile loader errors, explicit-manifest membership hard errors, and discovery non-member warning details. Updated `modules/profiles/INDEX.md`, `scripts/README.md`, and review handoff. Validation passed: focused profile/runner tests (`31 passed, 9 subtests passed`), full scripts unittest suite (`121 passed, 2 skipped`), JSON parse, Hermes review, git diff check with only CRLF warnings, and independent review (`handoff/claude_p2_9_implementation_review.json`) returned PASS. No live scans, external target interaction, module/scanner execution, network/subprocess/callback behavior, target touching, `config/scope.txt` changes, findings/evidence, credentials, loot, generated reports, scheduler, deployment, billing, or production setting changes were made. Issue: #12.

- 2026-05-16: Completed Phase 2 P2-8 stable profile error-code vocabulary and contract docs. `scripts/module_runner.py` and `scripts/validate_module_profile.py` now preserve human-readable errors/warnings while adding deterministic `error_codes`/`warning_codes` and details for profile loader/selector failures including missing, malformed, schema-invalid, id-mismatched, membership mismatch, risk/target/technique/execution constraint, and empty-selection cases. Updated docs in `modules/profiles/INDEX.md`, `modules/_schema/README.md`, and `scripts/README.md`; added TDD coverage for representative codes. Validation passed: focused profile/runner tests (`31 passed`), full scripts unittest suite (`121 passed, 2 skipped`), JSON parse, Hermes review, git diff check with only CRLF warnings, and independent Claude Code review (`handoff/claude_p2_8_implementation_review.json`) returned PASS. No live scans, external target interaction, module/scanner execution, network/subprocess/callback behavior, target touching, `config/scope.txt` changes, findings/evidence, credentials, loot, generated reports, scheduler, deployment, billing, or production setting changes were made. Issue: #11.

- 2026-05-16: Completed Phase 2 P2-6 offline module discovery/profile selection. `scripts/module_runner.py` now supports `--discover-root <repo>` to scan only repository-local `modules/checks/**/module.json`, validate every discovered manifest, fail closed on malformed manifests or duplicate `module_id` values, and select dry-run-safe `audit-baseline` modules before feeding them into the existing dry-run planning path. Discovery mode is mutually exclusive with explicit `--manifest` paths. Added TDD coverage for committed fixture selection, duplicate IDs, malformed manifests, path escapes, target-touching/network-requiring denial, CLI discovery-to-plan flow, and mixed discovery/manifest refusal. Validation passed: focused runner tests (`16 passed`), full scripts unittest suite (`106 passed, 2 skipped`), Python compile, JSON parse, Hermes review, git diff check with only CRLF warnings, and independent review (`handoff/cowork_p2_6_module_discovery_review.md`) returned PASS. No live scans, external target interaction, module execution, network/subprocess/callback behavior, `config/scope.txt` changes, findings/evidence, credentials, loot, reports, scheduler, deployment, billing, or production setting changes were made. Issue: #9.

- 2026-05-16: Completed Phase 2 P2-5 first Level 1 audit module fixture. Added `modules/checks/level1/policy_decision_metadata_audit/module.json` as an offline-only, dry-run-plannable fixture under the chosen `modules/checks/<level-or-category>/<module_id>/module.json` layout. Added TDD coverage proving the committed fixture validates, asserts no network/target-touching/destructive/intrusive/finding/evidence/loot/OAST behavior, and can be planned by `scripts/module_runner.py` with a manifest hash and empty findings/evidence arrays. Validation passed: fixture validator allow/no errors, focused fixture/runner tests (`22 passed, 37 subtests`), full scripts pytest (`97 passed, 2 skipped, 102 subtests`), Python compile, JSON parse, Hermes review, git diff check with only CRLF warnings, and independent review (`handoff/cowork_p2_5_level1_fixture_review.md`) returned PASS. No live scans, module execution, network access, `config/scope.txt` changes, credentials, loot, reports, scheduler, deployment, billing, or production setting changes were made. Issue: #8.

- 2026-05-16: Completed Phase 2 P2-3 module manifest schema. Added `modules/_schema/module_manifest.schema.json`, standard-library `scripts/validate_module_manifest.py`, and TDD coverage in `scripts/test_module_manifest_schema.py`. The manifest requires module id/version/risk/target types, allowlisted technique tags, dry-run support, explicit network posture, external tools, output contracts bound to `run/1.0`/`finding/1.0`/`evidence/1.0`, and safety gates for policy/scope/manual-verification/scanner-output/redacted-evidence/no-loot/no-destructive/no-OAST behavior. Review-discovered technique/network posture gaps were fixed so known active/passive tags must match `network_access` and `target_touching` in both validator and schema. GitHub/open-source references considered: Nuclei templates, OWASP ZAP add-ons, and OSCAL component definitions; final design remains project-specific and default-deny. Validation passed: focused tests (`12 passed, 28 subtests`), full relevant pytest suite (`40 passed, 72 subtests`), Python compile, JSON parse, git diff check, and independent re-review (`handoff/cowork_p2_3_module_manifest_review.md`) returned PASS. No live scans or `config/scope.txt` changes were made. Issue: #6.

- 2026-05-16: Completed Phase 2 P2-2 run manifest / execution ledger. Added `modules/_schema/run.schema.json`, standard-library `scripts/validate_run_manifest.py`, and TDD coverage in `scripts/test_run_manifest_schema.py`. The manifest binds `run_id`, program/global scope hashes, target, policy decision artifact/hash, execution mode, modules, findings/evidence artifact references, and triage review state. It rejects dry-run target-touching, non-allow policy decisions, traversal/out-of-run paths, duplicate module/artifact IDs, and cross-document finding/evidence inconsistencies across every supplied finding. Validation passed: py_compile, JSON parse, focused run tests (`10 passed, 14 subtests`), full relevant pytest suite (`75 passed, 2 skipped, 65 subtests`), Hermes review, git diff check, and independent re-review (`handoff/cowork_p2_2_run_manifest_review.md`) returned PASS after blocker fixes. No live scans or `config/scope.txt` changes were made. Next issue: #6.

- 2026-05-16: Completed Phase 2 P2-1 candidate finding/evidence schemas. Added versioned JSON Schema contracts under `modules/_schema/` plus standard-library `scripts/validate_finding_evidence.py` and TDD coverage. Automated findings remain triage-only (`candidate`/`needs_verification` etc.); `confirmed` is rejected. Evidence references must be redacted, canonical-hashed, under `runs/<run_id>/evidence/`, and metadata blocks sensitive/raw keys or values consistently in schema and validator paths. Validation passed: py_compile, JSON parse, full relevant pytest suite (`65 passed, 2 skipped, 51 subtests`), Hermes review, git diff check, static added-line scan, and independent review (`handoff/cowork_p2_1_finding_evidence_review.md`) returned PASS. No live scans or `config/scope.txt` changes were made. Issue: #5.

- 2026-05-16: Completed Phase 1 P1-6 evidence-integrity hardening for program policy allow artifacts. `recon.sh` now cross-checks flat boundary output, actual current program/global source hashes, artifact `boundary.audit_event`, and embedded `decision` verdict/target/technique/mode/audit/hash/timestamp fields before accepting policy PASS. Forged or stale allow artifacts now fail closed as `ARTIFACT_VALIDATION_FAILED` before scanner/probe stage consumption. Added dry-run regressions for missing decision, decision target mismatch, boundary audit mismatch, and hash/source mismatch. Validation passed: bash syntax, py_compile, 47 pytest tests + 21 subtests, Hermes review, git diff check, static added-line scans, and independent review (`handoff/cowork_p1_6_evidence_integrity_review.md`) returned PASS. No live scans or `config/scope.txt` changes were made.

- 2026-05-16: Completed Phase 1 P1-5 program policy artifact validation hardening. `recon.sh` now validates allow artifacts before accepting policy PASS: artifact path must resolve under the current policy artifact directory, artifact schema/status must match, and request target/stage/technique/mode must match the current decision. `PROGRAM_POLICY_TIMEOUT_SECS` now accepts only canonical integer seconds from 1 to 60. Added regression tests for outside-dir artifacts, request mismatch, and timeout edge cases (`0`, `61`, `099`, `000`, huge integer, non-integer). Validation passed: bash syntax, py_compile, 43 pytest tests + 21 subtests, Hermes review, git diff check, static added-line scans, and Claude/Cowork independent review (`handoff/cowork_p1_5_artifact_validation_review.md`) returned PASS. No live scans or `config/scope.txt` changes were made.

- 2026-05-16: Completed Phase 1 P1-4 runtime integration for program policy gates. `recon.sh` now applies global `safe_target` plus per-stage program policy decisions before target-touching stages, records policy evidence under `evidence/policy/`, and enforces CIDR `--allow-cidr` forced-deny. Hardened boundary execution against env/script/interpreter and `PYTHONPATH/sitecustomize.py` allow-forgery by using fixed repo boundary paths and Python isolated mode for both boundary and helper subprocesses. Validation passed: bash syntax, py_compile, 40 pytest tests + 15 subtests, Hermes review, static added-line scans, and Claude/Cowork independent review (`handoff/cowork_p1_4_runtime_review.md`) returned PASS. No live scans or `config/scope.txt` changes were made.

- 2026-05-15: Completed Phase 1 P1-4 Task B program policy boundary wrapper. Codex added `scripts/program_policy_boundary.py` and `scripts/test_program_policy_boundary.py`; Hermes verified py_compile, 7 boundary tests, 17 policy helper tests, recon CLI tests, `bash -n recon.sh`, and `./bin/hermes review`. Independent review returned ACCEPT. This remains offline-only: no `recon.sh` stage integration, no live scans, no `config/scope.txt` changes, and no target-touching automation.

- 2026-05-15: Completed Phase 1 P1-4 runtime integration design proposal. Claude/Cowork wrote `handoff/cowork_p1_4_proposal.md`; Codex engineering review initially ROUTE_BACKed six blockers, then re-reviewed the revised proposal and returned `ACCEPT_FOR_IMPLEMENTATION`.

- 2026-05-15: Completed Phase 1 P1-3.1 policy decision contract hardening. Codex added versioned `policy_decision/1.0` output, stable `deny_reason_codes`, allow-path `reasons`, SHA-256 provenance, deterministic `decided_at_utc`, explicit IPv6 denial, stricter edge-case tests, and README no-cache/dry-run-recheck guidance. Claude/Cowork independently reviewed and returned ACCEPT; Hermes verified compile, 17 unit tests, CLI help/JSON shape, `git diff --check`, and `./bin/hermes review`. No `recon.sh` runtime integration or target-touching behavior was added.

- 2026-05-15: Initialized Git version control for the workspace and created private GitHub repository `Jack753951/cybersec-lab`. Hardened `.gitignore` before staging so local SSH keys/configs, logs, loot, scans, generated reports, caches, and secret-like files remain untracked; initial commit pushed to private `main`.

## History

- 2026-05-15: Added `scripts/gen_report.sh` offline Markdown report generator, documented it, generated a sample report artifact, hardened report sanitization/path handling after independent review, hardened the Windows Codex worker wrapper for this non-git workspace, and recorded validation results.

## 2026-05-15 - Codex review and offline report generator

- Reviewed `handoff/cowork_proposal.md` for feasibility, scope, and safety.
- Implemented `scripts/gen_report.sh` as an offline-only draft report generator for existing scan directories.
- Added `gen_report.sh` to `scripts/INDEX.md` and `scripts/README.md` with safe-use notes.
- Generated smoke-test artifacts under `reports/generated/scanme.nmap.org_20260514_134527/`.
- Hardened `scripts/gen_report.sh` after independent review: URL query secret redaction, Markdown/HTML escaping for scanner-controlled values, safer JSON string escaping, canonical scan path handling, symlink refusal, Markdown-only default output, and option-confusion hardening for output directory creation / optional pandoc conversion.
- Hardened `run_hermes_worker.ps1` so the Codex worker adds `--skip-git-repo-check` when this workspace has no `.git` directory, and so native Codex stderr does not become a PowerShell `NativeCommandError`.
- Refused/deferred scheduler, CVE automation, PDF dependency installation, webhook changes, Git initialization, deployment, billing, production behavior, and `config/scope.txt` changes because operator approval was not present.
- Validation: `run_hermes_worker.ps1 -Doctor` passed; direct Codex worker execution passed with `--skip-git-repo-check`; PowerShell parse check passed; `bash -n` passed for `scripts/gen_report.sh`, `recon.sh`, and `bin/hermes`; Windows handoff check passed; report smoke generation passed; report manifest parsed as JSON; URL query secret redaction smoke test passed; latest handoff check reports Python compile OK; `.agent.lock` is clear; DOCX generation was skipped because `pandoc` is not installed and DOCX is not the default.

## 2026-05-15 - Claude/Cowork independent Phase 0 review

- Ran Claude Code as Cowork independent reviewer for Codex's Phase 0 scope-hardening changes.
- Added `handoff/cowork_phase0_review.md` with verdict: accept with non-blocking follow-ups; no blocking issues found before Phase 1.
- Added `handoff/phase0_claude_review_summary.md` as Hermes-readable summary.
- Updated `.hermes.md` Safety Gate to reference mandatory `safe_target` runtime enforcement and future `programs/<program-slug>/scope.json` program scope/rules.
- Updated `HERMES_WORKFLOW.md` routing so any scope-enforcement, `safe_target`, recon filtering, or program-scope parser change requires explicit operator sign-off and dry-run validation before Codex implementation.

## 2026-05-15 - Phase 0 recon scope hardening

- Hardened `recon.sh` so every host/URL path goes through a single `safe_target` runtime guard with syntax validation, scope matching, pass/fail logging, and audit events.
- Kept `--skip-scope-check` only as a compatibility dry-run override: it now exits non-zero unless `SCOPE_OVERRIDE_TOKEN` and `SCOPE_OVERRIDE_CONFIRM` are both set and equal, and accepted use forces `DRY_RUN=true`.
- Refused `REQUIRE_SCOPE_CHECK=false` in `config/recon.conf` as a hard error, and updated the config comment to make that behavior explicit.
- Revalidated domain-expansion output before later stages consume it. Dropped subfinder/crt.sh hosts are recorded in `subdomains_dropped.txt` with concrete reasons and summarized.
- Prefiltered inputs for `find_live_hosts`, `port_scan`, `service_fingerprint`, `web_probe`, `dir_bruteforce`, and `vuln_scan`; stages now consume safe temp/input files rather than raw expansion or tool output.
- Validation: `bash -n recon.sh` passed; unauthorized synthetic target rejected in dry-run; authorized synthetic target passed in dry-run; `REQUIRE_SCOPE_CHECK=false` rejected; `--skip-scope-check` without matching token rejected; matching override token forced dry-run and completed without live egress; fake local domain-expansion tooling kept authorized names and dropped out-of-scope/malformed names; `.agent.lock` is clear/missing.
- Follow-up validation hardening: `bin/hermes review` now falls back from a non-runnable Git-Bash `python3` shim to `python`, and the latest review reports Python compile OK, shell scripts OK, and lock clear.

## 2026-05-15 - Phase 1 P1-1 independent review and Hermes acceptance

- Ran Claude/Cowork independent review for P1-1 schema/docs/examples.
- Added `handoff/cowork_phase1_p1_1_review.md` with verdict: ACCEPT.
- Claude/Cowork noted non-blocking P1-2 loader recommendations: chronology checks, testing-window ordering, allowed/forbidden disjointness, punycode TLD regex support, IPv6 posture, and minor `$defs` cleanup.
- Hermes verified JSON parse for all JSON files under `programs/` and local review passed.
- Confirmed P1-1 did not implement runtime scanning behavior and did not modify `recon.sh`, `config/scope.txt`, or `config/recon.conf`.

## 2026-05-15 - GitHub private repository bootstrap

- Initialized local Git version control for the workspace.
- Created private GitHub repository `Jack753951/cybersec-lab`.
- Hardened `.gitignore` before the initial commit/push to exclude local-only and sensitive runtime artifacts:
  - `setting/local/` including Kali SSH config/private keys
  - `logs/`, `loot/`, `scans/`, `<artifact-output-dir>/`
  - `reports/generated/`
  - `node_modules/`, Python caches, packet captures, KeePass/key material, `.env` and secret-like filenames
- Verified representative sensitive paths are ignored before staging.
- Boundary: repository is private. No GitHub Actions secrets were configured; no production deployment or external target interaction occurred.

## 2026-05-15 - Phase 1 P1-4 runtime integration design proposal

- Added `handoff/cowork_p1_4_proposal.md`, a design-only proposal for safely consuming `policy_decision/1.0` inside `recon.sh` before any runtime implementation begins.
- Proposal scope:
  - Add opt-in `--program <slug>` and explicit `--policy-mode {dry-run,planned,live}`.
  - Preserve `safe_target` as the first gate and add program policy only as an additional deny gate.
  - Use only schema-enum technique values, including `directory_bruteforce`; leave `send_notifications` out of P1-4 because no notification technique exists yet.
  - Remove fake `policy_preflight`; use only a non-decision environment/file/helper preflight.
  - Reject target-touching execution with `--policy-mode dry-run`; require planned/live policy modes for non-dry-run execution.
  - Use a jq-free stdlib Python boundary wrapper for helper timeout, JSON contract validation, atomic artifact writes, and shell-readable status.
  - Create evidence directories before any allow decision must be recorded.
  - Compose only actual rate-limit schema keys and leave `FEROX_THREADS` unchanged because no matching schema key exists.
  - Guarantee absolute no-side-effect behavior when `--program` is unset: no helper/Python checks, files, audit rows, or stdout/stderr changes.
- Added `handoff/codex_p1_4_proposal_review.md`:
  - Initial Codex engineering/safety review returned `ROUTE_BACK_FOR_DESIGN_FIXES` with six blocking design issues.
  - Claude/Cowork proposal was revised to address them.
  - Codex re-review returned `ACCEPT_FOR_IMPLEMENTATION`.
- Hermes acceptance boundary: this is design-only. No runtime code, tests, config, scope files, scanner behavior, or target-touching automation changed.
- Next step: create a narrowly scoped Codex implementation task for P1-4 Task A/B only, preferably starting with CLI validation + boundary wrapper tests before touching stage execution.

## 2026-05-15 - Phase 1 P1-3.1 policy decision contract hardening

- Codex implemented the P1-3.1 cleanup/hardening pass before any P1-4 runtime integration.
- Hardened `scripts/core/policy.py` so decision JSON now includes:
  - `schema_version: policy_decision/1.0`
  - stable `deny_reason_codes`
  - non-empty allow-path `reasons`
  - `program_file_sha256`
  - `global_scope_sha256`
  - deterministic UTC `decided_at_utc`
- Hardened `scripts/core/scope.py` so IPv6 target input fails with a clear unsupported-target path; the policy layer emits `IPV6_UNSUPPORTED`.
- Expanded `scripts/test_program_policy_check.py` from 11 to 17 tests, covering empty/comment-only global scope, malformed global-scope warning-only behavior, IPv4 CIDR intersection, URL port strictness, IPv6 denial code, blackout denial code, and CLI JSON contract fields.
- Updated `programs/README.md` to document the versioned policy decision contract, deny-on-uncertainty rule, no-cache runtime rule, and dry-run recheck requirement.
- Added `handoff/cowork_phase1_p1_3_1_review.md`; Claude/Cowork verdict: ACCEPT, no blocking issues.
- Hermes final validation passed:
  - `python -m py_compile scripts/program_policy_check.py scripts/core/policy.py scripts/core/scope.py scripts/test_program_policy_check.py`
  - `python -m unittest scripts/test_program_policy_check.py` ran 17 tests OK
  - `python scripts/program_policy_check.py --help`
  - `git diff --check` clean except line-ending warnings
  - `USER=${USER:-Owner} HACKLAB=<private-workspace> ./bin/hermes review` reports Python compile OK for 13 files, shell scripts OK, lock clear, and 12 scope entries.
- Boundary reaffirmed: P1-3.1 remains offline contract hardening only. It did not modify `recon.sh`, `config/scope.txt`, `config/recon.conf`, scheduler/cron behavior, or any target-touching automation.
- Next recommended step: P1-4 design proposal before runtime wiring, covering no-cache rule enforcement, audit-log row shape, schema-version consumer gate, CIDR fan-out policy, effective rate-cap composition, and refusal of program paths outside `programs/<slug>/scope.json`.

## 2026-05-15 - Phase 1 P1-3 independent review and Hermes acceptance

- Codex implemented the P1-3 offline program policy decision helper:
  - `scripts/core/__init__.py`
  - `scripts/core/scope.py`
  - `scripts/core/policy.py`
  - `scripts/program_policy_check.py`
  - `scripts/test_program_policy_check.py`
- The helper makes default-deny allow/deny decisions for a target + technique + mode using validated program scope plus global `config/scope.txt` intersection.
- It supports domain, wildcard, URL, IPv4, IPv4 CIDR, and `url_prefix` matching; program `out_of_scope` takes precedence; forbidden/unknown techniques deny; `planned`/`live` require `automation_permitted=true` and valid testing windows.
- Added `handoff/cowork_phase1_p1_3_review.md`; Claude/Cowork verdict: ACCEPT, no blocking issues.
- Claude/Cowork recommended a short cleanup hardening task before P1-4 runtime wiring: single-parse program data, decision schema/version/provenance, deny reason codes, allow-path reasons, and additional tests for malformed global scope, CIDR, URL ports, and IPv6 rejection.
- Hermes final validation passed:
  - `python -m py_compile scripts/program_policy_check.py scripts/core/policy.py scripts/core/scope.py scripts/test_program_policy_check.py`
  - `python -m unittest scripts/test_program_policy_check.py` ran 11 tests OK
  - `python scripts/program_policy_check.py --help`
  - `USER=${USER:-Owner} HACKLAB=<private-workspace> ./bin/hermes review` reports Python compile OK for 13 files, shell scripts OK, lock clear, and 12 scope entries.
- Boundary reaffirmed: P1-3 is offline policy computation only. It did not modify `recon.sh`, `config/scope.txt`, or `config/recon.conf`; it did not run scans, probes, modules, fuzzers, or target-touching behavior.

## 2026-05-15 - Reusable workflow history / blueprint document

- Operator requested a reusable document explaining how this project built its workflow so other projects can learn from it.
- Added `handoff/workflow_build_history_and_reusable_blueprint.md` covering:
  - role split between Hermes, Codex, Claude/Cowork, and operator
  - default strategy -> implementation -> independent review -> Hermes verification loop
  - safety-gate model and how to generalize it to other domains
  - durable handoff files and append-only accepted history
  - staged build history from scope hardening through periodic third-party review
  - reusable adoption recipe, directory template, prompt templates, checklist, and anti-patterns
- Boundary: documentation only; no runtime behavior, scanning behavior, scope policy, or scheduler settings changed.

## 2026-05-15 - Periodic third-party strategy/project review process

- Operator requested recurring reviews of project state and future strategy by both Codex and Claude/Cowork.
- Added `handoff/periodic_third_party_review_process.md` defining the weekly offline review process, expected files, safety boundary, and review template.
- The recurring review should inspect project state, ask Codex for engineering/systematization recommendations, ask Claude/Cowork for strategy/product-security recommendations, and have Hermes synthesize next actions.
- Boundary: offline project review only; no live scans, no target interaction, no `config/scope.txt` modification, no exploit payloads, no secrets in outputs.

## 2026-05-15 - Claude/Cowork systematization recommendations adopted

- Reviewed Claude/Cowork's project-health and extensibility recommendations.
- Hermes locally verified the main structural claims:
  - `recon.sh` is 986 lines, confirming monolithic-script risk.
  - `exploits/`, `cves/`, `recon/`, and `tools/` existed but were empty.
  - `modules/`, `scripts/core/`, and `runs/` were not yet implemented.
  - existing candidate scripts include `headers_audit.sh`, `cors_audit.sh`, `jwt_inspect.sh`, and other triage-oriented scripts.
- Added `handoff/systematization_extension_plan.md` as the staged implementation plan for core contracts, schemas, run manifests, module manifests, runner skeleton, and later pilot modules.
- Added README files to previously empty intent directories so agents do not misinterpret them:
  - `exploits/README.md`
  - `cves/README.md`
  - `recon/README.md`
  - `tools/README.md`
- Updated `handoff/module_system_architecture_notes.md` to incorporate risk-tiered module directories: `modules/checks/`, `modules/triage/`, and `modules/verify/`.
- Decision: choose option (1) before option (2). Build `scripts/core/` and schema contracts first; do not migrate `headers_audit.sh` into a module until policy/finding/evidence/run contracts are stable.
- Boundary: documentation and directory-hygiene only; no scanner behavior, module runner, vulnerability module execution, or `recon.sh` runtime behavior changed.

## 2026-05-15 - Claude/Cowork review contract expansion

- Operator clarified Claude/Cowork review should not only identify blocking issues; it should also provide third-party recommendations aligned with the project goals.
- Updated `.hermes.md`, `HERMES_WORKFLOW.md`, and `handoff/sustained_review_loop.md` so future review prompts require:
  - blocking issues that must route back to Codex
  - non-blocking improvements worth doing soon
  - strategic recommendations for future phases
  - architecture-fit assessment against extensibility, updateability, modularity, safe automation, agent-assisted analysis, testing, and roadmap alignment
- Updated persistent workspace memory so this review style remains the default.

## 2026-05-15 - Module system architecture direction

- Incorporated the operator-provided Codex suggestion for modular vulnerability scripts/checks.
- Added `handoff/module_system_architecture_notes.md` documenting the proposed `scripts/core/` layer, top-level `modules/` layout, module manifest contract, normalized module input/output, three-level module model, safety gates, and agent-review integration.
- Updated `handoff/extensible_architecture_direction.md` so future phases preserve this shape.
- Boundary: architecture documentation only; no module runner, no vulnerability checks, no scan behavior, and no runtime execution were added.

## 2026-05-15 - Phase 1 P1-2 independent review and Hermes acceptance

- Ran Claude/Cowork independent review for the P1-2 offline validator.
- Added `handoff/cowork_phase1_p1_2_review.md` with verdict: ACCEPT, no blocking issues.
- Claude/Cowork reproduced offline validation: unit tests pass, safe examples allow, invalid fixtures deny, and additional default-deny probes deny as expected.
- Non-blocking observations recorded for future phases: URL-check readability, `file:` program URL documentation, IPv4-only scope entries, in/out scope duplicate precedence, and runtime timezone fail-closed behavior.
- Hermes final validation passed: examples allow with `--ignore-time`, unittest suite passes, Python compile OK, shell scripts OK, lock clear.
- Boundary reaffirmed: P1-2 remains offline validation only; P1-3 must treat `validate_file(...).verdict == "allow"` as necessary but not sufficient and still enforce global scope intersection, technique gates, automation flags, rate/window checks, and audit events.

## 2026-05-15 - Phase 1 P1-2 offline program scope validator

- Added `scripts/validate_program_scope.py`, a Python 3 standard-library offline validator for future `programs/<slug>/scope.json` files.
- Added `scripts/test_validate_program_scope.py` with `unittest` coverage for valid examples, invalid fixtures, JSON output shape, slug/path mismatch, expiration enforcement, and punycode/raw-IDN behavior.
- Added invalid safe fixtures under `programs/_examples/invalid/` for technique overlap, non-authorizable allowed technique, expired policy, raw Unicode IDN, and overnight testing window.
- Updated `programs/README.md` and `programs/_schema/README.md` with validator usage, default-deny behavior, structured output, and the P1-2 non-runtime boundary.
- Validation: safe examples passed with `--ignore-time`; `python -m unittest scripts/test_validate_program_scope.py` passed 6 tests; Hermes local review passed with Python compile OK, shell scripts OK, and lock clear.
- Boundary: did not modify `recon.sh`, `config/scope.txt`, or `config/recon.conf`; did not add runtime scanning behavior, live target testing, global-scope intersection, or runtime technique gates. P1-3 remains the future integration phase after review.

## 2026-05-15 - Phase 1 P1-3 offline program policy decision helper

- Added `scripts/core/scope.py` for standard-library target normalization and scope matching across domain, wildcard, URL, IPv4, IPv4 CIDR, and `url_prefix` entries.
- Added `scripts/core/policy.py` as the default-deny decision engine requiring P1-2 validator allow, program in/out scope, global scope intersection, technique gates, automation-mode gates, and testing-window gates for target-touching modes.
- Added `scripts/program_policy_check.py` CLI with text and `--json` output fields for verdict, normalized target, errors, warnings, and audit event.
- Added `scripts/test_program_policy_check.py` with 11 unittest cases using temp fixtures: global/program intersection allow and deny, out-of-scope precedence, forbidden technique deny, automation-disabled live deny, automation-disabled dry-run allow, raw Unicode IDN deny, wildcard apex/subdomain behavior, URL prefix allow, and CLI JSON shape.
- Updated `programs/README.md` to document P1-3 usage and boundary: offline decision only, no `recon.sh` integration yet, global scope intersection is required, and `allow` is policy preflight only.
- Validation: default `python` launcher is broken in this Windows shell, so the required Python commands fail with `No Python at ...Python312...`; the same compile, unittest, and help commands pass with the available local Epic Python 3 binary. Hermes review is recorded in `handoff/codex_review.md`.
- Boundary: did not modify `recon.sh`, `config/scope.txt`, or `config/recon.conf`; did not run scans, probes, curl, nmap, nuclei, fuzzers, modules, DNS lookups, or any external target-touching behavior.

## 2026-05-15 - Post-scan agent review workflow

- Operator confirmed scan-result review should not rely only on scripts.
- Added `handoff/post_scan_agent_review_workflow.md` defining the intended division of labor: scripts/modules produce structured candidate findings and evidence; Hermes verifies scope/run integrity; Claude/Cowork reviews triage, false positives, impact, manual verification steps, and report quality; Codex fixes automation defects.
- Updated `HERMES_WORKFLOW.md` and `handoff/extensible_architecture_direction.md` so future implementation preserves agent-assisted post-scan analysis.

## 2026-05-15 - Extensible architecture direction

- Operator clarified that the project should be update-friendly and systematized beyond multi-agent collaboration, with future extensibility such as modular vulnerability scripts/plugins as a central goal.
- Added `handoff/extensible_architecture_direction.md` documenting architectural principles, future module layout, module manifest concepts, policy/data separation, versioned contracts, and near-term constraints for Phase 1.
- Updated `HERMES_WORKFLOW.md` and `handoff/cowork_phase1_proposal.md` so future Codex tasks preserve extensibility and avoid growing `recon.sh` into an unmaintainable monolith.

## 2026-05-15 - Phase 1 planning kickoff

- Ran Claude/Cowork strategy pass for Phase 1 program-specific scope intake and validation.
- Added `handoff/cowork_phase1_proposal.md` covering `programs/<program-slug>/scope.json` schema, scope semantics, technique gates, rate/window rules, `recon.sh` integration, dry-run test matrix, migration plan, implementation chunks, and acceptance criteria.
- No code, scanner behavior, `config/scope.txt`, or `config/recon.conf` changes were made.
- Validation: local Hermes review passed after proposal creation.

## 2026-05-15 - Phase 0.1 Claude/Cowork review and hygiene

- Ran Claude/Cowork independent review for Phase 0.1 validation evidence.
- Added `handoff/cowork_phase0_1_review.md` with verdict: ACCEPT, no blocking issues, Phase 1 may start.
- Archived full local dry-run audit rows to `handoff/phase0_1_full_audit_rows.md` before removing temporary validation directories.
- Clarified `handoff/phase0_1_validation_evidence.md` wildcard semantics to reference the existing `config/scope.txt` policy.

## 2026-05-15 - Phase 0.1 validation evidence closure

- Added validation-only evidence for the non-blocking Cowork Phase 0 follow-ups in `handoff/phase0_1_validation_evidence.md` and summarized it in `handoff/codex_review.md`.
- Used isolated temp lab roots and temp scope files only; did not modify `config/scope.txt`, add recon features, or run live scans.
- Validated punycode allow behavior for `xn--bcher-kva.example`, raw Unicode IDN rejection, wildcard positive behavior for `a.b.example.com` under `*.example.com`, trailing-dot rejection for `example.com.`, and dry-run `scanme.nmap.org` safe-target audit rows.
- Documented current wildcard semantics: `*.example.com` currently includes apex `example.com`; Phase 1 should explicitly preserve or alter this as a policy decision.
- Validation: `bash -n recon.sh` passed; `USER=${USER:-Owner} HACKLAB=<private-workspace> ./bin/hermes review` exited `0` with lock clear and shell syntax OK, while emitting sandbox-specific `<user-home>` mkdir warnings.

## 2026-05-15 - Phase 1 P1-1 program scope scaffolding

- Added `programs/_schema/scope.schema.json` as the draft 2020-12 program scope/rules contract for future `programs/<program-slug>/scope.json` files.
- Added safe placeholder examples under `programs/_examples/` for public bounty, client engagement, and CTF platform workflows using only RFC 5737 IP ranges and RFC 2606/example domains.
- Added `programs/README.md` and `programs/_schema/README.md` documenting default-deny semantics, operator responsibilities, global `config/scope.txt` intersection, wildcard apex behavior, IDN restrictions, and future module/plugin reuse.
- Did not modify `recon.sh`, `config/scope.txt`, or `config/recon.conf`; no runtime scanning behavior or live target testing was added.
- Validation: Node parsed all JSON files under `programs/`; `USER=${USER:-Owner} HACKLAB=<private-workspace> ./bin/hermes review` exited `0`. Python and local JSON Schema validation were unavailable in this environment and are documented in `handoff/codex_review.md`.


## 2026-05-15 - Phase 1 P1-4 Task A CLI Surface

Accepted by: Hermes after Codex implementation and Claude/Cowork independent review.

Summary:

- Added inert `recon.sh` CLI state/parsing for `--program <slug>`, `--policy-mode {dry-run|planned|live}`, and `--allow-cidr`.
- Added `validate_program_flags` to enforce slug syntax, reserved/path-like slug rejection, `${HACKLAB}/programs/<slug>/scope.json` lookup, readable regular-file checks, resolved-path containment, `--program` vs `--skip-scope-check` incompatibility, explicit policy mode, and `--policy-mode dry-run` requiring `--dry-run`.
- Added an operator warning for validated `--program` runs that Task A has not wired per-stage policy decisions yet; only global `safe_target` is active until Task B.
- Added offline CLI regression tests in `scripts/test_recon_program_cli.py` using temporary `HACKLAB` fixtures and synthetic `authorized.test` targets.
- Preserved Task A non-goals: no boundary helper, no `program_policy_check.py` call from `recon.sh`, no per-stage gates, no rate-limit composition, no policy evidence artifacts, no CIDR policy decisions, and no `safe_target` behavior changes.

Validation:

- `bash -n recon.sh` PASS.
- `python -m unittest scripts/test_recon_program_cli.py` PASS: 12 tests OK, 1 optional symlink test skipped on Windows.
- `USER=${USER:-Owner} HACKLAB=<private-workspace> ./bin/hermes review` PASS: Python compile OK, shell scripts OK, lock clear.
- Independent Claude/Cowork final review: `handoff/cowork_p1_4_task_a_final_review.md` verdict ACCEPT.

Remaining non-blocking carry-forward for Task B:

- Add/keep defense-in-depth comments around redundant slug/path-like checks.
- Consider replacing the `case` resolved-path prefix check with a literal `[[ ... ]]` check for paths containing glob metacharacters.
- Add directory-level symlink and planned/live-without-`--dry-run` tests.
- Do not use `--program --policy-mode planned/live` against non-lab targets until Task B wires actual per-stage policy gates.

## 2026-05-15 - Phase 1 P1-4 Task B program policy boundary wrapper

Accepted by: Hermes after Codex implementation and independent review.

Summary:

- Added `scripts/program_policy_boundary.py`, a standard-library-only subprocess boundary around `scripts/program_policy_check.py --json`.
- The boundary validates the `policy_decision/1.0` contract, helper exit-code/verdict consistency, schema enum technique, hash provenance fields, timestamp shape, and audit-event consistency.
- The boundary fails closed on helper timeout, invalid JSON, contract mismatch, helper startup error, or artifact write failure; only validated `allow` exits `0`, validated `deny` exits `1`, and boundary/internal errors exit `2`.
- Added atomic JSON evidence artifact writes via temp file plus `os.replace` and flat shell-readable `POLICY_BOUNDARY_*=` stdout lines for future Bash integration.
- Added `scripts/test_program_policy_boundary.py` covering allow, deny, invalid JSON, timeout, contradictory helper exit/verdict, artifact shape, and no raw nested JSON stdout.
- Applied an independent-review hardening follow-up in `recon.sh`: resolved program scope files must now resolve exactly to `programs/<slug>/scope.json`, preventing same-tree symlink aliasing to another program scope.
- Added recon CLI coverage for same-tree symlink alias rejection where symlink creation is available.

Validation:

- `python -m py_compile scripts/program_policy_boundary.py scripts/test_program_policy_boundary.py` PASS.
- `python -m unittest scripts/test_program_policy_boundary.py` PASS: 7 tests OK.
- `python -m unittest scripts/test_program_policy_check.py` PASS: 17 tests OK.
- `python -m unittest scripts/test_recon_program_cli.py` PASS: 13 tests OK, 1 optional symlink test skipped on this Windows environment.
- `bash -n recon.sh` PASS.
- `USER=${USER:-Owner} HACKLAB=<private-workspace> ./bin/hermes review` PASS: Python compile OK, shell scripts OK, lock clear.
- Independent review: `handoff/cowork_p1_4_task_b_review.md` verdict ACCEPT.

Safety boundary:

- No live scans, probes, exploitation, brute force, callbacks, scanner invocations, scheduler/deployment changes, `config/scope.txt` edits, real program scope edits, or target-touching automation were performed.
- `recon.sh` stage execution still does not call the boundary helper; Task C remains required before dry-run stage integration.


## 2026-05-16 - Phase 2 P2-4 dry-run-only module runner skeleton

Accepted by: Hermes after TDD implementation, two independent review blocker cycles, and final PASS re-review.

Summary:

- Added `scripts/module_runner.py`, a standard-library dry-run-only runner skeleton that validates module manifests and policy allow artifacts before producing a planned `run/1.0` manifest preview.
- Added `scripts/test_module_runner.py` covering safe passive planning, invalid manifest denial, missing/deny policy denial, dry-run target-touching/network denial, policy target/mode/target_type mismatch denial, policy artifact run-path binding, and contradictory policy artifact denial.
- Updated `scripts/README.md` and `modules/_schema/README.md` with the runner command and safety boundary.
- Recorded independent review and blocker-fix history in `handoff/cowork_p2_4_module_runner_review.md`.

Validation:

- `env -u PYTHONIOENCODING -u PYTHONUTF8 python -m pytest scripts/test_module_runner.py -q` PASS: 8 passed, 9 subtests passed.
- `env -u PYTHONIOENCODING -u PYTHONUTF8 python -m pytest scripts/test_*.py -q` PASS: 95 passed, 2 skipped, 102 subtests passed.
- `python -m py_compile scripts/module_runner.py scripts/test_module_runner.py` PASS.
- JSON parse for `modules/_schema/*.json` PASS.
- `USER=${USER:-Owner} HACKLAB=<private-workspace> ./bin/hermes review` PASS.
- `git diff --check` PASS aside from CRLF warning on Markdown.
- Independent review final verdict PASS.

Safety boundary:

- Offline planning/test/docs only. No live scans, external target interaction, module execution, scanner invocation, exploit/fuzz/brute force/callback behavior, `config/scope.txt` change, credentials, loot, reports, scheduler, deployment, billing, or production setting changes.

## 2026-05-16 — P2-7 data-driven module profile contract

- Added repo-local `modules/profiles/audit-baseline.json` and `modules/_schema/module_profile.schema.json`.
- Added `scripts/validate_module_profile.py` standard-library validator.
- Updated `scripts/module_runner.py` to load profile data, fail closed on missing/malformed/unsafe profiles, enforce profile constraints for discovery and explicit manifests, and bind `execution.profile_id` / `execution.profile_sha256` in planned `run/1.0` previews.
- Updated run validator/schema, tests, docs, and Claude third-party direction/implementation reviews.
- Validation: focused profile/runner tests passed (27); full scripts unittest passed (117 passed, 2 skipped).
- Safety: offline schema/config/test/docs only; no live scans, target touching, module/scanner execution, network clients/callbacks, scope changes, credentials, loot, scheduler/deployment/billing/production changes.

## 2026-05-18 - P2-14 preview_manifest/1.0 schema and validator

Accepted by: Hermes final acceptance after local validation and independent Claude/Cowork implementation review PASS.

Summary:

- Added `modules/_schema/preview_manifest.schema.json` for the closed offline `preview_manifest/1.0` contract.
- Added `scripts/validate_preview_manifest.py`, a standard-library-only read-only validator for already-persisted `runs/<run_id>/preview` bundles.
- Validator rejects malformed/trailing/duplicate-key JSON, unknown fields, unsafe run IDs and paths, absolute paths, drive prefixes, backslashes, symlinks, missing/extra artifacts, `preview_manifest.json` artifact listing, hash/size drift, and non-allow/non-ok bundle consistency output.
- Updated P2-13 `scripts/module_runner.py` preview manifest data shape to emit the formal `producer`, `preview_mode`, `bundle_consistency`, and fixed artifact descriptors. The new validator is not wired into the runner write path.
- Added focused tests in `scripts/test_preview_manifest.py` and updated the existing module runner persistence assertion.
- Updated `scripts/README.md`, `modules/_schema/README.md`, and `handoff/codex_review.md`.

Validation:

- `python -m py_compile scripts/validate_preview_manifest.py scripts/test_preview_manifest.py` PASS.
- `python -m unittest scripts/test_preview_manifest.py` PASS: 15 tests OK, 1 optional symlink test skipped.
- `python -m py_compile scripts/module_runner.py scripts/test_module_runner.py` PASS.
- `python -m unittest scripts/test_module_runner.py` PASS: 32 tests OK, 1 skipped.
- `python -m unittest discover -s scripts -p 'test_*.py'` PASS: 165 tests OK, 4 skipped.
- `git diff --check` PASS with only CRLF normalization warnings.
- `USER=${USER:-Owner} HACKLAB=<private-workspace> ./bin/hermes review` via Git Bash exited 0 but was limited by this shell resolving `<user-home>/projects/cybersec` and printing permission warnings for that path.

Safety boundary:

- Offline contract/schema/validator/test/docs work only.
- No live scans, probes, scanner execution, module execution, subprocess/process launch paths, network clients, callbacks, exploit/fuzz/brute-force behavior, target touching, findings/evidence/report emission, loot access, generic output hooks, scope/config changes, credentials, scheduler, deployment, billing, or production settings changed.
- The unverified CVE quarantine file was preserved.

## 2026-05-18 - Phase 1 P1-4 Task B Boundary Contract Tightening

- Tightened `scripts/program_policy_boundary.py` so `policy_decision/1.0` helper output must use exactly the expected top-level contract fields; missing or unknown fields now fail closed as boundary errors.
- Added `scripts/test_program_policy_boundary.py` regression coverage for unexpected helper contract fields while preserving the original decision/error details in the artifact.
- Validation was offline only. Focused boundary and policy helper tests passed; recon CLI validation and direct `bash -n recon.sh` are blocked by existing CRLF line terminators in `recon.sh`, which was not changed for this Task B boundary-only slice.


## 2026-05-21T00:47:56Z — Phase 4A Juice Shop bounded lab calibration

- Scope: local host-only OWASP Juice Shop lab at `http://<lab-ip>:3000`; candidate-only.
- Added lab-only modular prototype: `setting/local/local_lab_web_observation_probe.py` and `setting/local/local_lab_web_observation_probe.module.json` for fixed-path metadata, SPA fallback, and `/ftp/` metadata-only classification.
- Added review/report artifacts: `handoff/phase4a_juice_review1_20260521.md`, `handoff/phase4a_juice_review2_20260521.md`, `handoff/phase4a_juice_review2_candidate_packet_20260521.md`, `reports/phase4a_juice_shop_lab_pentest_report_20260521.md`.
- Runtime evidence stored under `scans/phase4a_juice_20260521_083738/`.
- Validation: `python -m py_compile`, JSON validation, `bash -n`, victim health HTTP 200, and `HACKLAB=<private-workspace> USER=Owner ./bin/hermes review` passed.
- Safety: no credential attacks, dumps, recursive downloads, external callbacks, destructive actions, or confirmed real bug bounty claims.


## 2026-05-21T01:04:41Z — Claude Code report review and isolated aggressive lab gate

- Claude Code third-party reviewed `reports/phase4a_juice_shop_lab_pentest_report_20260521.md`; raw usage/result saved to `handoff/claude_report_third_party_review_20260521.json` and markdown to `handoff/claude_report_third_party_review_20260521.md`.
- Claude Code revised the lab-only report and created `handoff/phase4a_isolated_aggressive_lab_slice_20260521.md`; raw result saved to `handoff/claude_report_revision_and_aggressive_slice_20260521.json` and `handoff/claude_aggressive_slice_plan_20260521.json`.
- Hermes hardened `<victim-vm>` runtime sharing controls and wrote `handoff/phase4a_isolated_aggressive_lab_gate_status_20260521.md`; aggressive execution remains blocked because C: has only ~6 GB free and no separate attacker clone/snapshots exist.
- Claude Code post-revision review accepted the revised lab report as lab-only and accepted the aggressive slice as policy while keeping execution blocked; result saved to `handoff/claude_post_revision_gate_review_20260521.*`.
- Validation: Claude JSON files parse, lab plugin compiles, and `HACKLAB=<private-workspace> USER=Owner ./bin/hermes review` passed.


## 2026-05-21 Phase 4A isolated aggressive lab setup gate

- Hermes verified C: free space recovered to about 177 GB and cleared the prior disk blocker.
- Created full VirtualBox clone `<attacker-vm>` from `kali-linux-2026.1-virtualbox-amd64` after saving the source VM state when ACPI shutdown did not complete.
- Hardened clone to host-only only (`nic1=hostonly`, `nic2-4=none`), removed inherited `hacking` shared folder, disabled clipboard/drag-and-drop and runtime clipboard file transfer.
- Created attacker snapshot `clean-before-aggressive-tests-20260521-093233` and victim recovery snapshot `pre-aggressive-current-running-recovery-20260521-093252`; existing victim setup snapshot remains `setup-complete-with-tools`.
- Verified attacker route table has only `<lab-ip>/24` and no default route, and verified Juice Shop lab health from attacker to victim via `curl -I http://<lab-ip>:3000/` returning HTTP 200.
- Updated `handoff/phase4a_isolated_aggressive_lab_gate_status_20260521.md`.
- Boundary: setup slice only; aggressive execution still requires a script-specific run card and safety/model review before running.


## 2026-05-21 Phase 4A.1 bounded content discovery lab run

- Created run card `handoff/phase4a_1_bounded_content_discovery_run_card_20260521.md` for a local-lab-only, fixed-target, fixed-path, HEAD-only metadata discovery rehearsal.
- Safety review accepted the run card and was saved at `handoff/phase4a_1_bounded_content_discovery_safety_review_20260521.md`.
- Executed from `<attacker-vm>` against local Juice Shop victim `http://<lab-ip>:3000/` using direct SSH after the PowerShell wrapper quoting stripped Python heredoc/string quotes.
- Artifacts saved under `handoff/phase4a_1_bounded_content_discovery_run_20260521/`: pre/post health, observations JSONL, execution exit, copied run card, and summary.
- Result: pre-health 0, execution 0, post-health 0, 29 observations, status counts 200=25 / 301=1 / 500=3, no errors, no health degradation.
- Output-side review accepted results as candidate-only observations and recorded false-positive traps at `handoff/phase4a_1_bounded_content_discovery_output_review_20260521.md`.
- Boundary: do not claim confirmed findings; next allowed action is bounded manual content-class verification of a short candidate list only.


## 2026-05-21 Operator-provided manual tool references

- Recorded user-provided manual/support tool links in `handoff/operator_tool_references_20260521.md`: VirusTotal, IPAddressGuide CIDR calculator, HexEd.it, and Censys Search.
- Boundary: these references support manual triage/scope/research workflows only; they are not automation dependencies and do not authorize external target interaction or uploading sensitive artifacts.


- 2026-05-21: Completed Phase 4A.1 bounded manual content-class verification for the local Juice Shop lab candidate list after the output-side review allowed it. Generated `scans/phase4a_content_class_verification_20260521_104314/content_class_verification.json` and `scans/phase4a_content_class_verification_20260521_104314/lab_report_rehearsal.md`; recorded synthesis in `handoff/phase4a_1_content_class_verification_result_20260521.md`. The run used fixed GET-only paths (`/robots.txt`, `/.well-known/security.txt`, `/ftp/`, `/rest/products/search`, `/api-docs/`) with request cap 7 including pre/post health, no recursion/downloads/credentials/payloads/callbacks/brute force, and metadata/redacted snippets only. Health stayed HTTP 200 with stable root hash. `/ftp/` is verified as a real directory-listing candidate (`title: listing directory /ftp/`, 12 listing names, sensitive-looking filename preview) and is lab-report-draft-ready as metadata exposure only; it remains local-lab-only and not real bug-bounty-submission-ready. Also assessed `https://github.com/nashsu/llm_wiki` as useful for sanitized methodology/research knowledge but not for active authorization, sensitive evidence, or replacing repo handoff/security gates; assessment recorded in `handoff/llm_wiki_assessment_20260521.md`.


- 2026-05-21: Completed Phase 4A.1 option A by drafting a focused lab-only `/ftp/` directory-listing report rehearsal at `reports/phase4a_ftp_directory_listing_lab_report_20260521.md`. The report uses the bounded content-class verification artifacts from `scans/phase4a_content_class_verification_20260521_104314/`, keeps the claim constrained to metadata/directory-listing exposure, includes reproduction, impact limitations, remediation, retest plan, report-readiness gate, non-findings appendix, and `NOT_READY_FOR_REAL_BUG_BOUNTY_SUBMISSION` labeling. Ran a simple raw-secret pattern scan over the report (private key, AWS key, GitHub PAT, Slack token, JWT, Cookie/Set-Cookie header, password/api-key assignment patterns) with 0 findings. Boundary: report drafting only from local-lab minimized evidence; no new target requests, no file downloads, no sensitive content proof, no real bounty submission, and no status promotion beyond lab-report-draft-ready candidate.


- 2026-05-21: Completed independent report/safety review for `reports/phase4a_ftp_directory_listing_lab_report_20260521.md`. Reviewer route/tool: Hermes `delegate_task`; visible model/runtime self-report `gpt-5.5`, exact lower-level backend not independently exposed; reviewer read local report only and made no target requests. Verdict: `PASS_WITH_RECOMMENDATIONS`, no blockers. Hermes applied non-blocking wording refinements to avoid public-internet/readiness ambiguity: retitled the finding as unauthenticated local-lab exposure, clarified public means reachable without authentication inside host-only lab, changed section wording to directory-listing response evidence, qualified password-manager-database-like artifacts as only if present/confirmed, and made representative artifact retest checks conditional on separate authorization/scope. Recorded review synthesis in `handoff/phase4a_ftp_report_independent_review_20260521.md`. Re-ran raw-secret pattern scan over the revised report with 0 findings. Boundary: local report edits/review only; no target interaction, no file downloads, no sensitive evidence expansion, no real bounty submission, and no promotion beyond lab-only candidate/report rehearsal.


- 2026-05-21: Opened Phase 4B direction for OWASP Top 10 module collection and local-lab trials. Created `.hermes/plans/owasp-top10-module-collection-and-lab-trial-20260521.md`, `modules/owasp_top10_lab_module_catalog.json`, `modules/OWASP_TOP10_LAB_MODULE_CATALOG.md`, and `handoff/phase4b_owasp_module_collection_direction_20260521.md`. Scope boundary: catalog/planning only for this slice; no target-touching execution, no public/client/real bug-bounty targets, no random exploit-script vendoring, no credential/brute-force/callback/persistence/destructive/loot behavior, and no automatic confirmed-finding promotion. Future manifests/importers are T3 + OSS Recon Gate; any local-lab target-touching script is T4 per module with a script-specific run card, explicit approval, pre/post health, request cap, redaction, recovery, and output-side review.


- 2026-05-21: Collected and classified existing OWASP-relevant local scripts and Kali tool inventory for Phase 4B. Created `handoff/phase4b_owasp_script_collection_and_target_plan_20260521.md` and `handoff/phase4b_owasp_script_inventory_20260521.json`. Inventory source included existing project scripts (`headers_audit.sh`, `cors_audit.sh`, `open_redirect.sh`, `xss_finder.sh`, `sqli_triage.sh`, `lfi_finder.sh`, `ssrf_finder.sh`, etc.) and Kali bridge tool check (`nmap`, `nikto`, `sqlmap`, `gobuster`, `ffuf`, `nuclei`, `masscan`, `msfconsole`, `hydra`, `john`, `hashcat`, `burpsuite`; `zaproxy` missing). Decision: proceed in waves against the local Juice Shop lab only, starting with Wave 1A headers/CORS/known-path metadata. Deferred or blocked high-risk scripts as-is: SSRF/OAST callbacks, LFI secret file reads, brute force, hydra, masscan, msfconsole, and sqlmap until T4 run card. Boundary: no target-touching execution performed in this inventory slice; output remains candidate-only.


- 2026-05-21: Executed Phase 4B Wave 1A against the local OWASP Juice Shop host-only lab (`http://<lab-ip>:3000/`) through the Kali bridge. Route/tool: Hermes -> `scripts/kali-run.ps1` -> `<attacker-vm>`; output pulled to `<artifact-output-dir>/phase4b_wave1a_20260521T030959Z/`. Execution included only low-risk metadata/content-discovery checks: pre/post health, header observation, bounded CORS observation, known-path metadata, tiny ffuf/gobuster wordlists, and a custom local nuclei info template. Health remained HTTP 200 before/after. Result recorded in `handoff/phase4b_wave1a_result_20260521.md`. Key decisions: `/ftp/` remains the known lab-report candidate; `/api-docs/` is a separate documentation-exposure triage candidate; CORS wildcard without credentials is a non-finding; missing headers are hardening metadata; SPA fallback/gobuster wildcard behavior is a false-positive trap. Modularized the effective Wave 1A metadata flow into new dry-run/offline Level 1 module manifests: `level1.directory_listing_metadata`, `level1.robots_securitytxt_metadata`, `level1.api_docs_metadata`, `level1.dependency_manifest_metadata`, and `level1.cors_metadata`. Boundary: no SQLi/XSS/LFI/SSRF/bruteforce/callback/destructive scripts, no public targets, no raw body/loot collection, and no confirmed-finding promotion.


- 2026-05-21: Promoted Phase 4B Wave 1A from manifest-only planning into a reusable bounded local-lab adapter. Added `scripts/lab_modules/wave1a_metadata.py` and `scripts/test_wave1a_metadata_lab_module.py`. The adapter is plan-only by default and requires `--lab-approved` before writing an executable bash script; it rejects public targets, enforces local/private lab URL checks, request cap, timeout, rate limit, pre/post health, fixed known paths, fixed CORS origins, JSONL observation output, and non-promotional candidate-only semantics. It maps the runtime checks back to the Level 1 module IDs (`directory_listing_metadata`, `robots_securitytxt_metadata`, `api_docs_metadata`, `dependency_manifest_metadata`, `cors_metadata`) but still does not auto-promote findings, submit reports, recurse/crawl, transfer files, run exploit payloads, use callbacks, or touch real/public bug-bounty targets. Validation: RED test first failed on missing adapter; focused suite now passes (`python -m unittest scripts.test_wave1a_metadata_lab_module -v`), adjacent external-tool runner/importer tests pass, generated script passes `bash -n`, and public target generation fails closed with exit 2.


- 2026-05-21: Added the offline importer half of Phase 4B Wave 1A so adapter output can feed the review pipeline without trusting scanner output as a finding. Added `scripts/import_wave1a_metadata_observations.py`, `scripts/test_wave1a_metadata_observation_importer.py`, and fixtures under `tests/fixtures/wave1a_metadata/`. The importer reads only allowlisted `tests/fixtures/wave1a_metadata/*.jsonl`, normalizes `wave1a_metadata.py` JSONL into `wave1a_metadata_observations/0.1-trial` observations plus `needs_manual_review` candidate seeds for `/ftp/` directory-listing and `/api-docs/` documentation-exposure leads, and rejects malformed JSONL fail-closed with no partial observations. Boundary: offline/local only; no network I/O, subprocess execution, target interaction, raw body import, report submission, or `confirmed`/`verified`/`reportable`/`accepted` status output. Validation: RED test first failed on missing importer; focused importer tests now pass, and adjacent Wave 1A adapter/external-tool importer tests remain green.


- 2026-05-21: Added the Phase 4B Wave 1A candidate-review bridge so low-risk metadata candidate seeds can enter the existing offline review vocabulary without becoming findings. Added `scripts/build_wave1a_candidate_review_fixture.py` and `scripts/test_wave1a_candidate_review_bridge.py`; updated `scripts/README.md`. The bridge consumes only the Wave 1A fixture/importer path, converts `needs_manual_review` seeds into `finding/1.0` `candidate` fixtures with empty evidence, scanner-output-only/manual-verification triage, validates them with the existing finding validator, and projects them in memory into `candidate_review_packet/0.1-trial`. Boundary: JSON stdout only, no target interaction, network I/O, subprocess execution, default file writes, report drafting/submission, live target flags, or `confirmed`/`verified`/`reportable`/`accepted` status promotion. Validation: RED first failed on missing bridge; focused bridge tests now pass, adjacent Wave 1A importer/adapter and candidate-review packet/gap suites pass, and smoke output has 2 candidate fixtures / 2 packet findings with `report_readiness: not_ready`.


- 2026-05-21: Updated the Phase 4B OWASP Top 10 testing plan/catalog to clarify that OWASP Top 10 is not annual and that coverage should track every official web-application release edition instead of nonexistent yearly lists. Added release-edition coverage for 2003, 2004, 2007, 2010, 2013, 2017, 2021, and 2025 in `.hermes/plans/owasp-top10-module-collection-and-lab-trial-20260521.md`, `modules/owasp_top10_lab_module_catalog.json`, `modules/OWASP_TOP10_LAB_MODULE_CATALOG.md`, and `handoff/phase4b_owasp_module_collection_direction_20260521.md`. Boundary: planning/catalog only; 2021 remains the currently implemented module taxonomy until reviewed migration, 2025/latest and historical mappings require offline traceability work first, and no additional target-touching execution is authorized by this update.


- 2026-05-21: Added Phase 4B OWASP Top 10 release traceability matrix as an offline/catalog-only slice after pre-session multi-agent review. Created `modules/owasp_top10_release_traceability_matrix.json` and `modules/OWASP_TOP10_RELEASE_TRACEABILITY_MATRIX.md`; linked them from `modules/OWASP_TOP10_LAB_MODULE_CATALOG.md`. The matrix tracks official OWASP Top 10 web-application release editions 2003, 2004, 2007, 2010, 2013, 2017, 2021, and 2025; maps historical/latest categories to the current 2021 project taxonomy with confidence notes; and keeps historical/2025 mapping separate from runtime module authorization. Boundary: offline traceability only; no target interaction, no scanner/Kali bridge execution, no schema/runtime promotion, no report submission, no public/real bug-bounty activation, no exploit/bruteforce/callback/pivot/loot behavior, and no automatic confirmed/verified/reportable/accepted finding promotion.

- 2026-05-21: Added `handoff/phase4b_owasp_2025_migration_review_notes_20260521.md` after the operator clarified that the long-term goal must explicitly include automation: collect/synthesize OWASP Top 10 coverage, test scripts in controlled local lab when approved, and modularize successful checks for future policy-gated invocation. The note classifies OWASP 2025 categories into 2021-alias reuse, checklist-only additions, and blocked/high-risk runtime classes; records that Wave 2 probes remain deferred; and verifies that attack/victim engineering details were not lost by global memory compaction because they remain in repo-local authority files such as `handoff/kali_tool_lab_default.md`, `handoff/phase4a_isolated_aggressive_lab_gate_status_20260521.md`, `handoff/accepted_changes.md`, and Kali helper scripts. Updated `handoff/active_strategy_queue.md` and Obsidian Active Projects to make the next safe slice the offline 2025 alias catalog + script matrix update. Boundary: docs/handoff/navigation only; no target interaction, no scanner/Kali bridge execution, no Juice Shop requests, no schema/runtime/report promotion, no real bug-bounty activation, and no exploit/bruteforce/callback/pivot/destructive/loot behavior.

## 2026-05-21 Phase 4B OWASP local-lab probe run

- Executed a bounded local-lab-only OWASP metadata/benign-parameter probe from `<attacker-vm>` to `<victim-vm>` Juice Shop at `http://<lab-ip>:3000/`.
- Run id: `phase4b_owasp_lab_probe_20260521T051717Z`; local artifacts pulled under `<artifact-output-dir>/phase4b_owasp_lab_probe_20260521T051717Z/`.
- Wrote closeout artifact: `handoff/phase4b_owasp_lab_probe_result_20260521.md`.
- Safety boundary: fixed local target only, 16 requests, 5s timeout, no recursion, no file downloads beyond small response metadata, no credentials, no callbacks/OAST, no exploit payloads, no public targets, no finding/report promotion.
- Health remained OK: pre-health `200`, post-health `200`.
- Observed useful local-lab candidates: `/robots.txt`, `/.well-known/security.txt`, `/ftp/`, `/api-docs/`, product search API, SPA fallback behavior, and a negative open-redirect canary returning `406`.
- Output-side caveat: several `HEAD` probes and one `/ftp/` GET showed curl timeout suffixes; treat as harness/HTTP-method behavior, not vulnerability findings. Future local-lab adapter should prefer GET-only bounded metadata probes with explicit body-size controls.
- Hardened `scripts/kali-pull.ps1` to pass the project-local empty SSH config via `-F`, matching `scripts/kali-run.ps1`, so a BOM-broken user SSH config no longer blocks artifact pulls.

## 2026-05-21 Phase 4B lab fast-lane and GET-only adapter

- Added host-only Juice Shop lab fast-lane policy at `handoff/phase4b_lab_fast_lane_policy_20260521.md`, reducing pre-execution overhead for bounded Tier 1/Tier 2 local-lab probes while preserving hard stops for public targets, callbacks, brute force, scanner broad runs, credentials, destructive behavior, and finding promotion.
- Added reusable GET-only local-lab adapter `scripts/lab_modules/phase4b_get_only_metadata_probe.py` plus TDD coverage in `scripts/test_phase4b_get_only_metadata_probe.py`.
- Executed the adapter via the Kali bridge against `http://<lab-ip>:3000/`; artifacts were pulled to `<artifact-output-dir>/phase4b_fast_lane_20260521T053646Z/`.
- Recorded result in `handoff/phase4b_fast_lane_get_only_result_20260521.md`; health remained `pre_health=200`, `post_health=200`, `requests_sent=8`.
- Notable candidate-only observations: `/ftp/` directory listing metadata, `/api-docs/` Swagger UI metadata, SPA fallback false-positive control, and negative open-redirect canary.

## 2026-05-21 Phase 4B Wave2 benign parameter adapter

- Added reusable Wave2 benign parameter fast-lane adapter `scripts/lab_modules/wave2_benign_params.py` plus TDD coverage in `scripts/test_wave2_benign_params.py`.
- Adapter uses fixed GET-only inert canaries for reflection/open-redirect calibration; it rejects public targets, avoids redirect following, executable payloads, crawlers/scanners, callbacks, credentials, brute force, and finding promotion.
- Executed the adapter via the Kali bridge against host-only Juice Shop; artifacts were pulled to `<artifact-output-dir>/phase4b_wave2_benign_20260521T054852Z/`.
- Recorded result in `handoff/phase4b_wave2_benign_params_result_20260521.md`; health remained `pre_health=200`, `post_health=200`, `requests_sent=5`.
- Candidate-only outcome: no XSS/reflection candidate and no open-redirect candidate for the fixed inert canaries; SPA fallback and redirect error-body echo were captured as false-positive controls.

## 2026-05-21 Phase 4B script-first architecture reset

- Accepted operator correction that the desired workflow is script-first and context-driven: preview/recon results -> choose module bundle -> if no module fits, use script library -> execute a small situational script combination -> review -> modularize useful combinations -> repeat -> report.
- Added `handoff/phase4b_script_first_architecture_reset_20260521.md` as the direction reset.
- Added `scripts/SCRIPT_INVENTORY.md` as the operator-facing map of practical shell scripts, lab adapters, generated local scripts, and current bundle candidates.
- Added `modules/bundles/README.md` and `modules/bundles/lab_directory_listing_triage.md` to shift modules toward reusable context-specific script combinations instead of contract-first manifests as the main workflow.
- Updated `handoff/active_strategy_queue.md` and Obsidian project index to make the next slice `lab_directory_listing_triage` with a bounded `ftp_filename_content_class_verifier.py`, not more generic safety scaffolding.
- Boundary preserved: this direction reset does not authorize public/real bug-bounty target activation, broad scanner runs, credentials/brute force, callback/OAST, destructive behavior, loot/secret collection, confirmed-finding auto-promotion, or report submission.

## 2026-05-21 Restart checkpoint before Hermes reopen

- Added `handoff/restart_checkpoint_20260521_hermes_reopen.md` as the current resume point before restarting Hermes.
- Checkpoint preserves the Phase 4B script-first/context-driven reset, keeps multi-agent collaboration as support/review/implementation, and points the next slice to the `/ftp/` directory-listing triage bundle.
- Added matching Obsidian checkpoint note under `Projects/Cybersec Lab/00_Index/Restart Checkpoint Hermes Reopen 2026-05-21.md`.

## 2026-05-21 Phase 4B module/bundle distinction accepted

- Accepted the operator direction to keep `module` and `bundle` as related but distinct layers: `script/tool -> bundle -> module`.
- Added `handoff/module_bundle_distinction_direction_20260521.md` to record that bundles are the default Phase 4B operator-facing tactical workflow, while modules are platform-facing stable capabilities for schema/policy/runner/report integration.
- Direction: start local-learning work from scripts/tools and lightweight bundles; promote to `modules/checks/**/module.json` only after behavior/output/candidate-only semantics are stable or when authorized-assessment/platform integration requires it.
- Preserved boundary: no public target activation, credential theft, exfiltration, malware, stealth persistence, automatic finding confirmation, or report submission is authorized by this distinction.

## 2026-05-21 Phase 4B service baseline scanner targets

- Added `scripts/lab_modules/lab_service_baseline_targets.py`, a bundle-first local-lab adapter for Apache, Tomcat, OpenSSL/TLS, HAProxy, Envoy, and Traefik baseline probes.
- Added focused TDD coverage in `scripts/test_service_baseline_targets.py`; RED first failed because the adapter did not exist, then GREEN passed after implementation.
- Added bundle documentation at `modules/bundles/lab_service_baseline_targets.md` and updated `scripts/SCRIPT_INVENTORY.md` with the adapter, generated runner path, and bundle workflow.
- Boundary: plan-only by default, rejects public targets, requires `--lab-approved` to generate a runner, uses bounded path/TLS metadata probes, and keeps all output candidate-only with no credential attempts, brute force, secret retention, automatic finding confirmation, or report submission.
- Actual local-lab run added `handoff/service_baseline_targets_run_20260521.md` and artifacts under `<artifact-output-dir>/phase4b_service_baseline_targets_20260521T135015Z/`; final runner exit code was 0 after fixing generated-runner quoting/Python-shim bugs.
- Runtime corrections: quote generated service paths such as `'/;csv'`, use `python -` instead of `python3` for Git-Bash/Windows heredoc compatibility, preserve embedded Python newlines with a raw f-string template, suppress generic SPA/root fallback false positives with body-hash comparison, and avoid treating OpenSSL `Cipher is (NONE)`/no-peer-certificate output as a TLS candidate.
- Final candidate-only run summary: only `/metrics` remained as a Traefik-labeled service-baseline candidate; Apache/Tomcat/HAProxy/default path 200s were reclassified as generic root fallback controls; OpenSSL/TLS on the HTTP port was a control; nmap was unavailable in the Windows Git-Bash runtime.

## 2026-05-21 Phase 4B three OWASP exposure bundles

- Added shared bounded web exposure generator `scripts/lab_modules/web_exposure_common.py` plus three adapters: `lab_api_docs_exposure_triage.py`, `lab_metrics_exposure_triage.py`, and `lab_source_map_disclosure_triage.py`.
- Added focused RED/GREEN coverage in `scripts/test_phase4b_three_exposure_bundles.py`: adapters are plan-only by default, reject public targets fail-closed, record mature OSS tool decisions, generate candidate-only runners, and use `python -` in rendered bash.
- Added bundle docs at `modules/bundles/lab_api_docs_exposure_triage.md`, `modules/bundles/lab_metrics_exposure_triage.md`, and `modules/bundles/lab_source_map_disclosure_triage.md`; updated `scripts/SCRIPT_INVENTORY.md` with adapters and generated runners.
- Mature OSS references recorded: OWASP ZAP, nuclei, ffuf, dirsearch, Prometheus/promtool, Retire.js, SecretFinder, trufflehog, and LinkFinder; decision is wrap/reference mature tools but start with fixed GET-only local-lab probes.
- Actual local-lab artifacts saved under `<artifact-output-dir>/phase4b_three_exposure_bundles_20260521T143412Z/` and summarized in `handoff/phase4b_three_exposure_bundles_run_20260521.md`.
- Candidate-only results: `/api-docs` and `/api-docs/` exposed Swagger UI markers; `/metrics` exposed Prometheus-style metrics markers; source-map disclosure produced no candidate after root-fallback filtering. All require manual verification before finding/report language.

## 2026-05-22 Phase 4B OWASP/CVE continuation bundles

- Completed the delayed Phase 4B local-lab continuation immediately against only `http://<lab-ip>:3000/`; run id `phase4b_owasp_cve_continuation_20260521T232928Z`, artifacts under `<artifact-output-dir>/phase4b_owasp_cve_continuation_20260521T232928Z/`.
- Added bounded plan-first adapters: `lab_auth_surface_no_bruteforce.py`, `lab_component_metadata_triage.py`, and `lab_integrity_metadata_triage.py`; each rejects public targets, is plan-only unless `--lab-approved`, generates candidate-only bash runners, and uses fixed GET-only paths.
- Added bundle docs for auth/no-bruteforce, component metadata, integrity metadata, and API-docs/metrics manual verification/logging checklist. Updated `scripts/SCRIPT_INVENTORY.md` and the OWASP 2017/2021/2025 tracker so A06/A07/A08/A09/2025 supply-chain/auth/logging rows no longer show as entirely unmapped.
- Candidate-only run results: auth surface candidate at `/rest/admin/application-configuration`; component/version clue at `/rest/admin/application-version`; integrity metadata candidates at `/.well-known/security.txt`, `/security.txt`, and `/robots.txt`; package-manifest/service-worker/static paths mostly root/SPA fallback controls; `/api/Users` returned 401 access-control observed.
- Added focused tests by extending `scripts/test_phase4b_three_exposure_bundles.py`; validation passed focused tests, py_compile, `bash -n` for generated runners, all three runner executions, post-health HTTP 200, and `HACKLAB=$(pwd) ./bin/hermes review`.
- Boundary: no public targets, no credential attempts/brute force, no callbacks/OAST, no exploit payloads, no recursion/crawling, no destructive actions, no raw secret/loot retention, no CVE claim from version strings, and no confirmed/reportable finding promotion.



## 2026-05-21 — Verified exploit-flow rerun wave 1

- Scope/target: authorized local lab `http://<lab-ip>:3000/`, covered by `<lab-ip>/16` in `config/scope.txt`; pre/post health HTTP 200.
- Artifacts: `<artifact-output-dir>/verified_flow_wave1_20260521T235533Z/`.
- Added verified-lab-flow bundles: SQLi auth-bypass + `/api/Users` read, unauth admin config read, `/ftp/` listing/file read, API docs + metrics exposure.
- Added attempted-not-verified record for JWT alg-none oracle, coupon route 401, XSS execution not browser-verified, source-map fallback, headers/CORS/component/integrity metadata-only, and missing host tools for ffuf/nikto/nmap/sqlmap.
- Updated script inventory, OWASP tracker, active strategy queue, daily note, and Obsidian-style project note/index.

- 2026-05-25: Fixed the local `bin/hermes` wrapper root-default footgun before live-target intake. `bin/hermes` now defaults `HACKLAB` to the repository that owns the wrapper (`$(dirname ${BASH_SOURCE[0]})/..`) instead of `$HOME/projects/cybersec`, while preserving explicit `HACKLAB=...` overrides. Validation: `bash -n bin/hermes` and bare `./bin/hermes doctor` now report project root `<private-workspace>` with `.hermes.md` and `handoff/` found. Boundary: wrapper path-resolution fix only; no target, scope, credential, VM/network, scanner, exploit, scheduler, or worker-task activation.

- 2026-05-25: Converted the public <program-redacted> Taiwan <bug-bounty-platform> policy/scope into a blocked Phase 5A scope package without touching the target. Added `programs/<program-slug>/scope.json` with the 47 observed in-scope eligible assets, policy exclusions, conservative manual-only rate defaults, and required `X-<bug-bounty-platform>-Researcher: <researcher-alias>` session header. Added `handoff/coupang_tw_phase5a_dry_run_packet_20260525.md` selecting an account-owned access-control/IDOR first lane and documenting the account-creation decision gate. Boundary: no live request, signup, login, scan, fuzz, exploit, callback/OAST, upload, payment/KYC/checkout/cash action, report submission, or `config/scope.txt` authorization change. Next gate: operator must choose whether to create two owned test accounts or explicitly authorize Hermes to attempt official signup with operator-handled CAPTCHA/OTP/email/phone verification.

- 2026-05-25: Recorded operator authorization for Hermes to attempt creating two <program-redacted> Taiwan throwaway accounts through the official in-scope signup flow, with the operator handling CAPTCHA/email/OTP/phone verification. Added the minimal signup/auth assets to `config/scope.txt`: `www.tw.<program-redacted>.com`, `tw.<program-redacted>.com`, `member.tw.<program-redacted>.com`, and `id.tw.<program-redacted>.com`; updated `handoff/coupang_tw_phase5a_dry_run_packet_20260525.md` status and checklist. Boundary: authorization is limited to official signup/account creation and later low-speed manual/browser-assisted owned-account checks; no scan, fuzz, exploit, callback/OAST, payment/KYC/checkout/cash flow, upload test, report submission, or non-owned account/data interaction. Remaining blocker: operator must provide two owned email aliases/addresses for Account A/B before Hermes starts signup.

- 2026-05-25: Added and verified `scripts/kali-vnc-control.ps1` for a safer VM GUI-control route to `<attacker-vm>`: Kali-side x11vnc/noVNC services bind to Kali localhost only (`127.0.0.1:5901` and `127.0.0.1:6080`), Windows access uses an SSH local tunnel at `http://127.0.0.1:6080/vnc.html?autoconnect=1&resize=scale`, and `start/status/stop` actions manage service/tunnel state. After operator-installed packages, verification showed `x11vnc=running`, `websockify=running`, `local_tunnel=running`, and local noVNC HTTP 200. Boundary: local VM control-plane setup only; no target interaction, no credential/OTP handling, no proxy/Tor enablement, no scanning/fuzzing/exploitation, and no public report/finding promotion.

- 2026-05-25: Started the <program-redacted> Taiwan single-account surface-map lane from the logged-in Kali VM browser using the authorized live-target workflow skill. Created `handoff/coupang_tw_single_account_surface_map_20260525.md` and saved initial redacted screenshots under `setting/local/screenshots/coupang_surface_20260525/`. Verified a logged-in home state, then stopped when the Kali desktop lock screen appeared during the first GUI-control attempt; no post-login target navigation, scanning, fuzzing, exploit, cross-account test, payment/checkout/KYC/upload/seller/admin action, report submission, or credential/OTP/token storage occurred. Hermes attempted non-sensitive lock-prevention settings; operator must unlock the VM locally before the lane resumes.

- 2026-05-26: Closed the <program-name>/<program-redacted> third-target pre-contact readiness checkpoint without making a new live-target request. Added `handoff/program-redacted_pre_contact_ready_checkpoint_20260526.md` and `handoff/dirty_tree_checkpoint_audit_20260526.md`; updated `handoff/current_navigation.md`, `handoff/active_strategy_queue.md`, `handoff/current_artifact_index.md`, and `handoff/third_target_contact_checkpoint_20260526.json` so the current state is `READY_FOR_OPERATOR_GATE` / `A2_PENDING_OPERATOR_AUTH`. Runtime readiness verified Kali VM/SSH/noVNC local tunnel availability, with Xvfb/Xfce fallback documented because physical `:0` was at LightDM/Xauthority. Boundary: no <program-name> page was opened in this readiness pass; no signup submit, phone/password/OTP/CAPTCHA/email verification, cookie/token/API-key handling, scanner/fuzzer/DAST/exploit/callback/OAST/tunnel, customer/non-owned data access, integration/invite, billing/KYC, report generation, or report submission occurred. Next operator gate: open local noVNC and complete/stop the signup/auth gate locally, then reply with a non-sensitive status such as `front_signup_complete`, `blocked_phone`, `blocked_email_verification`, `blocked_captcha`, `blocked_policy`, or `stop`.


## 2026-05-26 — <program-name> authorized attacker-flow skill/practical checkpoint

- Created user-local Hermes skill `authorized-attacker-flow` for authorized attacker-perspective workflow: scope gate, proof boundary, proof surrogate, stop-before, evidence packet, candidate preservation.
- Ran first practical <program-name> pass after operator reported first owned account creation complete.
- Evidence captured under `setting/local/screenshots/program-redacted_live_20260526/`; latest: `dashboard_channel_connect_gate.png`.
- Wrote attacker-flow packet: `handoff/program-redacted_authorized_attacker_flow_packet_20260526.md`.
- Updated `programs/<program-redacted>/lane_state.json` and `handoff/third_target_contact_checkpoint_20260526.json` from signup gate to post-auth passive mapping paused at external shared-channel connection gate.
- Safety: no scanner/fuzzer/DAST, no exploit, no API/token capture, no invite sent, no channel connected, no customer/non-owned data touched, no report submitted.

## 2026-05-26 — Authorized attacker-flow multi-agent hard rule

- Patched user-local skill `authorized-attacker-flow` so non-trivial attacker-flow lanes must actually invoke suitable external/independent workers when available, instead of satisfying role separation by having Hermes or one model read an MD file.
- Added skill reference `references/multi-agent-memory-sync-rule.md` covering Claude Code/Cowork + Codex routing, memory-sync packet requirements, worker artifact requirements, verification, and failure handling.
- Added repo workflow artifact `handoff/multi_agent_tactical_review_memory_sync_rule_20260526.md` and synced the rule into `handoff/active_strategy_queue.md`, `handoff/current_artifact_index.md`, and `notes/obsidian_projects/Cybersec Lab.md`.
- Safety: workflow/memory update only; no target touched, no browser action, no scanner/fuzzer/DAST, no exploit, no credentials/tokens, no customer/non-owned data, no report submission.


## 2026-05-26 — Multi-agent hard rule reviewed by Claude Code and Codex

- Actually invoked non-Hermes review routes for the workflow rule instead of only reading/writing MD from Hermes:
  - Claude Code CLI output: `handoff/claude_review_multi_agent_tactical_review_memory_sync_20260526.json`
  - Codex CLI output: `handoff/codex_review_multi_agent_tactical_review_memory_sync_20260526.md`
- Both reviewers returned `REQUEST_CHANGES`; incorporated the main fixes into `handoff/multi_agent_tactical_review_memory_sync_rule_20260526.md` and user-local skill `authorized-attacker-flow`: mandatory Claude/Cowork + Codex pair for non-trivial lanes, explicit `non-trivial` trigger, invocation evidence requirement, shape-only-attestation warning, and structured skipped-route/passive-only handling.
- Safety: read-only local workflow review and documentation/skill update only; no target touched, no browser/VM/account action, no scanner/fuzzer/DAST, no exploit, no token/credential handling, no customer/non-owned data, no report submission.

## 2026-05-26 — <program-name> candidate downgraded pending actual multi-agent review

- Incorporated stricter Claude/Codex review feedback after the workflow-rule review: reconciled role labels to canonical `config/worker_roles.txt` vocabulary, backfilled invocation evidence for the rule-review runs, fixed rule numbering, and made the <program-name> implication explicit.
- Downgraded `<program-name>-shared-inbox-object-permission` in `handoff/program-redacted_authorized_attacker_flow_packet_20260526.md` from `bounded_executable` to `needs_multi_agent_review` until a <program-name>-specific Claude/Cowork + Codex packet exists.
- Added `worker_route_status` to `programs/<program-redacted>/lane_state.json` recording that the workflow rule was reviewed, but <program-name> proof-specific routes remain pending and the lane is passive-only until complete.
- Safety: local documentation/JSON state update only; no target touched, no browser/VM/account action, no scanner/fuzzer/DAST, no exploit, no token/credential handling, no customer/non-owned data, no report submission.

## 2026-05-26 — <program-name>-specific multi-agent workflow practical test

- Ran the new role-separated workflow against the <program-name> `<program-name>-shared-inbox-object-permission` candidate without touching the target.
- Actual worker routes invoked:
  - Claude Code tactical/boundary/evidence review: `handoff/claude_program-redacted_tactical_boundary_evidence_review_20260526.json` (normalized valid JSON; REQUEST_CHANGES; raw preserved at `handoff/claude_program-redacted_tactical_boundary_evidence_review_20260526.raw.txt`; wrapper hit max-turns but produced usable role-specific findings).
  - Codex deterministic review: `handoff/codex_program-redacted_deterministic_review_20260526.md` (BLOCK).
- Added Hermes synthesis: `handoff/program-redacted_multi_agent_review_synthesis_20260526.md`.
- Updated `programs/<program-redacted>/lane_state.json` `worker_route_status` to record <program-name>-specific route results and keep the lane passive-only / blocked beyond passive mapping.
- Safety: local read-only review and JSON/handoff update only; no target touched, no browser/VM/account action, no scanner/fuzzer/DAST, no exploit, no token/credential handling, no customer/non-owned data, no report submission.

## 2026-05-26 — <program-name> BLOCK issue reduction and bundle preservation

- Resolved the non-target-touching parts of the <program-name> multi-agent BLOCK: tightened `<program-name>-shared-inbox-object-permission` so object creation is no longer an allowed state change and is instead an explicit operator-gated future action.
- Added passive docs/object-boundary matrix and no-exclusion bundle preservation artifact: `handoff/program-redacted_passive_docs_bundle_map_20260526.md`.
- Updated `programs/<program-redacted>/lane_state.json` to `passive_mapping_extended_not_proof_ready` / `block_reduced_passive_mapping_allowed_not_proof_ready`; proof remains blocked.
- Preserved all useful bundle families: inbox membership, non-public group linking, conversation cross-listing, message/comment/draft splits, workflow/rule routing, channel/OAuth, API/UI mismatch, plugin context, archive/delete residue, metadata-only leaks.
- Safety: public docs/reference review and local handoff/JSON edits only; no app/browser/account action, no object creation, no invite/role mutation, no token/API call, no workflow activation, no channel connection, no customer/non-owned data, no scanner/fuzzer/DAST, no report submission.
- 2026-05-26: Continued <program-name> (`<program-redacted>`) within the existing passive-only BLOCK-reduced lane. Verified noVNC tunnel reachability and captured non-sensitive Kali workspace screenshots under `setting/local/screenshots/program-redacted_live_20260526/`; current visible workspaces showed desktop/no visible <program-name> browser, so no further live app interaction was performed. Public `dev.frontapp.com/llms.txt` inventory expanded preserved bundles for channels, contact/account metadata, tags, conversation/comment/draft surfaces, and rule/workflow triggers. Updated `programs/<program-redacted>/lane_state.json` and `handoff/program-redacted_passive_docs_bundle_map_20260526.md`; no object creation, invite, role change, token/API call, channel/OAuth connection, workflow activation, customer/non-owned data, scanner, or report submission.
- 2026-05-26: Corrected the <program-name> (`<program-redacted>`) workflow after operator feedback: Claude/Codex reviewers are tactical/adversarial/evidence advisors, not extra blocking safety gates. Updated `programs/<program-redacted>/lane_state.json`, added `handoff/program-redacted_reviewer_advisory_correction_20260526.md`, and patched the user-local `authorized-attacker-flow` skill accordingly. Future <program-name> execution blockers must be concrete scope/operator/safety constraints (owned accounts/objects, token/channel/workflow approval, non-owned data, destructive/strong techniques, report approval), not reviewer veto. Boundary: workflow/state documentation only; no target interaction, no scanner/fuzzer/DAST, no object creation, no invite/role change, no token/API call, no channel/OAuth connection, no workflow activation, no customer/non-owned data, no report submission.
- 2026-05-26: Prepared <program-name> (`<program-redacted>`) Account B signup in a separate Kali Chromium profile/window up to the operator phone/submit gate using only non-secret operator-approved metadata. Saved local screenshot evidence under `setting/local/screenshots/program-redacted_live_20260526/second_signup_ready_for_operator.png`; did not submit the form, enter phone, handle CAPTCHA/email/OTP/password, or store secrets. Added `handoff/program-redacted_safe_flow_research_20260526.md` with public <program-name>/SaaS bug-flow research and safe owned-data translations. Updated `programs/<program-redacted>/lane_state.json` to make the current blocker concrete: Account B operator completion, not reviewer veto. Boundary: no scanner/fuzzer/DAST, no exploit, no API token/API call, no channel/OAuth/webhook connection, no workflow activation, no invite/role change, no message/discussion send, no customer/non-owned data, no report submission.

## 2026-05-27 — <program-name> first-bounty target selection and permission-control run card

- Ran two Hermes delegate_task external advisory reviews over the current H1/live queue and <program-name>/<program-redacted>/<program-redacted> artifacts without target contact. Both selected <program-name> as Rank #1 for first-bounty continuation, with <program-redacted> parked on second-account gate and <program-redacted> kept no-finding/closed.
- Created `programs/<program-redacted>/notes/program-redacted_first_bounty_run_card_permission_controls_20260527.md` selecting `removed-downgraded-stale-access` / `auth-role-separation` for owned A/B permission-control precondition checks.
- Updated `programs/<program-redacted>/lane_state.json`, `handoff/live_bounty_lane_queue.json`, `handoff/current_navigation.md`, and `handoff/active_strategy_queue.md` to reflect <program-name> `ready_precondition_check`, Account C line parked, and the remaining concrete blocker: no clean low-priv/removed/downgraded negative control yet.
- Safety: local file review and planning artifacts only; no live target/browser/account action in this step, no scanner/fuzzer/DAST, no token/API call, no channel/OAuth/webhook/callback/tunnel, no customer/non-owned data, no secrets, no report submission.

## 2026-05-27 — <program-name> permission-control precondition check parked

- Per operator handoff that <bug-bounty-platform> was logged in, confirmed <program-name> H1 scope via browser/noVNC and then performed only low-speed manual <program-name> settings/permissions precondition checks inside the existing owned account session.
- Evidence/checkpoint: `programs/<program-redacted>/notes/program-redacted_permission_precondition_check_20260527.md` with local screenshots under `setting/local/screenshots/program-redacted_live_20260527/`.
- Decision: PARK. H1 scope and <program-name> settings/permissions surfaces are confirmed, but no clean low-priv/removed/downgraded/resource-excluded negative control exists; current visible permissions are group/plan constrained, so a reportable access-control proof cannot be claimed.
- Updated `programs/<program-redacted>/lane_state.json`, `handoff/live_bounty_lane_queue.json`, `handoff/current_navigation.md`, and `handoff/active_strategy_queue.md` to park <program-name> and prefer switching target unless a clean owned negative control becomes available.
- Safety: no scanner/fuzzer/DAST, no token/API call, no channel/OAuth/webhook/callback/tunnel, no customer/non-owned data, no secrets/OTP/cookies/API keys/phone stored, no destructive action, no report submission.

## 2026-05-27 — H1 next-target quick triage after <program-name> park

- Used logged-in <bug-bounty-platform> Opportunity Discovery / program pages only to inspect public/H1-visible metadata after <program-name> was parked.
- Created `handoff/h1_next_target_quick_triage_after_front_park_20260527.md`.
- ALSCO was opened as a candidate due low visible asset count and high response efficiency, but parked because its visible scope/instructions imply mobile app/one-time-code/test-account coordination and upload/auth-bypass setup, which is higher operator cost than ideal for the immediate first-bounty sprint.
- Safety: H1/program-page metadata only; no ALSCO target app contact, no scanner/fuzzer/DAST, no token/API call, no customer/non-owned data, no secrets/OTP/cookies/API keys/phone stored, no report submission.

## 2026-05-27 — H1 freshness-first shortlist and <program-slug> decision gate

- Continued from operator correction that latest/new/updated targets should be prioritized and the operator should be used as a human plugin/reviewer while Hermes remains project owner.
- Created `handoff/h1_freshness_first_target_shortlist_20260527.md`.
- Freshness-first triage result: <program-slug> private opportunity ranks #1 but requires operator decision before clicking `Claim your spot` because H1 shows it as an available private opportunity with up to 1 per 30 days; Banco Plata parked due Mexico phone gate; Discourse killed for first-bounty due temporary bounty suspension; campaign fallback ranking starts with <program-redacted> if <program-slug> is declined.
- Safety: H1/program-page and minimal landing-page/precondition checks only; no scanner/fuzzer/DAST, no token/API call, no customer/non-owned data, no secrets/OTP/cookies/API keys/phone stored, no report submission.

## 2026-05-27 — <program-slug> claimed and passive precondition review

- Operator approved claiming <program-slug> and clarified that future H1 private-opportunity claims can be done directly when the only visible cost is slot/cadence and there is no account-risk warning; memory updated with that rule.
- Opened `https://<bug-bounty-platform>.com/syfe_bbp`, captured scope/policy, and created `programs/<program-slug>/run_card.md`, `programs/<program-slug>/scope.json`, `programs/<program-slug>/lane_state.json`, `programs/<program-slug>/evidence_index.json`, and `programs/<program-slug>/candidate_packet_20260527.md`.
- Passive precondition results: production and UAT signup pages both expose email/password/social signup and no form was submitted; UAT frontend static bundle points to `api-uat-bugbounty.nonprod.<program-slug>.com` and references signup/MFA/email OTP flows; tiny fixed-path API root checks showed UAT API 404 JSON, mark8 401 JSON, and production API Cloudflare 403.
- Decision: <program-slug> remains worth pursuing, but current state is `NEEDS_OPERATOR` because owned account/control setup is required before any object/API bundle can run. Preferred next bundle after auth is `object-ownership-idor`, fallback `api-ui-permission-mismatch`.
- Safety: H1-visible metadata, signup field mapping, passive static frontend review, and three fixed-path root checks only; no signup submission, no scanner/fuzzer/DAST, no endpoint brute force, no token/cookie/API-key/OTP/password/phone storage, no customer/non-owned data, no report submission.

## 2026-05-27 — Kali/noVNC secret fill bridge

- Operator requested stable automatic filling for routine bug-bounty signup email/password/phone while keeping OTP/CAPTCHA/final submit human-gated.
- Mirrored existing Hermes profile secrets into ignored `setting/local/hacklab.secrets.env` for Kali/noVNC use; verified the file is covered by `.gitignore` via `setting/local/`.
- Added `scripts/fill_signup_secret.py` to read ignored env secrets and fill focused browser fields via Kali clipboard/xdotool, with optional Playwright CDP mode for future remote-debug Chromium usage. The script reports only the secret kind/method and never prints secret values.
- Added `scripts/kali-fill-secret.ps1` wrapper to invoke the helper through existing `scripts/kali-run.ps1` on the Kali VM. Dry-run for `password` succeeded from the Windows control plane without printing the value.
- Started Kali/noVNC, opened <program-slug> signup in a dedicated Chromium profile with CDP enabled, successfully filled <program-slug> email and primary password via the CDP secret bridge, and clicked SIGN UP. The page then exposed a reCAPTCHA/`Please verify` gate, so execution stopped for operator CAPTCHA handling.
- Added `programs/<program-slug>/secret_fill_bridge_20260527.md` with usage and safety notes.
- Safety: local secret mirror is ignored, no secret values were written to tracked artifacts, no OTP/CAPTCHA capture or bypass, no customer/non-owned data, no report submission.
## 2026-05-28 — hermes-as-primary restructure + cleanup

- Reviewed <program-redacted> Public Bug Bounty (Intigriti) policy via Playwright MCP. Operator logged in to Intigriti; driver navigated to program detail, snapshotted accessibility tree, parsed scope/bounty/rules. Created `programs/<program-slug>/` with scope.json, lane_state.json, policy_snapshot. Lane parked at A0_PROGRAM_SUSPENDED because program is currently Suspended at Intigriti (5/19); park_expires_at 2026-06-11. Registered in `handoff/live_bounty_lane_queue.json` priority 3. `handoff/pending_intake.json` emptied (<program-redacted> was sole candidate; decision EXECUTE_POLICY_REVIEW completed).
- Restructured project to hermes-as-primary mode. New canon files at root: `INDEX.md` (project map, ownership, drift rules), `CLAUDE.md` (force-read for Claude Code sessions), `.hermes.md` (Hermes system context). New `hermes/` runtime structure with policies/ (autonomous_actions, consult_claude, consult_codex, operator_gates, stop_conditions), loops/ (daily_sweep, hourly_diff, minute_alerts, on_trigger), state/, digests/, calls/, proposals/. SAFETY.md authority order updated; repo discipline references INDEX.md.
- Archived to `archive/cleanup_hermes_primary_20260528/`: `handoff/{INDEX,active_strategy_queue,current_artifact_index}.md` (A1-A3, subsumed by new canon; CVE detail + 30-day metric folded into current_navigation.md); `programs/_schema/` + `programs/_examples/` + `scripts/validate_program_scope.py` + tests (B1-B3, schema never enforced, deviated from real practice); `scripts/tmp_*.py` <program-redacted>/CDP orphan scripts (C, not listed in inventory).
- Gitignored `.playwright-mcp/` runtime cache (browser snapshots, console logs).
- Safety: no destructive ops on lane state / scope / evidence / governance / loot / logs; all cleanup used `git mv` to archive; no `git reset --hard` / `rm -rf` on tracked content. Authorization unchanged: `config/scope.txt` untouched, no live action authorized beyond what was already in place.

## 2026-05-28 — Local lab proof: LiquidJS <specific-cve-id> marker-only RCE surrogate

- Verified latest advisory candidate `<specific-ghsa-id>` / `<specific-cve-id>` in `<victim-vm>` only.
- Boundary: local artifact directory marker write only; no live target, no scanner/fuzzer/DAST, no callback/OAST, no secrets, no destructive action.
- Evidence: `<artifact-output-dir>/liquidjs_rce_cve_2026_45618_20260528T101739Z/summary.json`.
- Proof packet: `labs/proofs/liquidjs_rce_cve_2026_45618_marker_proof_20260528.md`.
- Bundle: `modules/bundles/verified_lab_flow_liquidjs_cve_2026_45618_marker_rce.md`.
- Result: vulnerable `liquidjs@10.25.7` wrote the synthetic marker; patched `10.26.0` did not.

## 2026-05-28 — Structure/index hygiene after LiquidJS proof

- Added `.gitignore` rules for machine-local `.claude/` and transient `hermes/state/latest_*_package_cache.txt` pointers.
- Expanded `INDEX.md` canonical directory map to classify existing tracked legacy zones (`cves/`, `schemas/`, `fixtures/`, `tools/`, `skills/`, `templates/`, `wordlists/`, `public_exports/`, `pictures/`, `targets/`, `setting/`, runtime/cache dirs) instead of leaving them implicit.
- Added anti-sprawl rule: new top-level paths require INDEX-first classification, placement under an existing governed zone, or explicit ignored-runtime classification.
- Clarified drift scans should exclude archived historical debt unless deliberately auditing archive contents.
- Boundary: repo-governance/index hygiene only; no target contact, scope broadening, report promotion, or destructive cleanup.

## 2026-05-28 — Memory-flow Obsidian bridge refreshed for hacking2

- Audited memory routing after the Hermes-as-primary restructure. Profile/global memory remained compact signposts and repo handoff/INDEX/navigation were active, but the live Obsidian Cybersec Lab namespace still pointed at legacy `<private-workspace>` and old Phase 4B wording.
- Updated Obsidian `00_Index/Hermes Memory Routing.md` to point to `<private-workspace>`, current active truth files, live lane queue, and the legacy-reference-only boundary for old `hacking`.
- Updated Obsidian `00_Index/Active Projects.md` to mark `hacking2` as primary, old `hacking` as reference-only, and the current north star as first report-ready draft/submission candidate by 2026-06-27.
- Boundary: memory/governance routing only; no target contact, no secrets, no scope change, no report promotion.

## 2026-05-28 — Three latest npm advisories verified in Kali victim lab

- Selected three fresh, locally-verifiable GitHub Advisory npm candidates from the 2026-05-27 feed: LiquidJS `<specific-ghsa-id>` / `<specific-cve-id>`, @hapi/content `<specific-ghsa-id>` / `<specific-cve-id>`, and tmp `<specific-ghsa-id>` / `<specific-cve-id>`.
- Built vulnerable/patched dependency environments on the Windows control plane, transferred them to `<victim-vm>` over host-only SSH, and ran local-only positive + patched/negative controls.
- Evidence root: `<artifact-output-dir>/three_latest_npm_vulns_20260528T110251Z/evidence/` with `summary.json`, per-vulnerability proof JSON, and `posture.txt`.
- Proof packets: `labs/proofs/liquidjs_redos_cve_2026_45617_strip_html_20260528.md`, `labs/proofs/hapi_content_cve_2026_44974_parameter_smuggling_20260528.md`, and `labs/proofs/tmp_cve_2026_44705_path_traversal_patched_control_20260528.md`.
- Bundles: `modules/bundles/verified_lab_flow_liquidjs_cve_2026_45617_strip_html_redos.md`, `modules/bundles/verified_lab_flow_hapi_content_cve_2026_44974_parameter_smuggling.md`, and updated `modules/bundles/verified_lab_flow_tmp_path_traversal_arbitrary_file_creation.md` with the patched-control re-verification.
- Boundary: local lab only; no live target, no scanner/fuzzer/DAST, no callbacks/OAST, no secrets, no report promotion. The batch maximized recoverable local impact via CPU stall, parser-smuggling primitive, and synthetic directory-escape marker without uncontrolled VM damage.

## 2026-05-29 — <program-redacted> no-finding park + H1/noVNC target shortlist

- Operator said to continue after <program-redacted> Account B role-lifecycle/webhook boundary reached a no-finding checkpoint; updated `programs/<program-slug>/lane_state.json` to `no_finding`/PARK and removed the lane from `handoff/live_bounty_lane_queue.json` while preserving resume context.
- Used the existing logged-in Kali/noVNC <bug-bounty-platform> Opportunity Discovery session and CDP visible-text extraction for passive target intake only.
- Scored a new first-bounty shortlist in `handoff/pending_intake.json`: <program-redacted>, Inc. is the recommended next lane; <program-redacted> Taiwan remains a strong marketplace fallback behind scope broadening; <program-slug> remains a low-gate local/source fallback; Meesho/Whatnot/Banco Plata/CLEAR are parked due friction or gate risk.
- Updated `handoff/current_navigation.md` so the highest-priority operator action is the <program-redacted> exact-host scope/lane approval gate.
- Boundary: logged-in platform policy/scope reading and repo handoff updates only; no live target testing, scanning, fuzzing, exploitation, OAST/callback, account mutation, credential/token handling, customer/non-owned data access, report promotion, or submission.
- Validation: `bash tests/test_current_live_bounty_contracts.sh` PASS; `bash tests/test_operator_inbox_summary.sh` PASS.

## 2026-05-29 — <program-redacted> dry-run gate fixed and login gate reached

- Restored `scripts/validate_program_scope.py` from the archived validator dependency required by `scripts/core/policy.py` and adjusted slug validation to accept repo-standard underscore slugs such as `<program-slug>`.
- Converted `programs/<program-slug>/scope.json` into the enforced program-policy schema; preserved passive intake details in `programs/<program-slug>/notes/policy_intake_20260529.json`.
- Verified <program-redacted> dry-run gate: all three exact in-scope hosts passed `safe_target` and program-policy `http_probe`; out-of-scope `dashboard.<program-redacted>.com` failed closed; no policy helper JSON/import errors remained.
- Navigated in Kali/noVNC to the exact <program-redacted> dashboard host and stopped at `/sign_in` email-address operator auth gate. Evidence note: `programs/<program-slug>/notes/2026-05-29_dashboard_login_gate.md`.
- Boundary: no scanner/fuzzer/DAST, no API token/webhook/integration/callback/OAST action, no secrets stored, no customer/non-owned data, no report submission.
- Validation: `python -m unittest scripts.test_program_policy_check scripts.test_program_policy_boundary -v` PASS; `bash tests/test_current_live_bounty_contracts.sh` PASS; `bash tests/test_operator_inbox_summary.sh` PASS.

- 2026-05-29: Parked <program-redacted> after exact-host first contact and env-backed owned-account login attempts showed no usable account acquisition/verification-send path. Moved active queue to <program-slug> passive policy/scope intake only; live target contact remains blocked until exact assets/rules and operator scope approval are established.
- 2026-05-29: Installed and ran a realistic SaaS/API local-lab target on `<victim-vm>` (`modern-realistic-api` at `<victim-vm>:18080`) and verified a multi-class high-value bug-bounty tactic batch with synthetic markers only: BOLA/IDOR, role authorization, path traversal, SSRF loopback surrogate, XXE, unsafe deserialization, upload retrieval, plus XSS sink candidate. Added proof packet `labs/proofs/realistic_saas_api_multi_class_kali_victim_20260529.md`, bundle `modules/bundles/verified_lab_flow_realistic_saas_api_multi_class.md`, note `notes/cybersec_lab_realistic_saas_api_tactics_20260529.md`, and artifact root `<artifact-output-dir>/realistic_saas_api_20260529T063059Z/`. Boundary: local lab only, no live target authorization or report-ready promotion.

## 2026-05-29 — Kali/noVNC target-search intake pipeline

- Added `scripts/kali-target-search.ps1` to run the passive Kali browser capture sequence in one command: open URL, wait, extract CDP visible text, screenshot, downloads metadata, sanitize visible text, and write a manifest under `<artifact-output-dir>/browser_state/<profile>_<timestamp>/`.
- Added `scripts/normalize_platform_policy.py` to convert passive platform-policy/CDP text or existing policy-intake JSON into candidate-only normalized scope/rule/gate/class JSON.
- Added `scripts/build_target_candidates.py` to convert normalized policy plus optional public-doc/disclosed-report snippets into scored `handoff/pending_intake.json` candidates while preserving the no-live-testing boundary.
- Added `scripts/test_target_search_pipeline.py` covering normalization, candidate scoring/merge behavior, and wrapper pipeline contract.
- Generated `programs/<program-slug>/notes/policy_normalized_20260529.json` from the existing <program-slug> passive intake and refreshed <program-slug>'s pending-intake candidate as candidate-only passive intake.
- Boundary: tooling and passive/offline normalization only; no live target contact, scan/fuzz/DAST, exploit attempt, account mutation, credential/token handling, scope authorization, report promotion, or submission.


## 2026-05-29T09:17:14Z — <program-slug> scope packet and browser-control engineering closeout

- Committed the current engineering slice for <program-slug> exact-scope approval artifacts, operator inbox rendering, candidate-promotion drafts, and reviewed Kali/noVNC CDP browser actions.
- Updated `handoff/current_navigation.md` to close the stale passive-intake routing state: <program-slug> is now the active lane, blocked at `A2_SIGNUP_PASSWORD_GATE`; scheduler remains inactive until operator enablement, but the manual inbox consumer path is proved.
- Validation before commit: focused py_compile + 17 focused unit tests passed; `./bin/hermes doctor` passed; `git diff --check` passed. Full `python -m unittest discover scripts` still has unrelated pre-existing fixture/golden-file failures around `programs/_examples/sample-lab/scope.json`, recon CLI slug ordering, and security-header triage README.
- Boundary: no proof testing, scanners/fuzzers/DAST, OAuth/API-token/webhook/integration creation, customer/non-owned data access, report submission, scheduler activation, or new live authorization beyond the operator-approved exact <program-slug> scope and owned-account surface-mapping gate.

## 2026-05-29T10:36:51Z — Active recon safety posture broadened

- Operator approved shifting exact-scope low-speed active recon into the default tactical base, with A3 API-token/owned-object proof actions as lane-approved routine capabilities and A4 SSRF/OAST/exploit-chain proof as controlled per-lane capabilities.
- Updated `SAFETY.md` to allow bounded low-speed recon by default and to permit API token creation, SSRF/OAST marker callbacks, and exploit-chain steps only through per-program/lane-approved capability records.
- Updated `hermes/loops/hourly_diff.md` and `config/recon.conf` so live bounty defaults are `live-low-speed`, rate-limited, technique-gated, and explicit that hourly diff queues rather than silently executes A3/A4 proof actions.
- Updated `scripts/validate_program_scope.py` and `scripts/core/policy.py` so A3/A4 techniques require matching automation profiles and A4 capability records; `exploit_chain_bounded_owned_impact` is allowed only with owned/recoverable impact, stop-before controls, and cleanup.
- Boundary: policy/validator/recon-profile engineering only; no live target contact, token creation, SSRF/OAST callback, exploit execution, scope broadening, scheduler activation, report promotion, or submission.

## 2026-05-29T11:08:13Z — Former A4 collapsed into A3 controlled capabilities

- Operator directed that A4 should be folded into A3 to reduce live-bounty proof friction.
- Updated safety/policy docs so A3 is the single standing lane-approved proof tier; former A4 SSRF/OAST and exploit-chain actions are controlled A3 capabilities rather than a separate autonomy tier.
- Updated runtime validator/policy code so former A4 techniques use `A3_BOUNDED_PROOF_ACTIONS` plus `controlled_capabilities` records; legacy `a4_capabilities` / `A4_CONTROLLED_HIGH_RISK_PROOF` remain accepted only as compatibility aliases with deprecation warnings.
- Boundary remains unchanged for hard stops: no customer/non-owned data, secret/credential extraction, uncontrolled internal enumeration, persistence, destructive impact outside recoverable owned state, high-volume automation, or final submission without explicit approval.

## 2026-05-29T12:00:14Z — Disclosed-report mining offline foundation

- Added `schemas/disclosed_report.schema.json` plus `scripts/ingest_disclosed_reports.py` to normalize offline public disclosed-report text into sanitized, target-touching-false records.
- Added `scripts/score_disclosed_report_patterns.py` to aggregate sanitized records into candidate primitive/class/surface/proof pattern weights for lane scoring and local-lab bundle design.
- Added `fixtures/disclosed_reports/sample_public_reports.jsonl` and `scripts/test_disclosed_report_mining.py` covering schema boundary, sanitizer/redaction, login/CAPTCHA/private-source fail-closed behavior, CLI batch output, pattern scoring, and non-promotional vocabulary.
- Updated `scripts/INDEX.md`, `handoff/current_navigation.md`, and the Cybersec Lab Obsidian methodology note to reflect the first offline/TDD slice.
- Boundary: file-input passive intelligence only; no URL fetching, login/session use, live target contact, scan/fuzz/DAST, callback/OAST creation, customer/non-owned data access, private-content ingestion, finding/report promotion, or submission.
- Validation: `python -m unittest scripts.test_disclosed_report_mining scripts.test_target_search_pipeline -v` PASS.
- Independent review route: Hermes delegate_task leaf subagent, model reported by tool as `gpt-5.5`; initial verdict failed on private-gated body retention. Fixed by adding a RED regression and replacing private/login-gated summaries with `[private_or_login_gated_content_not_ingested]`; focused and adjacent tests PASS.
