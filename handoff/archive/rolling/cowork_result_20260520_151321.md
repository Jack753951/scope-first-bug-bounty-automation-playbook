> Public sanitized edition: sensitive personal, target, path, IP, alias, advisory, and run-specific evidence fields are redacted. This repository does not authorize live testing.

Wrote the design-only direction review to `handoff/cowork_soc_reviewer_gap_catalog_direction_review.md`.

**Verdict:** `PROCEED_CATALOG_ONLY` / `APPROVE_WITH_CHANGES` at **T2**, Hermes **direct** authority.

**Key narrowings vs. Option 1 as written:**
- Catalog lives under `fixtures/soc_evidence_bucket/` (next to the P3.11 fixture), **not** under `templates/`, to avoid look-alike with the consumer-loaded `templates/report_readiness_reviewer_prompts.json`.
- Two artifacts: Markdown explanation + small JSON catalog (Markdown-only loses the testable vocabulary lock; JSON-only loses the human reviewer affordance).
- Cross-file **byte-equal vocabulary lock** with the P3.11 fixture's 12 gap codes and 5 non-promotional statuses — drift fails closed.
- Flat trial marker `soc_reviewer_gap_catalog_v0_trial`, mirroring the P3.5 discipline; no slash-shaped `*/0.1-trial` schema posture.
- No Python constants module, no chain consumer wiring, no reviewer-answer capture (still deferred).

**Other answers from the prompt's required-analysis list:**
- **OSS Recon Gate:** informational only (T2), with §3.2 recording catalog-specific adopt/adapt/ignore deltas on top of the P3.11 baseline so a future T3 contract review doesn't restart from zero.
- **Risks:** vocabulary promotion, schema creep, report-readiness creep, runtime coupling — each mitigated explicitly in §3.3.
- **Turn budget:** use the temporary per-invocation raise (`CLAUDE_IMPL_MAX_TURNS=35` or `40`); do **not** raise the global default. P3.11 hit `error_max_turns` at 26 — two slices is not enough evidence to change the repo default.
- **Final Multi-Party Review Decision block** included in §9 with full Hermes-authority / operator-approval / safety-boundary / next-action wording and reviewer self-disclosure for the cowork harness model/runtime limitation.
