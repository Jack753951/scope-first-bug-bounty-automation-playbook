> Public sanitized edition: sensitive personal, target, path, IP, alias, advisory, and run-specific evidence fields are redacted. This repository does not authorize live testing.

# Claude Code Implementation Result — P3.7 Program-Policy Dry-Run Regression

Date: 2026-05-19
Decision implemented: P3.7 `APPROVE_WITH_CHANGES` / `PASS_WITH_CONDITIONS` from `handoff/cowork_p3_7_direction_review.md`.

## Changed files

- `programs/_examples/sample-lab/scope.json`
  - New synthetic offline program fixture for dry-run regression.
- `scripts/test_recon_program_policy_dry_run.py`
  - New offline unittest coverage for `recon.sh --dry-run --program <slug> --policy-mode planned|live` using temporary HACKLAB state.
- `handoff/cowork_p3_7_direction_review.md`
  - Direction review output from Cowork.
- `handoff/cowork_result.md`
  - Rolling Cowork summary pointer.
- `handoff/claude_code_task_p3_7.md`
  - Named implementation task.
- `handoff/claude_code_task.md`
  - Rolling implementation task pointer.
- `handoff/claude_code_result_p3_7.md`
  - This named implementation summary.
- `handoff/claude_code_result.md`
  - Rolling Claude Code result generated by wrapper; note the worker reached max turns after producing workspace changes, so Hermes completed verification and this named result manually.
- `handoff/active_strategy_queue.md`
  - Updated current navigation after P3.7 direction review.
- `handoff/accepted_changes.md`
  - Append-only acceptance entry for the P3.7 slice.
- `handoff/claude_code_impl_run_20260519_163456.json`
  - Usage/run metadata. Worker exited `error_max_turns` after 36 turns, cost `$3.2401427500000004`, with model usage showing `claude-opus-4-7` and `claude-haiku-4-5-20251001`.
- `handoff/archive/rolling/claude_code_result_20260519_163456.md`
- `handoff/archive/rolling/cowork_result_20260519_162311.md`
  - Rolling handoff archives preserved by the workflow.

No optional docs page was added; the test file docstring and handoff artifacts document the boundary sufficiently for this slice.

## Tests/validation run

- `python -m unittest scripts/test_recon_program_policy_dry_run.py`
  - PASS: `12 tests`, `OK`.
- `python -m unittest scripts/test_recon_program_cli.py scripts/test_program_policy_check.py scripts/test_program_policy_boundary.py scripts/test_recon_program_policy_dry_run.py`
  - PASS: `62 tests`, `OK (skipped=2)`.
- `python -m unittest discover scripts`
  - PASS: `387 tests`, `OK (skipped=8)`.

Hermes still needs to run final repository review, diff checks, and safety scans after this handoff file is written.

## Safety boundary confirmation

Confirmed by local inspection and passing tests:

- The new fixture uses only synthetic/reserved test targets: `authorized.test`, `lab.local`, and RFC 5737 documentation CIDR in a temp-copy test case.
- Tests use a temporary HACKLAB directory and verify real `config/scope.txt` and the committed synthetic fixture hash are unchanged.
- Tests invoke `recon.sh` only under `--dry-run`.
- `--policy-mode live` is covered only as an offline regression path under `--dry-run`.
- No module runner invocation or recon-to-runner coupling was added.
- No runtime code path was modified by the implementation.
- No scanner/module execution, exploit/fuzz/brute force/callback/OAST/proxy/pivot/tunnel behavior, report drafting/submission, platform adapter, credentials, OAuth, scheduler, deployment, billing, or production setting change was introduced.

## Forbidden files untouched

The implementation did not modify:

- `recon.sh`
- `scripts/program_policy_boundary.py`
- `scripts/program_policy_check.py`
- `scripts/module_runner.py`
- `scripts/core/**`
- `modules/**`
- `modules/_schema/**`
- `config/scope.txt`
- `config/recon.conf`
- `bin/hermes`
- `run_hermes_worker.ps1`
- `reports/**`, `loot/**`, `scans/**`, `runs/**`, `evidence/**`

## Deferred assertions or blockers

No blocking issue remains for the tests/fixture-only P3.7 slice.

Deferred or clarified assertions:

- The new malformed-scope test confirms fail-closed semantics by asserting `policy DENY`, `VALIDATOR_DENY`, no policy PASS, and no scanner marker. The current pipeline may still exit 0 after safely producing a summary with zero live hosts; changing that runtime exit behavior is out of scope for P3.7.
- The CIDR test confirms dry-run and policy-limited behavior with synthetic documentation CIDR plus `--allow-cidr`, including port-scan denial by program policy. A stricter forced-deny semantics change would require a separate micro-direction review because runtime code is locked for this slice.
- Stale artifact and explicit target/mode/technique mismatch mutation tests were not added because asserting those through the recon runtime without modifying production code would require a separate harness; existing lower-level tests cover artifact validation paths.

## Reviewer route/tool and visible model/runtime

- Direction review route/tool: Claude Code MAX/OAuth Cowork direction review. Visible model/runtime was self-reported in the review as `claude-opus-4-7`; exact backing runner beyond tool output is not exposed.
- Implementation route/tool: Claude Code MAX/OAuth via `./bin/hermes claude-impl`, then Hermes local verification/fixup after max-turn exit.
- Implementation JSON metadata: `handoff/claude_code_impl_run_20260519_163456.json` reports `error_max_turns`, `num_turns=36`, `total_cost_usd=3.2401427500000004`, model usage for `claude-opus-4-7` and `claude-haiku-4-5-20251001`.
- Hermes runtime/model: gpt-5.5 via openai-codex provider; lower runtime not additionally exposed.

## Next recommended review

Run independent implementation/safety review for P3.7, focused on:

1. Confirm only approved files changed.
2. Confirm `recon.sh`, policy helpers, module runner, configs, schemas, modules, and `config/scope.txt` are untouched.
3. Confirm all validation commands pass.
4. Confirm test vocabulary does not create a live-use affordance or platform/reporting surface.
5. Confirm the max-turn Claude worker output is acceptable because Hermes completed local verification and recorded the named result.
