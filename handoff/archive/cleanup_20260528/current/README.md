> Public sanitized edition: sensitive personal, target, path, IP, alias, advisory, and run-specific evidence fields are redacted. This repository does not authorize live testing.

# Current handoff IPC

Short-lived rolling prompts/results can move here as platform code is updated to read from this directory.

For now, this is a staging destination only. Do not move active `handoff/cowork_task.md` or runner paths here until `bin/hermes` and related scripts are updated and tested.
