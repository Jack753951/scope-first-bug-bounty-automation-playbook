> Public sanitized edition: sensitive personal, target, path, IP, alias, advisory, and run-specific evidence fields are redacted. This repository does not authorize live testing.

# Daily Notes — 2026-05-22

## Phase 4B OWASP/CVE local-lab continuation

Completed delayed continuation run immediately against authorized local Juice Shop lab only: `http://<lab-ip>:3000/`.

Artifacts:

- `<artifact-output-dir>/phase4b_owasp_cve_continuation_20260521T232928Z/`
- `handoff/phase4b_owasp_cve_continuation_run_20260522.md`

Work completed:

- Added/reran `lab_auth_surface_no_bruteforce` for A07 auth-surface metadata without brute force.
- Added/reran `lab_component_metadata_triage` for A06 vulnerable/outdated component metadata clues.
- Added/reran `lab_integrity_metadata_triage` for A08 security/integrity metadata clues.
- Added `lab_api_docs_metrics_manual_verification` mini-bundle for API docs, metrics, and logging/monitoring evidence checklist.

Candidate-only observations:

- `/rest/admin/application-configuration` auth-surface metadata candidate.
- `/rest/admin/application-version` component/version metadata candidate; no CVE claim.
- `/.well-known/security.txt`, `/security.txt`, and `/robots.txt` integrity/security metadata candidates.
- Many package/static/service-worker paths were root/SPA fallback controls.
- `/api/Users` returned 401 access-control observed.

Validation:

- Focused tests passed.
- Generated runners passed `bash -n` and executed successfully.
- Post-run health returned HTTP 200.

Boundary:

No public targets, no credential attempts/brute force, no callbacks/OAST, no exploit payloads, no recursion/crawling, no destructive behavior, no raw secret/loot retention, and no confirmed/reportable finding promotion.


## Cybersec Lab — verified exploit-flow rerun wave 1

- Artifact: `<artifact-output-dir>/verified_flow_wave1_20260521T235533Z/`.
- Promoted lab-only verified flows: SQLi auth bypass + `/api/Users` read, unauth admin config read, `/ftp/` directory listing + file reads, API-docs/metrics exposure.
- Recorded failed/blocked attempts: JWT alg-none oracle invalid, coupon route 401, XSS marker not execution-verified, source maps root fallback, missing host tools for ffuf/nikto/nmap/sqlmap.
- Recovery: no persistent lab state change; post-health HTTP 200.


## End-of-session wrap-up — 2026-05-22T07:01:05Z

Today's main progress:

- Completed DVWA command injection max-impact path from app-level command execution to true attacker-side callback.
- Installed/verified Docker on `<attacker-vm>` with operator help, then closed NAT afterward.
- Proved `DVWA command injection -> www-data command execution -> /tmp marker write/readback -> callback to attacker VM <lab-ip>:18182`.
- Added/updated repo handoff, module bundle, Obsidian notes, accepted changes, active queue, and script inventory for the DVWA callback lane.
- Added `<specific-cve-id>` to the kernel/local candidate lane.
- Pulled NVD/kernel.org/oss-security references and saved the upstream one-line RDS patch.
- Completed non-destructive victim-lab RDS precondition triage: `CONFIG_RDS=m`, RDS module files present, RDS not loaded, Kali kernel package predates upstream fix.

Important blocker:

- `<attacker-vm>` live snapshot attempt for the kernel lane got stuck in VirtualBox `livesnapshotting`; SSH to `<lab-ip>` timed out.
- Hermes cannot issue VM shutdown/poweroff due platform safety blocking.
- Operator must manually recover `<attacker-vm>` before further kernel lane work.

Next safe resume point:

1. Manually recover `<attacker-vm>` from VirtualBox `livesnapshotting`.
2. Verify SSH, Docker, host-only network, and NAT-off state.
3. Use powered-off clone/snapshot, not live snapshot, before <specific-cve-id> RDS module-load or crash-path testing.
4. If taking a break from kernel lane, reuse aggressive-lab Docker callback listener infra for SSRF/XXE/deserialization max-impact callback proofs.

Primary handoff files:

- `handoff/dvwa_command_injection_true_attacker_callback_20260522.md`
- `handoff/cve_2026_43494_kernel_lane_triage_20260522.md`
- `handoff/active_strategy_queue.md`
- `handoff/accepted_changes.md`

Obsidian notes updated:

- `Projects/Cybersec Lab/02_Lab Runs/DVWA Command Injection True Attacker Callback 2026-05-22.md`
- `Projects/Cybersec Lab/02_Lab Runs/<specific-cve-id> Kernel Lane Triage 2026-05-22.md`
- `Projects/Cybersec Lab/00_Index/Active Projects.md`
