# Trial verifier descriptor: parser_validation_checksum
#
# Status: TRIAL INPUT for P2.18 CTF verifier metadata trial. NON-BINDING.
# Not a schema, not a registered contract, not consumed by any runtime.
# Promotion to a versioned JSON Schema is deferred to P2.19+.
#
# Models the Java Script Kiddie lesson: parser-driven verifier that
# walks a captured client-side artifact, applies a deterministic
# parser/checksum routine offline, and emits a single candidate flag.
# No external service. No host execution affordances.
id: parser-validation-checksum
category: web
mode: offline
requires_scope: true
destructive: false
uses_external_service: false
oracle_required: false
kali_required: false
confidence: candidate
second_review_triggers:
  - abnormal_format
  - multiple_candidates
evidence_outputs:
  - raw_artifact
  - candidate
  - verification_log
