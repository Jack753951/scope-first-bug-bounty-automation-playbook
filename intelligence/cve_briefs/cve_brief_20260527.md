> Public sanitized edition: sensitive personal, target, path, IP, alias, advisory, and run-specific evidence fields are redacted. This repository does not authorize live testing.

# CVE briefing — 2026-05-27T02:11

_Window: last **24 h** • Watchlist: nginx, apache, tomcat, openssl, haproxy, envoy, traefik_

> **Source-status note.** `services.nvd.nist.gov` was unreachable from the
> Cowork sandbox (HTTP 403 from upstream proxy), so `cve_watch.py` could not
> populate the brief directly. Findings below were assembled by web search of
> vendor advisory pages (nginx.org, tomcat.apache.org, openssl-library.org,
> httpd.apache.org) and CVE-numbered GitHub PoC repos. Re-run on the user's
> own host (which has direct NVD egress) to get the full NVD payload.

## Last-24h verdict

**No CVE >= CVSS 7.0 against the watchlist was newly published in the last 24 hours.**

The freshest publications against the watchlist were:

- **nginx <specific-cve-id> / -42926 / -42945 / -42946 / -42934 / -40460 / -40701**
  — F5/nginx published a batch of medium-severity advisories several weeks ago
  (buffer overflow in ngx_http_rewrite_module, HTTP/2 request injection in
  ngx_http_proxy_module, buffer overread in scgi/uwsgi, HTTP/3 address spoofing,
  resolver UAF in OCSP). All are fixed in nginx 1.31.0+ / 1.30.1+.

- **Apache Tomcat 11.0.21 batch (2026-04-04)** — <specific-cve-id> (EncryptInterceptor
  bypass, the fix-for-the-fix of <specific-cve-id>), <specific-cve-id> (OCSP soft-fail),
  <specific-cve-id> (k8s bearer-token leak), <specific-cve-id> (JSON access-log injection).
  All medium-importance, made public 9 April 2026.

- **HAProxy <specific-cve-id>** — H3/QUIC standalone-FIN body validation bypass enabling
  cross-user HTTP request smuggling via the backend connection pool when
  `http-reuse always` is configured. PoC public at
  https://github.com/r3verii/<specific-cve-id> . Pre-auth, default-reachable when QUIC
  + pooled backends are in use. Worth a look against any in-scope program that
  fronts services with HAProxy + QUIC.

- **OpenSSL** — last advisory wave dated 2026-01-27 (3.0.18 / 3.3.x series with
  <specific-cve-id> PKCS7_digest_from_attributes ASN1_TYPE type confusion, moderate).
  Nothing new in the last 24h.

- **envoy / traefik** — no new vendor advisory in the last 24h.

## Action

Nothing fires the "drop everything" rule today. If you want any movement,
the most productive 1-hour spend is still **<specific-cve-id>**: chain
`recon_pipeline.py` to surface QUIC/H3-enabled endpoints in your in-scope
asset list, then probe with the r3verii PoC under a controlled lab first
before any in-scope testing.

For the nginx batch, run `nginx_rift_scanner.py` against your in-scope
HTTP(S) endpoints to fingerprint Server / build banners and flag any
version still <= 1.30.0 — that's a clean reachability check for the seven
medium-severity bugs above and a low-effort triage win.

## Files

- This brief: `<private-workspace>\cve_brief_20260527.md`
- Machine-readable: `<private-workspace>\cve_brief_20260527.json`

